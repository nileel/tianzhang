# 设定来源
- 讨论或修改任何设定时，设定原文默认来源于 docs/ 下各子文件夹中的 .txt 文件。

# 游戏类型
- 2D 沙盒世界，战棋玩法的修仙游戏。
- 参考游戏：《龙胤立志传》、《鬼谷八荒》、《觅长生》。

# 设计规范
- 设计功法、术法、神通等游戏内容时，必须先查阅对应的设计规范文档：
  - 功法：`docs/角色养成/功法设计规范.txt`（约束 + 检查清单）+ `docs/角色养成/功法/功法设计.txt`（模版字段说明）
  - 术法：`docs/角色养成/术法设计规范.txt`（约束 + 检查清单）+ `docs/角色养成/术法/术法设计.txt`（模版字段说明）
  - 神通：`docs/角色养成/神通设计规范.txt`（约束 + 检查清单）+ `docs/角色养成/神通/神通设计.txt`（模版字段说明）
# 数值模拟
- 讨论或验证角色数值平衡时，必须运行战斗模拟器：
  - 位置：`simulations/BattleSim/Program.cs`（.NET 10 项目，零依赖）
  - 运行：`dotnet run --no-build -c Release --project "D:\天章游戏开发\simulations\BattleSim"`
  - 首次或无变更时须先编译：`dotnet build -c Release --no-restore "D:\天章游戏开发\simulations\BattleSim"`
- 模拟器包含：修炼引擎（200轮从凡人出发）+ CTB战斗引擎（格挡/魂盾/闪避/暴击/抗性）+ 10Build胜率矩阵。
- 可调参数在 Program.cs 顶部 GameData 类中：BaseGainPerCycle(10.0)、CultivationCycles(200)、BreakthroughBaseRate(0.70)、根骨HP衰减指数(0.75)。
- 数值设计完整文档：`docs/基础设定/角色数值设计.txt`（v3.4版，含次线性HP公式+二级属性重映射）。

# 工作规则
- 思考时尽量使用中文进行推理和分析。
- 设计生成时，非规则描述类的设计内容完成品（如术法、神通、功法、角色等）每个都单独一个文件，不要合并。

# 技术经验

- **编码已统一**：项目所有源文件（.cs, .txt, .md）已统一为 UTF-8 with BOM。直接用 ReadAllText/WriteAllText 即可，无需特殊编码处理。obj/ 下构建产物为自动生成，忽略。
- **模拟器即真理**：任何数值平衡讨论必须先跑 dotnet run -c Release --project simulations/BattleSim 看矩阵输出，不要凭感觉判断。
- **行级编辑优于字符串替换**：`Replace` 易因换行符/空格差异静默失败。拆成数组 `$lines` 按行号精准编辑更可靠。
- **文档跟着模拟器走**：模拟器参数调完，必须同步更新对应的 docs 设计文档（术法/神通倍率等），否则下次对话会出现两边不一致。

- **功法成长表优先于权重**：SubGrowthSum 新增 gongFaName 可选参数，有功法名时直接读取 GongFaTables 字典（值=每境每级点数×小境界数，无需权重缩放），无功法名时回退原有权重近似。BuildDef 新增 GongFaName 字段。
- **守一/符胆建模经验**：守一是"累积型buff"（叠层不消耗，受击消耗1层减伤），适合持续作战；符胆是"消耗型爆发"（使用即清零，需重新叠），金丹期偏弱但元婴/化神解锁更强机制后起飞。两者都应在 `Combat.Simulate` 内用局部变量建模，与川流/逝水/秋水保持一致的初始化模式。
- **BuildDef 追加后 N 自动增长**：`int N = buildDefs.Length` 是动态的，新增 Build 无需手动改 N。
- **PowerShell 批量写 txt**：多行中文内容用 `@'...'@ | Out-File -Encoding UTF8` 写入，`@'` 内可含单引号无需转义。注意结尾 `'@` 必须顶格。
- **模拟器修改先 build 再 run**：修改 Program.cs 后先 `dotnet build --no-restore -c Release` 排查编译错误，再 `dotnet run --no-build` 验证默认参数下输出应与修改前一致（treasureGrade="" 时 bonus=0）。
- **下一步建议清理时机**：条目完成并推送远端后，从 AGENTS.md 移除该条→重编号剩余条目→在当前状态追加新项。不要在提交前删除（万一回退会丢记录）。
- **已有参数优于新增参数**：发现现成接口参数时优先接入，避免新增签名。

# 当前状态

- **v5.0 守一 & 符胆机制已接入**：太一道庭《抱元守一经》守一印记（叠层增神魂攻+受击减伤20%+满层神魂防15%）和《云篆度人经》符胆印记（叠层消耗爆发+满层零耗MP）已在 BattleSim 建模。太一·法修对法系CR 100%，对物系WK 1%（低物防所致）；太一·符修符胆消耗型机制金丹期偏弱（对法·均衡WK 5%）。

- **v5.1 功法成长表差异化**：BuildDef→功法映射已建立，SubGrowthSum优先读取功法文档实际小境界成长表（疾雷破山经/含弘光大典/抱元守一经等9本功法），回退原有权重近似。太一·法修对物·均衡从WK 26%→FV 74%。
- **v5.2 功法属性倾向同步**：BuildDef的weights从手动设定改为从功法文档「属性倾向」星数字段自动推算。星数→归一化权重公式：weight=stars/sum。极端Build弱化（HP平均-27%），均衡Build更全面（物·均衡获魂盾率12%）。总平均回合8.0→10.0。
- **v5.3 符胆机制高阶补全**：消费Bug修复（仅满层消耗）+元婴保留1层/化神2层解锁+云篆神识逐层生效（每层+6%）+天书破防逐层生效（每层+6%）+金丹倍率15%→25%元婴18%→22%。符修对物系胜率0%→20-31%，对中档法系0%→13-18%，定位符箓专精。
- **资质均等化压力测试**（临时资质=14/权重=0.20，11Build全金丹0）：法系绝对压制物系（远程0.35x惩罚+星数权重稀释物防），灵修型全能王(CR 92%+)，太一·符修全败(0%)。总回合10.0→20.7。根因：星数→权重过于均匀，物系Build被迫分配魂攻魂防导致两头不精。
- **模拟器已接入术法/神通 AI**：神通(CD5)>术法(CD3,MP20)>平A，法系有远程优势。金丹同境~6.8回合，筑基~3.1，练气快照~4.2。
- **物/法平衡**：10×10矩阵。新增太一·法修（taiyi泳道，神魂1.4/1.8倍率，对法系CR 95%+）
- **SubGrowth 已对称化**：肉攻=神攻、肉防=神防，物法副属性比率接近一致
- **太一道庭养成体系已补全**：功法1→7（抱元守一经+云篆度人经），术法0→11（练气→炼虚），神通4→7（血脈4+本命2+领域1）。符修《云篆度人经》筑基→化神，符胆叠层机制，与王氏血脈「兰亭遗墨」联动。
- **练气对战**：已加入40轮快照模式，Cultivation.Simulate 支持 maxCycles 可选参数
- **突破率已接入丹药/天材地宝**：Cultivation.Simulate 中 treasureGrade 参数已接入突破率公式（下品+10%/中品+15%/上品+20%/极品+25%）。道具体系设计文档已建立：`docs/道具/`（丹药6境界 + 天材地宝6境界 + 设计规范）。
- **v4.2 水系机制+眩晕已接入**：川流之势减伤(35%,神通触发)、逝水印记叠防(每层-5%物防,最大3~5层)、秋水回血(1%/回合)+濒死护盾(15%HP,1~3次/场)、眩晕(川流劲10%概率,跳过1回合)。水·散修从全劣势→均衡Build，金丹同境平均7.9回合。
- **v5.4 三宗门功法全接入 + 符修崛起**：南华玄感录（太虚观）/绳墨正法录（混元山）功法成长表&星数已加入GongFaTables/GongFaStars。玉清·雷劫（九霄雷劫录，物理反击型对物CR 95%+对法WK 5%）、玉清·苦行（苦行剑典，血剑气物理对法WK 0%）、玉清·雷体（雷池淬体功，肉盾下品门槛低FV 70%均势）、太虚·玄感（南华玄感录，暗水双修debuff清除型）、混元·正法（绳墨正法录，阵型光环型）5个新BuildDef已接入。符修优化：安神符ArtMult 0.5→0.65 + 远程优势（符箓=远程，0.5x惩罚），金丹同境对物系CR 100%、对法·均衡CR 89%、对水·散修WK 38%、对玉清·剑修WK 40%。21Build全矩阵总平均回合9.7。
- **v5.6 雷劫印记机制已接入**：玉清崖《九霄雷劫录》雷劫印记（受击叠层→物理出手消耗全部印记→每层伤害加成→满层魂防-20%）已在 BattleSim 完整建模。leijiePerStack 分境界递增（筑基15%/金丹18%/元婴22%/化神30%/炼虚35%）。玉清·雷劫对物系CR 95%+、对法·均衡CR 92%。
- **v5.7 南华玄感录机制已接入**：太虚观《南华玄感录》debuff清除链（无待→色空→玄同→物物而不物于物）已在 BattleSim 建模。每回合概率清除debuff（20%→80%分境界）+ 神识强度加成（+3→+12）+ 清除回血5% + 玄同免疫控制（1→2回合）+ 灵力消耗-30%。同步修复 v4.2 遗留：川流劲眩晕10%概率触发。太虚·玄感 1v1 环境下 debuff 少、收益不明显（对物·均衡 WK 5%、对法·均衡 EV 54%），在 debuff 密集的 PvE/群战中更有价值。

- **v5.8 苦行剑典血剑气机制已接入**：玉清崖《苦行剑典》血剑气链已在 BattleSim 建模。血剑气替代术法作为主力攻击：HP消耗换高倍率物理伤害（金丹2.2x）+ 无法格挡 + 断陇终结技（目标HP<50%×1.3）+ 无倦回血30% + 防御惩罚30%（对手反击后清除）。玉清·苦行从全败（WK 0%）→对法系CR 87-100%、对物系CR 82%/EV 50%，玻璃大炮定位精准。总平均回合10.0。
- **上次提交**：<待提交> — v5.8 苦行剑典血剑气 + v5.6雷劫 + v5.7玄感重建
# 下一步建议

