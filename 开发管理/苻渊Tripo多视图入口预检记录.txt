# 苻渊 Tripo 多视图入口预检记录

检查日期：2026-08-16
任务：`A-CHAR-TRIPO-MULTIVIEW-PREFLIGHT-01`
页面：`https://studio.tripo3d.ai/workspace/generate`
状态：已完成；唯一结论为“支持修订后的项目严格四视图合同”。

## 当前入口事实

- 创建模式为 `高精度模型`，AI 模型显示 `v3.1 – 最高质量`，预计等待五分钟。
- 多视图模式在同一个输入框中提供四个固定槽位：`前`、`左`、`右`、`后`；四个槽位共用同一个 `生成模型 40` 按钮，会在同一次请求中提交。
- 上传区可见格式为 `JPG`、`PNG`、`WEBP`，图片大小小于 20MB；当前页面没有显示分辨率限制。槽位顺序由前／左／右／后的固定角色决定，没有另行排序控件。
- `几何与纹理` 中，`超清几何精度` 为 off；`纹理生成` 为 on，纹理质量选择 `4K`，另有 `2K` 与 `8K Trial x1`；`PBR` 为 on。
- 拓扑选择 `三角面`；面数控制范围显示 500～1,000,000，当前目标值为 `12000`。
- `分部件生成` 为 off，显示 `Trial x1`；页面外层 `8K 贴图`为 off，显示 `Trial x1`；模型私密性字面量为 `仅分享`。

## 与修订后严格四视图合同的核对

- 负责人已批准把设计 §5.1 修订为同一次请求消费正面 A／T Pose、角色左侧严格正侧面、角色右侧严格正侧面和背面。
- 当前入口的前／左／右／后四个固定角色与修订后合同逐槽一致，不需要自定义角色、拼图或上传探测。
- “左／右”按角色自身方向解释；后续四张输入也使用明确的 `character-left-side`／`character-right-side` 文件名，避免与画面左右混淆。

## 唯一结论与操作边界

- 结论为“支持修订后的项目严格四视图合同”。`A-CHAR-TRIPO-FOURVIEW-01` 可以进入图片制作；其批准、三个严格生成和三个严格槽位验证仍按既有依赖顺序等待，不在本预检中提前执行。
- 本轮没有选择本地图片、上传、拖放、提交生成、扣除 credits，也没有创建、删除、重跑、公开或修改任何 Tripo 任务。
- 不把四图拼成一张图，不恢复旧单图路线，也不从旧模型补渲染缺失视图；前三分之四角度只保留为模型生成后的比较渲染，不属于平台输入。
