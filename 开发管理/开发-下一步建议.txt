# 开发下一步建议（✅ 已审核）

> 2026-07-04 Codex / gpt-5.5：瘦身为开发 backlog 索引。旧版全文已归档至 `开发管理/任务归档/2026-07-04-开发下一步建议瘦身前归档.txt`。
> 日常执行先读 `开发管理/当前任务队列.txt`；分线 backlog 读 `开发管理/任务列表/`。

## 当前开发判断

- 场景架构线已完成 TQ-016、TQ-017、TQ-022、TQ-023：四场景可生成/注册，StartMenu 门派选择已迁入会话流，WorldScene 已有节点式主世界原型，SettlementScene 已有通用据点/宗门信息面板。
- 下一条开发主线是 Settlement/World → Adventure 入口与返回上下文闭环，然后继续拆 `ExplorationController` 中的战斗调度壳。
- 数据链路线已具备独立 `element` 列与 `contentScope` 列；后续重点是导入器/运行时字段保存、检查脚本告警与高阶内容保留边界。
- Unity 机制线已完成符胆首轮回归；雷劫、玄感、含弘/载物仍按独立切片处理。
- 数值线不再把紫府正面对抗金丹 0.0% 当缺口；当前可做的是筑基/紫府 Build 极化诊断，再决定是否调功法成长或 AI 行为。

## Backlog 路由

| 分线 | 文件 | 何时读取 |
|------|------|----------|
| 场景与 Unity | `开发管理/任务列表/场景与Unity任务.txt` | 推进场景、控制器、UI、Unity 机制回归 |
| 数据链路 | `开发管理/任务列表/数据链路任务.txt` | 修改 CSV、asset、导入器、检查脚本 |
| 数值与战斗 | `开发管理/任务列表/数值与战斗任务.txt` | 讨论 BattleSim、平衡、槽位、战斗规则 |
| 内容设计 | `开发管理/任务列表/内容设计任务.txt` | 扩写门派/古修/散修/NPC/经济设定 |
| 审核与交接 | `开发管理/任务列表/审核与交接任务.txt` | 用户发送 `2` 或消费 DeepSeek 交接 |

## 补入当前队列规则

1. `当前任务队列.txt` 维持 5-10 张近期可执行任务卡。
2. 从分线任务列表补入时，必须写清 ID、优先级、主责、类型、状态、必读文件、验证命令和完成条件。
3. 已完成任务不回填长过程；归档到 `开发管理/任务归档/`，只把长期事实同步到 `设计-当前状态.txt`。
