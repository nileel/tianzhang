# 开发下一步建议（索引）

> 日常执行先读 `开发管理/当前任务队列.txt`。本文件不保存完成记录、执行过程或替代任务卡。

## 当前判断

- P0 为 G3 的 TQ-057、TQ-059；G2 已由 TQ-055 关闭。主责、依赖和验证命令只以当前队列为准。
- G1 已通过，P1 架构治理可并行；当前队列已列出 TQ-064（结构事实图）和 TQ-068（最小会话状态）。
- 内容冻结持续有效；不得把内容扩写、内容补全或机制规模扩展补入当前队列。

## 分线入口

| 分线 | 文件 | 仅在何种需要下读取 |
|------|------|--------------------|
| 场景与 Unity | `开发管理/任务列表/场景与Unity任务.txt` | 正式链、运行时所有者、会话状态、控制器或 UI。 |
| 数据链路 | `开发管理/任务列表/数据链路任务.txt` | CSV、asset、导入器、检查器或字段语义。 |
| 数值与战斗 | `开发管理/任务列表/数值与战斗任务.txt` | BattleSim、覆盖、战斗规则或数值结论。 |
| 内容设计 | `开发管理/任务列表/内容设计任务.txt` | 内容冻结解除后，或仅处理已批准的纠错。 |
| 审核与交接 | `开发管理/任务列表/审核与交接任务.txt` | 用户发送 `2`、复审或消费交接。 |

## 补入当前队列的条件

1. 条目满足依赖，范围可在少数轮内验证并提交。
2. 任务卡写明 ID、优先级、主责、类型、状态、必读文件、验证命令和完成条件。
3. P1 任务须在 G1 通过后才可补入；内容任务还须满足内容冻结的解锁条件。
4. 已完成项归档；长期事实只同步到 `开发管理/设计-当前状态.txt`。
