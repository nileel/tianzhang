# 自动工作流状态（✅ 已审核）

> 2026-07-11 Codex：运行锁、检查点和恢复次数已迁移到本机 JSON；本文件只保存项目负责人需要看到的有效结果与阻塞，不记录空转轮次。

## 使用规则

1. 调度、选题、锁与恢复规则见 `开发管理/自动工作流规则.txt`。
2. 本文件不替代任务队列、审核入口、未通过审核清单或 AI 合作沟通。
3. 无任务、有效锁、身份确认失败或人工脏工作区不更新本文件。

## 待你决策

### DEC-20260712-B212D99FBDA4 · G2 极端归因判据

- 事项：在当前距离模型基线的 96 个极端对局中，何种功法包交换反事实证据才足以认定“明确构筑克制来源”？
- 选项 A：任一可复现的非零功法包交换变化即认定为明确来源并分类为设计极端。
- 选项 B：仅当功法包交换使胜率跨越 50% 时认定为明确来源，其余按无明确归因进入缺陷修复。
- 推荐：B。它以可逆胜负关系作为可审计门槛，不把极小变化误判为因果。
- 影响：选 A 可更快解除 G2；选 B 将保留更多需要修复和重跑的极端。
- 创建时间：2026-07-12 22:20（Asia/Hong_Kong）。通知状态：已发送（第 1 次）。
- 严格回复格式：`DEC-20260712-B212D99FBDA4：选 A`（或 `DEC-20260712-B212D99FBDA4：选 B`）。未收到有效回复前不会自行决定。

> 自动控制器出现必须由负责人选择的事项时，在此显示决策编号、事项、选项、推荐项、创建时间、通知状态和严格回复格式；不得写入邮箱地址、搜索条件、令牌或邮件正文。

## 最近有效结果

| 字段 | 值 |
|------|----|
| 迁移前最后结果 | 已保留在旧自动化 memory 与 Git 历史，不复制为滚动状态 |
| 单控制器部署 | 2026-07-11 首次真实小时轮次已完成：复审 HANDOFF-20260710-01 / TQ-038，通过并提交 |
| 最近队列校验 | 2026-07-12 队列维护完成：近期表因待决策项保留而仅有 4 条；已补入无已声明依赖、Codex 主责的 TQ-065。队列恢复为 5 条，TQ-055 仍待负责人决策、TQ-057 仍由 DeepSeek 主责、TQ-059 仍准确标为阻塞，内容冻结保持不变 |
| 最近执行结果 | 2026-07-12 TQ-055 已在当前距离模型下重新验证：400 轮的 21 Build 金丹覆盖均为 200/200、每格 2,000 场且结论为 SUFFICIENT；归因输出改为 96 个极端对局和先天/功法包两层反事实。旧产品决策 A 已归档，但“明确构筑来源”的证据判据未定义，G2 等待 DEC-20260712-B212D99FBDA4 |
| 最近控制器加固 | 2026-07-12 Unity EditMode 运行器已扩展为恢复全部追踪的 `Assets/**` 与 `ProjectSettings/**`，避免 `.asset.meta` 测试副作用中断工作流；同日新增待决策状态、任务标题/状态文件可见入口、Gmail 邮件回执协议与三次通知重试上限，邮件地址仅存本机私有配置 |

## 当前阻塞

当前无 `AUTO-BLOCKED` 项。

## 运行证据

- 逐轮轨迹：Codex 自动化 `memory.md`。
- 业务成果：Git 提交、任务归档和任务卡验证结果。
- 本机租约：`tools/automation-controller-state.ps1 Show`。
