# 自动工作流状态（✅ 已审核）

> 本文件只保留迁移事实、人工阻塞、恢复所有权和最近已核验提交。实时 status 以 automation 配置为准；lease、recovery、pending resume 与 lastResult 以本机 runtime 为准；业务结果以 Git 为准。

## 已确认架构

- 唯一写入入口为 `tzg-hourly-controller`；旧 WF1、WF3、WF4 已归档。每日简报只读。
- 权威设计为 `docs/superpowers/specs/2026-07-18-hourly-automation-simplification-design.md`。
- 旧 controller v2、机器任务注册表、manifest 和中央状态机已退出活动代码；单写入租约、workspace guard、CLI-native session、决策恢复和 Git 元数据保留。
- 外部 AI 继续创建 `businessCommit` 与仅含交接登记的 `handoffCommit`，后者不重复统计。

## 当前人工阻塞与恢复所有权

- 修复期间自动化保持人工暂停；恢复时间由负责人决定，不在本文件复制界面实时值。
- `src/Assets/Tests/EditMode/SpatialQueryBoardTests.cs` 是 Task `U-2_5D-01D` 的自动责任方 session `019f891d-a12a-7230-944a-0b9e1db14220` 留下的未提交半成品，不是人工文件。恢复前不得删除、覆盖或交给新责任方。
- 原责任方当时未创建有效 recovery：修改后误用 guard `Check`、测试未进入目标测试程序集、任务卡漏列新脚本 `.meta`，并把旧 decision binding 误当互斥锁后记录了无 recovery 的 `waiting_decision`。

## 最近已核验提交

- 2026-07-21 手动生产金丝雀完成 TQ-069：`b2ec7930aa2476df82c48cff23e7f22667d83c20`。
- 2026-07-21 自然轮次完成 N-DIST-01：`954f5c8fc92dda8373660671f71190bed094bdb6`；随后完成 N-ENV-01：`5108bbead888d93d836742f532d2fce011f54634`。
- 2026-07-22 队列维护补入 N-GROUP-02A：`fa383fbbbc787e841717d65583bcb359d1c094f4`。控制器当时遗漏报告该 SHA，Git 元数据本身有效。

## 证据位置

- 业务成果：Git 提交、任务归档和任务卡验证结果。
- 调度轨迹：Codex 自动化任务及其 memory。
- 本机租约与恢复：用户级 `tzg-hourly-controller-runtime/runtime.json`。
- 飞书桥状态：用户级 `tzg-feishu-decision-bridge/`。
