# 自动工作流状态（✅ 已审核）

> 本文件只保留迁移事实、人工阻塞、恢复所有权和最近已核验提交。实时 status 以 automation 配置为准；owner run 以本机 schema 5 runtime 为准；进程持有型集成锁以实时 `Show` 为准；业务结果以 Git 为准。

## 已确认架构

- 两个独立写入入口为 `codex-hourly-worker` 与 `deepseek-hourly-trigger`；前者只处理 Codex 路由，后者只调用固定 DeepSeek Windows 入口。每日简报只读。
- 权威设计为 `docs/superpowers/specs/2026-08-01-independent-codex-deepseek-hourly-worktree-design.md`。
- 旧 controller v2、schema 3 长业务租约、通用 external wrapper 生产入口、机器任务注册表、manifest 和中央状态机均退出活动路径。
- 两个 owner 各自在 `.worktrees/automation/<runId>/<owner>` 形成 candidate／canonical；共享进程持有型排他锁只保护一次核验与 fast-forward，不再把短时集成租约写入 runtime。
- DeepSeek 正式结果是一个同时包含业务变化、pending review 投影和交接登记的原子提交，不再创建独立 `handoffCommit`；审核前不解锁依赖。
- 2026-08-13：已确认 `codex-hourly-worker`、DeepSeek 薄触发器、日报和周报的 automation 配置模型均为 `gpt-5.6-terra`；DeepSeek candidate adapter 已由 `deepseek-v4-flash` 切换为 `deepseek-v4-pro`。真实 canary 返回 `DeepSeek V4 Pro 0813`，并核验私有 runtime 隔离、主工作区 HEAD 不变、两个 owner run 为空且集成锁空闲。写入 automation 当前仍按实时配置保持暂停。

## 当前人工阻塞与恢复所有权

- 2026-08-11：Codex run `187a0928-0811-4e9c-b124-fae78681244c` 在 `U-ARCH-REBUILD-01E2` 返回 `failed/codex_checkpoint_invalid` 后进入 `attention_required`。人工核验确认 task digest、run worktree／branch、base 与 HEAD `4dfefe30ac61768f7a068ff873e0d179793834b0` 精确一致，worktree 干净、无 candidate／canonical 提交、活动进程或集成锁；已按 empty-attention 合同精确 `CompleteRun` 并只删除该空 worktree、临时 branch 与空 run 目录，关闭后 schema 5 两个 owner 均为空、集成锁空闲。业务根因是 01E2 生产切换仍缺纯 Combat 移动、换法、七类合法行动契约及两条程序集边界路径，现插入 `01E1A` 并改为 `01E1（已完成） -> 01E1A -> 01E2 -> 01E3`。适配器只把“干净 base、空 checkpoint／paths、显式 detailCode”的非法 `needs_decision` 归入既有 blocked 流程；合法 checkpoint 与证据不一致失败路径的确定性测试已通过。实施期间仅暂停 Codex 小时入口，集成后仍须在最新 master 通过 Codex canary 才可按原配置恢复，DeepSeek 状态与配置不变。
- 2026-08-11：Codex run `3413d1a5-da10-4965-a49c-7e9e0a4381da` 在 `U-ARCH-REBUILD-01E` 返回 `failed/combat_architecture_migration_incomplete` 后进入 `attention_required`；后续六个小时轮次只是重复报告同一 run。人工核验确认任务卡标准化摘要、base `e4fe28215a0c2c56ffc0bf4d6600e638c60b4eac`、candidate branch／HEAD、worktree 注册与清洁状态全部一致，无 candidate／canonical 提交、活动进程或集成锁；已按 empty-attention 合同精确 `CompleteRun`，再核验并删除该空 worktree 与临时 branch。关闭后 schema 5 两个 owner 均为空、集成锁空闲。根因是原 01E 把纯内核、生产切换、遗留删除和总验收压入同一自动候选，且 expectedPaths 漏列 `AdventureSceneController` 与 `CharacterStateTests`；当时先按批准设计重划为 `01E1 -> 01E2 -> 01E3`，未修改 runtime、失败停止、通知或恢复机制，当前依赖链以上一条修订记录为准。
- 2026-08-02：负责人确认独立 Codex／DeepSeek 小时入口方案；固定 Windows 入口 canary 已核验 `pwshMajor=7`、Git、`local_deepseek_gateway`、`deepseek-v4-flash`、私有状态隔离和 project-owned canary worktree 清理，主工作区 HEAD 与状态未被 canary 改变。
- 2026-08-04：独立入口实现保持活动架构；实时 `codex-hourly-worker` 与 `deepseek-hourly-trigger` 的启用状态只以 automation 配置为准。本次受控恢复与 canary 完成前两者保持暂停；schema 5 runtime 已核验为两个 owner 均为空、`activeTaskIds` 为空、进程持有型集成锁空闲。
- 2026-08-04：旧 QueueMaintenance run `3355f006-0335-4413-9404-99a745d4c611` 的候选树只含负责人批准保留的两行 backlog 事实修正；仅修正 Automation 提交元数据后形成 `306212c96f1638059ef3a15bb1532c7ffdaa3b75`，已通过共享集成锁 fast-forward、精确关闭 runtime 并清理该 run 现场。
- 2026-08-04：`U-TZ-CHARTER-SAVE-01A` 按负责人当前 Codex 对话选择 A 人工恢复：补入下游任务卡路径，恢复 `codex_execute/codex/ready` 与原队列位置，同步 backlog，删除旧 checkpoint `2255afc0fe7e5d37e26ec5cba96cf9655385798a`，未创建 `automationReply`。decisionId／PauseDecision 合同与无写入负例随 `3223e574fed32faa3f96bd0919bf3e2ddd097ac5` 集成。
- 2026-08-04：无 runtime 所有权现场逐个取证；`014f81a0-.../deepseek` 与 `04b8d73f-.../codex` 已证明为被拒绝或被正式提交覆盖的冗余现场并精确清理。`51efd154-.../deepseek` 含 master 尚未覆盖的悬赏状态／进度一致性证据，同时其旧实现改变 catalog 必填边界且自身相关测试失败，因此保留现场，不直接集成，后续另卡处理。
- 2026-08-04：run `ce16c825-43e1-42dc-b246-c52f32ac6397` 因 Codex 子会话无结构化终态（`last_agent_message=null`）且无候选提交或文件修改，已按精确关闭合同关闭；其干净 worktree、candidate branch 及旧设计 worktree／branch 均已精确清理，master 中正式设计文档保留。未传 `RepositoryRoot` 的 schema 5 `Show` 在两个 owner 为空时返回 `unknown` 仅表示无范围查询；显式传入 `-RepositoryRoot "D:\天章游戏开发"` 后已核验 `runs.codex=null`、`runs.deepseek=null`、`integrationLockStatus=none`。
- 2026-07-31：负责人先对 `DEC-20260731-TZCHARTERMODEL01` 选择 C，随后在获知 A/B 实际差异后明确改选 A；已授权 Unity 与 BattleSim 共用冲突／跨阶授权契约，并将现有 BattleSim 语义迁为唯一实现。`D-TZ-CHARTER-CONFLICT-01` 已锁定 shared source、契约和迁移边界；`D-TZ-CHARTER-CONFLICT-02` 已完成唯一实现迁移，`U-TZ-CHARTER-MODEL-01` 已解除该前置，等待队列维护建立执行卡，不再等待负责人决定。
- 2026-07-31：负责人对 `D-COMBAT-02` 选择 A，授权独立迁移 BattleSim 范围载体，使圆形显式表达无朝向；`D-COMBAT-02A` 已建立并排队，原跨端 fixture 任务等待该前置完成，不修改既定契约。
- 2026-07-24 00:37（Asia/Hong_Kong）`DEC-20260724-ENVPROFILE` 的 A 回复已形成有效签名 processed 证据；01:19 的旧控制器仍恢复等待决定的原 session，导致该回复未进入责任方。现已取消 decision 原 session 恢复、即时桥接中继、pending resume 队列和独立决定触发器；后续每小时轮次直接读取 A 证据并用新的责任方 session 继续原 TaskId。
- 2026-07-23 01:38（Asia/Hong_Kong）负责人明确恢复自动化；已先清除逻辑暂停并确认 `lease`、`recovery`、`pendingResumes` 均为空，再通过自动化管理接口将 `tzg-hourly-controller` 与 `tzg-daily-automation-briefing` 设为 `ACTIVE`。错过的 01:00 日报按同一只读 Git 数据源手动补跑，不改日常时间表。
- 原责任方 session `019f891d-a12a-7230-944a-0b9e1db14220` 的失联改动已完成恢复处理；当时 runtime 的 `lease`、`recovery` 与 `pendingResumes` 均为空，本地不再有孤立测试文件。

## 最近已核验提交

- 2026-08-04 QueueMaintenance 两行 backlog 事实修正恢复并集成：`306212c96f1638059ef3a15bb1532c7ffdaa3b75`。
- 2026-08-04 小时自动化 decisionId 合同与 `U-TZ-CHARTER-SAVE-01A` 人工 A 恢复：`3223e574fed32faa3f96bd0919bf3e2ddd097ac5`。
- 2026-07-21 手动生产金丝雀完成 TQ-069：`b2ec7930aa2476df82c48cff23e7f22667d83c20`。
- 2026-07-21 自然轮次完成 N-DIST-01：`954f5c8fc92dda8373660671f71190bed094bdb6`；随后完成 N-ENV-01：`5108bbead888d93d836742f532d2fce011f54634`。
- 2026-07-22 队列维护补入 N-GROUP-02A：`fa383fbbbc787e841717d65583bcb359d1c094f4`。控制器当时遗漏报告该 SHA，Git 元数据本身有效。
- 2026-07-23 决策回复与恢复边界修复：`0be26c12bf73e7e8edf3003ca540663933ff6dd0`、`2dcf2c333feca24a7cd6fa5ede540de38ee22e01`。
- 2026-07-23 正式冒险基线回滚：`bb8050e01a1713b00086979eaf4a2c9251304835`；SceneBuilder 重复构建资源修复：`b31e1524f1e18fe87b0ca38d48edf8f937402439`。
- 2026-07-25 U-2_5D-01D 重新实施并归档：`c80b8a9d05dd815488f0ef47669f9e14a7dc335f`；正式 `AdventureScene` 已显式绑定生产环境档案，2026-07-23 的回滚阻塞不再是当前状态。

## 证据位置

- 业务成果：Git 提交、任务归档和任务卡验证结果。
- 调度轨迹：Codex 自动化任务及其 memory。
- 本机 owner run：用户级 `tzg-hourly-controller-runtime/runtime.json`；进程持有型集成锁不写入该文件，以 schema 5 `Show` 的实时结果为准。
- 飞书桥状态：用户级 `tzg-feishu-decision-bridge/`。
