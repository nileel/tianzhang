# 自动工作流状态（✅ 已审核）

> 2026-07-11 Codex：运行锁、检查点和恢复次数已迁移到本机 JSON；本文件只保存项目负责人需要看到的有效结果与阻塞，不记录空转轮次。

## 使用规则

1. 调度、选题、锁与恢复规则见 `开发管理/自动工作流规则.txt`。
2. 本文件不替代任务队列、审核入口、未通过审核清单或 AI 合作沟通。
3. 无任务、有效锁、身份确认失败或人工脏工作区不更新本文件。

## 当前待决策

### DEC-20260714-29A5D1356CC8 · TQ-057 炼神境界处理口径

- 事项：TQ-057 当前任务卡要求补齐 `realm_lianshen` 的语言键并保留引用行；已归档的 DEC-20260713-A07FA708DB22 选择 A 则要求删除所有引用未注册境界的 CSV 行、对应文档和 Unity asset。
- 选项：A 以当前任务卡为准，新增 `realm_lianshen` 语言键并保留引用行；B 以已归档旧决策为准，删除全部 `realm_lianshen` 引用行及对应文档和 Unity asset。
- 推荐：A。它保留现有高境界设计数据，并与 TQ-057 的当前完成条件一致。
- 通知：邮件已成功发送 1 次；有效回复格式为 `DEC-20260714-29A5D1356CC8：选 A` 或 `DEC-20260714-29A5D1356CC8：选 B`。
- 影响：未收到明确选择前，控制器不会修改该批数据。

## 最近有效结果

| 字段 | 值 |
|------|----|
| 迁移前最后结果 | 已保留在旧自动化 memory 与 Git 历史，不复制为滚动状态 |
| 单控制器部署 | 2026-07-11 首次真实小时轮次已完成：复审 HANDOFF-20260710-01 / TQ-038，通过并提交 |
| 最近队列校验 | 2026-07-13 队列维护完成：TQ-057 已按负责人指令转为 Codex / gpt-5.5 主责并继续作为最高优先级；TQ-059 仍准确标为阻塞，内容冻结保持不变 |
| 最近执行结果 | 2026-07-13 TQ-067 已闭环：以 `Foundation → Domain → Combat → Gameplay` 建立项目派生 asmdef 依赖方向；不移动脚本或资产路径，Unity EditMode 66/66 通过 |
| 最近决策 | 2026-07-14 DEC-20260713-A07FA708DB22 已收到有效邮件回复并选择 A：删除仍引用已删除或未注册境界的 CSV 行、对应文档和 Unity asset；TQ-057 已恢复路由，尚未执行数据删除 |
| 最近决策 | 2026-07-14 DEC-20260714-6E0E6BCF974C 已收到有效邮件回复并选择 B：为 11 个古修术法补齐 CSV、语言键和 Unity asset；实现前发现与既有 `realm_lianshen` 处理口径冲突，等待新的明确选择 |
| 最近控制器加固 | 2026-07-12 Unity EditMode 运行器已扩展为恢复全部追踪的 `Assets/**` 与 `ProjectSettings/**`，避免 `.asset.meta` 测试副作用中断工作流；同日新增待决策状态、任务标题/状态文件可见入口、Gmail 邮件回执协议与三次通知重试上限，邮件地址仅存本机私有配置 |

## 当前阻塞

当前无 `AUTO-BLOCKED` 项。

## 运行证据

- 逐轮轨迹：Codex 自动化 `memory.md`。
- 业务成果：Git 提交、任务归档和任务卡验证结果。
- 本机租约：`tools/automation-controller-state.ps1 Show`。
