# 自动工作流状态（✅ 已审核）

> 2026-07-11 Codex：运行锁、检查点和恢复次数已迁移到本机 JSON；本文件只保存项目负责人需要看到的有效结果与阻塞，不记录空转轮次。

## 使用规则

1. 调度、选题、锁与恢复规则见 `开发管理/自动工作流规则.txt`。
2. 本文件不替代任务队列、审核入口、未通过审核清单或 AI 合作沟通。
3. 无任务、有效锁、身份确认失败或人工脏工作区不更新本文件。

## 当前手动接管

### DEC-20260712-5C82B96E9054 · 旧探索场景入口处理

- 关联任务：TQ-065 · 拆分 ExplorationController / SceneBuilder 真实职责边。
- 问题：旧“生成探索场景”菜单仍可创建非正式 ExplorationScene，而正式入口已迁至 AdventureScene。
- A：移除旧菜单和其构建入口，仅保留正式 AdventureScene。
- B：保留旧菜单，但改名并明确标为非正式编辑器样例。
- 推荐：A；可清除竞争入口并收束 SceneBuilder 职责。
- 影响：A 不再提供旧场景快捷生成；B 保留调试便利，但须持续维护其非正式边界。
- 创建时间：2026-07-13 00:22（Asia/Hong_Kong）；邮件通知：已发送（第 1 次）。
- 严格回复：`DEC-20260712-5C82B96E9054：选 A`（或选 B）。未回复前不会自行决定。

> 自动控制器出现必须由负责人选择的事项时，在此显示决策编号、事项、选项、推荐项、创建时间、通知状态和严格回复格式；不得写入邮箱地址、搜索条件、令牌或邮件正文。人工接管项不作为自动化候选。

## 最近有效结果

| 字段 | 值 |
|------|----|
| 迁移前最后结果 | 已保留在旧自动化 memory 与 Git 历史，不复制为滚动状态 |
| 单控制器部署 | 2026-07-11 首次真实小时轮次已完成：复审 HANDOFF-20260710-01 / TQ-038，通过并提交 |
| 最近队列校验 | 2026-07-13 队列维护完成：近期表按少于 5 条条件补入无已声明依赖、Codex 主责的 N-SLOT-01；当前共 5 条，TQ-057 仍由 DeepSeek 主责、TQ-059 仍准确标为阻塞，内容冻结保持不变 |
| 最近执行结果 | 2026-07-12 TQ-055 已由人工接管闭环：修复 BattleSim 未设种子的随机数后，400 轮的 21 Build 金丹覆盖均为 200/200、每格 2,000 场且结论为 SUFFICIENT；98 个极端对局均由先天、功法包或风格机制交换的至少一层可复现归因，G2 已通过；DEC-20260712-B212D99FBDA4 以 manual/A 记录并清除 |
| 最近控制器加固 | 2026-07-12 Unity EditMode 运行器已扩展为恢复全部追踪的 `Assets/**` 与 `ProjectSettings/**`，避免 `.asset.meta` 测试副作用中断工作流；同日新增待决策状态、任务标题/状态文件可见入口、Gmail 邮件回执协议与三次通知重试上限，邮件地址仅存本机私有配置 |

## 当前阻塞

当前无 `AUTO-BLOCKED` 项。

## 运行证据

- 逐轮轨迹：Codex 自动化 `memory.md`。
- 业务成果：Git 提交、任务归档和任务卡验证结果。
- 本机租约：`tools/automation-controller-state.ps1 Show`。
