# 自动工作流状态（✅ 已审核）

> 本文件只保留迁移事实、人工阻塞、恢复所有权和最近已核验提交。实时 status 以 automation 配置为准；lease、batch、recovery 与 lastResult 以本机 schema 4 runtime 为准；业务结果以 Git 为准。

## 已确认架构

- 唯一写入入口为 `tzg-hourly-controller`；旧 WF1、WF3、WF4 已归档。每日简报只读。
- 权威设计为 `docs/superpowers/specs/2026-07-31-parallel-automation-lanes-design.md`。
- 仓库内已迁移为 schema-4 协调器 lease、通用 `lanes[]`、显式 task claim、隔离 Worker worktree 和固定串行集成器；生产配置只允许 Codex 与 DeepSeek V4 Flash，`maxConcurrent=2`。
- Worker 只创建 workerPaths candidate；固定集成器创建 canonical businessCommit。DeepSeek 任务随后形成只含交接登记的 handoffCommit，继续进入 Codex 独立复审。
- 2026-08-01：负责人已通过自动化管理界面同步实时 `tzg-hourly-controller` prompt；实时 prompt 与仓库 canonical prompt 完全一致，`check-automation-workflow.ps1 -RequireActive -RequireLegacyRetired` 通过。专用临时仓库双 Worker canary 已通过，确认两路结果按原队列顺序形成独立 canonical 提交、临时 runtime 在完成后清空，真实主工作区未新增污染；并行生产配置已部署。小时业务调度仍由 `MANUAL-AUTOMATION-DEPLOY-BLOCK` 人工租约临时阻断，当前占用和到期时间以 `Show` 为准。

## 当前人工阻塞与恢复所有权

- 2026-07-31：负责人先对 `DEC-20260731-TZCHARTERMODEL01` 选择 C，随后在获知 A/B 实际差异后明确改选 A；已授权 Unity 与 BattleSim 共用冲突／跨阶授权契约，并将现有 BattleSim 语义迁为唯一实现。`D-TZ-CHARTER-CONFLICT-01` 已锁定 shared source、契约和迁移边界；`D-TZ-CHARTER-CONFLICT-02` 已完成唯一实现迁移，`U-TZ-CHARTER-MODEL-01` 已解除该前置，等待队列维护建立执行卡，不再等待负责人决定。
- 2026-07-31：负责人对 `D-COMBAT-02` 选择 A，授权独立迁移 BattleSim 范围载体，使圆形显式表达无朝向；`D-COMBAT-02A` 已建立并排队，原跨端 fixture 任务等待该前置完成，不修改既定契约。
- 2026-07-24 00:37（Asia/Hong_Kong）`DEC-20260724-ENVPROFILE` 的 A 回复已形成有效签名 processed 证据；01:19 的旧控制器仍恢复等待决定的原 session，导致该回复未进入责任方。现已取消 decision 原 session 恢复、即时桥接中继、pending resume 队列和独立决定触发器；后续每小时轮次直接读取 A 证据并用新的责任方 session 继续原 TaskId。
- 2026-07-23 01:38（Asia/Hong_Kong）负责人明确恢复自动化；已先清除逻辑暂停并确认 `lease`、`recovery`、`pendingResumes` 均为空，再通过自动化管理接口将 `tzg-hourly-controller` 与 `tzg-daily-automation-briefing` 设为 `ACTIVE`。错过的 01:00 日报按同一只读 Git 数据源手动补跑，不改日常时间表。
- 原责任方 session `019f891d-a12a-7230-944a-0b9e1db14220` 的失联改动已完成恢复处理；当时 runtime 的 `lease`、`recovery` 与 `pendingResumes` 均为空，本地不再有孤立测试文件。

## 最近已核验提交

- 2026-07-21 手动生产金丝雀完成 TQ-069：`b2ec7930aa2476df82c48cff23e7f22667d83c20`。
- 2026-07-21 自然轮次完成 N-DIST-01：`954f5c8fc92dda8373660671f71190bed094bdb6`；随后完成 N-ENV-01：`5108bbead888d93d836742f532d2fce011f54634`。
- 2026-07-22 队列维护补入 N-GROUP-02A：`fa383fbbbc787e841717d65583bcb359d1c094f4`。控制器当时遗漏报告该 SHA，Git 元数据本身有效。
- 2026-07-23 决策回复与恢复边界修复：`0be26c12bf73e7e8edf3003ca540663933ff6dd0`、`2dcf2c333feca24a7cd6fa5ede540de38ee22e01`。
- 2026-07-23 正式冒险基线回滚：`bb8050e01a1713b00086979eaf4a2c9251304835`；SceneBuilder 重复构建资源修复：`b31e1524f1e18fe87b0ca38d48edf8f937402439`。
- 2026-07-25 U-2_5D-01D 重新实施并归档：`c80b8a9d05dd815488f0ef47669f9e14a7dc335f`；正式 `AdventureScene` 已显式绑定生产环境档案，2026-07-23 的回滚阻塞不再是当前状态。

## 证据位置

- 业务成果：Git 提交、任务归档和任务卡验证结果。
- 调度轨迹：Codex 自动化任务及其 memory。
- 本机租约与恢复：用户级 `tzg-hourly-controller-runtime/runtime.json`。
- 飞书桥状态：用户级 `tzg-feishu-decision-bridge/`。
