# 自动工作流状态（✅ 已审核）

> 2026-07-11 Codex：运行锁、检查点和恢复次数已迁移到本机 JSON；本文件只保存项目负责人需要看到的有效结果与阻塞，不记录空转轮次。

## 使用规则

1. 调度、选题、锁与恢复规则见 `开发管理/自动工作流规则.txt`。
2. 本文件不替代任务队列、审核入口、未通过审核清单或 AI 合作沟通。
3. 无任务、有效锁、身份确认失败或人工脏工作区不更新本文件。

## 最近有效结果

| 字段 | 值 |
|------|----|
| 迁移前最后结果 | 已保留在旧自动化 memory 与 Git 历史，不复制为滚动状态 |
| 单控制器部署 | 2026-07-11 首次真实小时轮次已完成：复审 HANDOFF-20260710-01 / TQ-038，通过并提交 |
| 最近队列校验 | 2026-07-12 队列维护完成：因队列降至 4 条，补入已登记的 TQ-063；其仍准确标为阻塞（TQ-062），队列恢复为 5 条，内容冻结保持不变 |
| 最近执行结果 | 2026-07-12 TQ-062 实现与测试改动仍在恢复指针中，尚未提交；TQ-063 的解除阻塞结论须待 TQ-062 成功提交后方可生效 |

## 当前阻塞

`tzg-hourly-controller` 已在恢复 TQ-062 时达到第二次恢复失败阈值并进入 `AUTO-BLOCKED`。原因：Unity 留下的 4 个资源元数据改动不在本轮 `expectedPaths` 中：`AdventureAttackHighlight.asset.meta`、`AdventureGround.asset.meta`、`AdventureMoveHighlight.asset.meta`、`AdventureSelected.asset.meta`。人工应先检查并移除或明确纳入这些改动；处理原因后，再用 `tools/automation-controller-state.ps1 ResetBlocked -ErrorMessage '<处理说明>'` 重置控制器。

## 运行证据

- 逐轮轨迹：Codex 自动化 `memory.md`。
- 业务成果：Git 提交、任务归档和任务卡验证结果。
- 本机租约：`tools/automation-controller-state.ps1 Show`。
