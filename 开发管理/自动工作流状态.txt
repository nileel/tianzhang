# 自动工作流状态（✅ 已审核）

> 2026-07-11 Codex：运行锁、检查点和恢复次数已迁移到本机 JSON；本文件只保存项目负责人需要看到的有效结果与阻塞，不记录空转轮次。

## 使用规则

1. 调度、选题、锁与恢复规则见 `开发管理/自动工作流规则.txt`。
2. 本文件不替代任务队列、审核入口、未通过审核清单或 AI 合作沟通。
3. 无任务、有效锁、身份确认失败或人工脏工作区不更新本文件。

## 待你决策

### DEC-20260712-42EECD7ECA53 · G2 极端结果分类与结论

- 事项：现有充分覆盖的 0%/100% 金丹极端对局应按何种产品口径分类，才能决定 G2 是否继续阻塞？
- 选项 A：将具备明确构筑克制来源的极端结果视为设计极端，仅对无机制归因的结果立项修复。
- 选项 B：将任何 0%/100% 金丹结果视为平衡缺陷，全部进入数值修复与受影响重跑。
- 推荐：A。它保留构筑克制，并要求每个结果仍有可追溯归因。
- 影响：选 A 限定后续修复范围；选 B 会扩大调参与重跑范围。
- 创建时间：2026-07-12 20:18（Asia/Hong_Kong）。通知状态：已发送（第 1 次）。
- 严格回复格式：`DEC-20260712-42EECD7ECA53：选 A`（或 `DEC-20260712-42EECD7ECA53：选 B`）。未收到有效回复前不会自行决定。

> 自动控制器出现必须由负责人选择的事项时，在此显示决策编号、事项、选项、推荐项、创建时间、通知状态和严格回复格式；不得写入邮箱地址、搜索条件、令牌或邮件正文。

## 最近有效结果

| 字段 | 值 |
|------|----|
| 迁移前最后结果 | 已保留在旧自动化 memory 与 Git 历史，不复制为滚动状态 |
| 单控制器部署 | 2026-07-11 首次真实小时轮次已完成：复审 HANDOFF-20260710-01 / TQ-038，通过并提交 |
| 最近队列校验 | 2026-07-12 队列维护完成：发现近期表仅 4 条且 TQ-055 仍错误标为阻塞；其四项依赖均已完成，已改为待处理并补入队列。队列恢复为 5 条，TQ-057 仍由 DeepSeek 主责、TQ-059 仍准确标为阻塞，内容冻结保持不变 |
| 最近执行结果 | 2026-07-12 TQ-055 归因审计已完成：400 轮下 21 Build 金丹覆盖均为 200/200、每格 2,000 场；90 个覆盖充分的极端对局已得到先天交换、功法包交换和远程资格统一的反事实输出。工具不改默认数值；极端结果尚待产品分类，G2 继续阻塞 |
| 最近控制器加固 | 2026-07-12 Unity EditMode 运行器已扩展为恢复全部追踪的 `Assets/**` 与 `ProjectSettings/**`，避免 `.asset.meta` 测试副作用中断工作流；同日新增待决策状态、任务标题/状态文件可见入口、Gmail 邮件回执协议与三次通知重试上限，邮件地址仅存本机私有配置 |

## 当前阻塞

当前无 `AUTO-BLOCKED` 项。

## 运行证据

- 逐轮轨迹：Codex 自动化 `memory.md`。
- 业务成果：Git 提交、任务归档和任务卡验证结果。
- 本机租约：`tools/automation-controller-state.ps1 Show`。
