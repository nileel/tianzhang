# 自动工作流状态（✅ 已审核）

> 本文件只保留负责人需要看到的迁移事实、长期阻塞和最近有效生产结果；逐轮轨迹位于自动化 memory 或用户级私有 runtime。

## 当前切换状态

- 生产入口：`tzg-hourly-controller` 仍为 `PAUSED`；旧 WF1、WF3、WF4 仍为 `PAUSED`。
- 权威设计：`docs/superpowers/specs/2026-07-18-hourly-automation-simplification-design.md`。
- 实施分支：`codex/hourly-automation-simplification`；建设 worktree：`D:\天章游戏开发-worktrees\hourly-controller-v2`。
- TQ-057 的五项最终决定已归位到 `开发管理/任务列表/数据链路任务.txt`，当前队列只保留短入口；事实迁移提交为 `6925e35625383b3d6a1b3f18ec23571fe9312483`。
- 旧私有源状态及时间戳备份均保留；本次备份为 `tzg-hourly-controller.pre-simplification.20260718T013707349Z.json` 与 `tzg-hourly-controller-v2.pre-simplification.20260718T013707349Z.json`。
- 新租约、原会话续跑和外部两提交边界目前只有隔离建设证据；尚未取得精简流程的手动生产任务成功或自然整点成功，不得把离线测试表述为生产成果。

## 最近有效生产事实

- 2026-07-17：飞书决策桥保持单实例长连接；选项、卡片自定义输入和普通文本三条真实金丝雀已通过。该事实只证明既有消息通道，不证明本次精简调度。
- 2026-07-11：旧单控制器曾完成一次真实小时复审并提交。该历史结果不作为新薄路由的验收证据。

## 当前阻塞

当前没有需要写入本文件的长期自动化阻塞。生产切换仍受实施计划 Task 7–10 的合并、暂停态手动任务和自然整点观察门禁约束。

## 证据位置

- 业务成果：Git 提交、任务归档和任务卡验证结果。
- 调度轨迹：Codex 自动化 memory。
- 本机租约与恢复：用户级 `tzg-hourly-controller-runtime/runtime.json`。
- 飞书桥状态：用户级 `tzg-feishu-decision-bridge/`。
