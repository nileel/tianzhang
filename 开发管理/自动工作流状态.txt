# 自动工作流状态（✅ 已审核）

> 2026-07-10 Codex / gpt-5.5：新增三小时循环自动工作流状态文件。用途：记录 4 个自动工作流的运行锁、轮次、检查点、重试次数、中断原因和恢复指针；不承载任务事实源、审核结论或 AI 交接正文。

## 使用规则

1. 本文件只记录自动化运行状态，不替代 `当前任务队列.txt`、`审核入口.txt`、`未通过审核清单.txt` 或 `AI合作沟通.txt`。
2. 每个自动工作流启动前必须读取本文件和 `git status --short`。
3. 若存在 6 小时内仍为 `RUNNING` 的锁，后续工作流不得并发修改同一批管理文件，应记录跳过原因并退出。
4. 若上一轮因重连次数耗尽、超时或会话中断停在检查点，下一轮同一工作流优先恢复原任务，不重新选题。
5. 同一任务最多自动恢复 2 次；第 3 次仍失败时标记 `AUTO-BLOCKED`，写明阻塞原因和人工处理入口。
6. 自动化只能 stage 和提交本轮相关文件；遇到无关脏改时不得覆盖或回退。

## 当前全局状态

| 字段 | 值 |
|------|----|
| 当前轮次 | WF3-20260710-123505 |
| 全局锁 | IDLE |
| 锁持有者 | 无 |
| 锁开始时间 | 无 |
| 最近完成阶段 | commit_completed |
| 最近中断原因 | 无（Claude CLI 前台调用超时后子进程继续完成并提交） |
| 当前恢复指针 | 无 |
| 本轮身份模型 | gpt-5.5 |

## 工作流状态

| 工作流 | 计划时间 | 状态 | 当前任务/对象 | 检查点 | 恢复次数 | 最近结果 |
|--------|----------|------|----------------|--------|----------|----------|
| WF1-QUEUE-MAINTENANCE | T+00 | IDLE | 队列维护 | commit_completed | 0 | commit_completed：移出已交接 TQ-038，补入 TQ-048，验证通过 |
| WF2-CODEX-ONE | T+30 | IDLE | TQ-048 / C-STORY-WM-05A | commit_completed | 0 | commit_completed：TQ-048 玄荒新路天才人物志规划骨架完成，验证通过 |
| WF3-CLAUDE-ONE | T+90 | IDLE | TQ-043 / D-IMPORT-01 | commit_completed | 0 | commit_completed：Claude Code 提交 `3c8c1cd` 并进入 HANDOFF-20260710-02；注意本轮选择了 Codex 主责任务，需 WF4/`2` 复审 |
| WF4-CODEX-TWO | T+150 | IDLE | HANDOFF-20260710-01 / TQ-038 | skipped_cleanly | 0 | skipped_cleanly：6小时内已有 WF2-CODEX-ONE RUNNING 锁，未启动复审 |

## 检查点枚举

| 检查点 | 含义 |
|--------|------|
| identity_checked | 已完成身份确认 |
| queues_loaded | 已读取任务、审核和交接入口 |
| task_selected | 已确定本轮任务或复审对象 |
| mutation_started | 已开始修改文件 |
| verification_completed | 已完成本轮必要验证 |
| commit_completed | 已提交本轮相关文件 |
| skipped_cleanly | 因无任务、锁冲突或前置缺失干净退出 |

## 阻塞记录

当前无 `AUTO-BLOCKED` 项。
