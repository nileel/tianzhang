# AI合作沟通（✅ 已审核）

> 用途：本文件只记录当前有效交接线索、疑点、待判断项和协作备忘。
> 历史过程记录已归档至 `开发管理/任务归档/2026-07-12-管理入口瘦身前状态归档.txt`；当前状态以本文件为准。

## 当前交接队列

> 注：本文件交接记录仅反映当前状态；复核完成后移入归档

### C-GZ-ITEM-01 · 关中首批最小物品档案（已排入定向返工）

- 方向：Codex / ChatGPT5.5 → DeepSeek V4 Pro 定向返工
- 任务：`C-GZ-ITEM-01`
- 复审提交：`3df513723b3b013f61ef99c141d89c5d1c33e0da`（前次问题：`REV-C-GZ-ITEM-01-01`）
- 审核结论：不通过；依据见 `REV-C-GZ-ITEM-01-02`。队列维护已确认返工范围完整且预期路径无人工改动冲突，任务卡现为 `route=external_execute`、`owner=deepseek`、`dispatchState=ready`。
- 已确认：稳定 ID、显示名与 `Language.csv` 一致，`Enemies.csv` 的 `enemy_shijiahou` 行引用两项 ID；自然形成、怪物生理、材质与悬赏击杀证据语义均已删除。
- 返工范围：只改两份物品档案的“允许用途”。未来用途可写为掉落或悬赏奖励库存候选；删除当前已进入背包、可计数／存储／查询，或现有掉落／悬赏系统已通过正式 `itemId` 消费的断言。不得修改 CSV、Language、asset、运行时代码或参数。
- 已验证：复审逐项对照任务卡、`Language.csv`／`Enemies.csv`、正式框架 §5.3 和目标提交；`src` 无 `ItemData`，`ImportEnemies` 不读取 `dropTable`，`CreateDropItems` 返回硬编码文本。
- 未验证：未运行 BattleSim（未修改数值或模拟器）或 `check-data-chain.ps1`（未改 CSV／asset）；本轮管理修改由 Codex 运行文本、任务卡和 Git 检查。
- 残留风险：两份物品档案仍不可作为正式掉落或悬赏内容前置；`C-GZ-BOUNTY-01` 继续受 `C-GZ-ENEMY-01` 与本卡双重阻塞。
- 已排定下一步：按上述最小范围重写后，重新标记「⚠️ 已修改/未审核」、转为 `codex_review` 并提交复审。

### C-GZ-ENEMY-01 · 石甲兽正式怪物档案（返工完成，待复审）

- 方向：DeepSeek V4 Pro → Codex / ChatGPT5.5 复审
- 任务：`C-GZ-ENEMY-01`
- 业务提交：`47e61603466c7b5bc1dde8f7de6e0fff1bc0f79a`；问题编号：`REV-C-GZ-ENEMY-01-02`；状态：⚠️ 已修改/未审核
- 已完成：按实施范围白名单重写石甲兽档案。仅保留稳定 ID `enemy_shijiahou`、妖兽类型 `type_yaoshou`、练气期低阶定位、厚甲/迟缓/近战/格挡倾向四项定性识别、`ai_melee` 最小行为语义（空过→接近→相邻攻击→防御）、战斗模板 `Char_Enemy_enemy_shijiahou.asset` 引用、CSV 掉落候选 ID `item_shijia_piece` 和 `item_lingshi_low` 引用、`guanzhong_wild` 单敌遭遇使用范围与可重复进入边界。明确 `ImportEnemies` 不消费候选字段、`CreateDropItems` 按 `realmMultiplier` 返回硬编码掉落。
- 已验证：逐句对照任务卡实施范围白名单、`Enemies.csv`/`Language.csv` 行、`Char_Enemy_enemy_shijiahou.asset`、`adventure_guanzhong_wild.txt`、EnemyData 框架 §5.2、`ImportEnemies` 与 `CreateDropItems` 当前消费者边界，确认无 CT/行动条、暴击、闪避、魂盾、结构化结算断言、生态/材质/生理/经济补写残留；`git diff --check` 通过。
- 未验证：`check-review-text.ps1`（权限受限）；未运行 BattleSim（未修改数值或模拟器）；未运行 `check-data-chain.ps1`（未改 CSV/asset）。
- 残留风险：石甲兽档案仍为最小身份档案，不描述运行时掉落结算；在 Codex 复审通过前不得作为 `EnemyData` 运行时消费或悬赏行为的事实源。掉落候选仍等待 `N-GZ-REWARD-01` 锁定概率与数量。
- 已排定下一步：Codex 复审 `47e61603466c7b5bc1dde8f7de6e0fff1bc0f79a`；通过后归档并解除 `D-ENEMY-01` 和 `C-GZ-BOUNTY-01` 的内容前置。

## 短格式
