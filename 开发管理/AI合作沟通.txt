# AI合作沟通（✅ 已审核）

> 用途：本文件只记录当前有效交接线索、疑点、待判断项和协作备忘。
> 历史过程记录已归档至 `开发管理/任务归档/2026-07-12-管理入口瘦身前状态归档.txt`；当前状态以本文件为准。

## 当前交接队列

> 注：本文件交接记录仅反映当前状态；复核完成后移入归档

### C-GZ-ITEM-01 · 关中首批最小物品档案（已排入定向返工）

- 方向：Codex / ChatGPT5.5 → DeepSeek V4 Pro 定向返工
- 任务：`C-GZ-ITEM-01`
- 复审提交：`3df513723b3b013f61ef99c141d89c5d1c33e0da`（前次问题：`REV-C-GZ-ITEM-01-01`）
- 审核结论：不通过；依据见 `REV-C-GZ-ITEM-01-02`。队列维护已确认返工范围完整且预期路径无人工改动冲突，任务卡现为 `route=external_execute`、`owner=deepseek`、`dispatchState=ready`。
- 已确认：稳定 ID、显示名与 `Language.csv` 一致，`Enemies.csv` 的 `enemy_shijiahou` 行引用两项 ID；自然形成、怪物生理、材质与悬赏击杀证据语义均已删除。
- 返工范围：只改两份物品档案的“允许用途”。未来用途可写为掉落或悬赏奖励库存候选；删除当前已进入背包、可计数／存储／查询，或现有掉落／悬赏系统已通过正式 `itemId` 消费的断言。不得修改 CSV、Language、asset、运行时代码或参数。
- 已验证：复审逐项对照任务卡、`Language.csv`／`Enemies.csv`、正式框架 §5.3 和目标提交；`src` 无 `ItemData`，`ImportEnemies` 不读取 `dropTable`，`CreateDropItems` 返回硬编码文本。
- 未验证：未运行 BattleSim（未修改数值或模拟器）或 `check-data-chain.ps1`（未改 CSV／asset）；本轮管理修改由 Codex 运行文本、任务卡和 Git 检查。
- 残留风险：两份物品档案仍不可作为正式掉落或悬赏内容前置；`C-GZ-BOUNTY-01` 继续受 `C-GZ-ENEMY-01` 与本卡双重阻塞。
- 已排定下一步：按上述最小范围重写后，重新标记「⚠️ 已修改/未审核」、转为 `codex_review` 并提交复审。

### C-GZ-ENEMY-01 · 石甲兽正式怪物档案（复审不通过，待定向返工）

- 方向：Codex / ChatGPT5.5 → DeepSeek V4 Pro 定向返工
- 任务：`C-GZ-ENEMY-01`
- 复审提交：`47e61603466c7b5bc1dde8f7de6e0fff1bc0f79a`；问题编号：`REV-C-GZ-ENEMY-01-03`
- 审核结论：不通过；任务卡已设为 `route=external_execute`、`owner=deepseek`、`dispatchState=blocked` 并移出当前队列。
- 已确认：`enemy_shijiahou`、`type_yaoshou`、`ai_melee`、`realm_lianqi`、两项候选物品 ID、空术法／神通列表和 `Char_Enemy_enemy_shijiahou.asset` 均可由 Enemies／Language、现行模板和正式框架对应；`guanzhong_wild` 只配置一个石甲兽遭遇，且可重复从关中城进入。
- 返工范围：只重写石甲兽档案。四项识别只保留厚甲、迟缓、近战和格挡倾向标签，不展开岩质甲壳、体态、头角、侧甲或四肢；AI 只保留接近、相邻基础攻击、不能行动时防御，不写被控制或防御加成。删除禁止项和引用链中未落地的 `dropEntries`；掉落只保留两项候选 ID、当前消费者不读取候选结构以及概率／数量等待 `N-GZ-REWARD-01`。不得修改 CSV、Language、asset、场景、导入器、AI 或战斗代码。
- 已验证：Codex 独立核对提交 diff、任务卡白名单、未通过项、Enemies／Language 目标行、CharacterData 模板、EnemyData 框架 §5.2、`adventure_guanzhong_wild.txt`、`ImportEnemies` 和 `CreateDropItems`；目标档案扫描命中上述超范围内容，提交差异的 `git diff --check` 通过。
- 未验证：未运行 BattleSim（未修改数值或模拟器）或 `check-data-chain.ps1`（未改 CSV／asset）；管理状态修改的文本和任务卡检查由本轮 Codex 执行。
- 残留风险：石甲兽档案在重新通过独立复审前不得作为正式 EnemyData、掉落或悬赏行为的事实源；`D-ENEMY-01` 与 `C-GZ-BOUNTY-01` 继续受阻。
- 已排定下一步：队列维护解除本问题的阻塞后，DeepSeek 仅按上述范围重写档案、标记「⚠️ 已修改/未审核」并转为 `codex_review` 提交复审。

## 短格式
