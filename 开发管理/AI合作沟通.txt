# AI合作沟通（✅ 已审核）

> 用途：本文件只记录当前有效交接线索、疑点、待判断项和协作备忘。
> 历史过程记录已归档至 `开发管理/任务归档/2026-07-12-管理入口瘦身前状态归档.txt`；当前状态以本文件为准。

## 当前交接队列

### C-GZ-ITEM-01 · 关中首批最小物品档案（复审仍不通过，待返工）

- 方向：Codex / ChatGPT5.5 → DeepSeek V4 Pro 定向返工
- 任务：`C-GZ-ITEM-01`
- 复审提交：`74977b6438d7bb8c734922ddcf777b9ffe5c1e0f`（前次退回提交：`22b7967d733d2769d6998e9f09e9754ef70a5add`）
- 审核结论：不通过；任务卡已设为 `blocked` 并移出当前队列，依据见 `REV-C-GZ-ITEM-01-01`。
- 已确认：稳定 ID、显示名与 `Language.csv` 一致；`Enemies.csv` 的 `enemy_shijiahou` 行引用两项物品；错误风隼引用、唯一掉落来源、显式经济／修炼效果和晶化／矿化断言已删除。
- 返工范围：仅重写 `docs/道具/基础资源/劣质灵石.txt` 和 `docs/道具/妖兽材料/石甲碎片.txt` 的描述、来源和允许用途。删除自然生成、低阶妖兽活动区、灵矿边缘、甲壳受击碎裂、体表和外观等无直接事实支持的来源／材质语义；删除“悬赏击杀证据”用途。仅保留稳定 ID、显示名、类别、可堆叠身份、CSV 候选引用与未来掉落／悬赏奖励库存用途；不得修改 CSV、Language、asset、运行时代码或参数。
- 已验证：复审逐项对照任务卡、`REV-C-GZ-ITEM-01-01`、`Language.csv`／`Enemies.csv`、已不通过的石甲兽档案、正式框架 §5.3 与目标提交；目标提交的 `git diff --check` 通过。
- 未验证：未运行 BattleSim（未修改数值或模拟器）、`check-data-chain.ps1`（未改 CSV／asset）；本轮管理修改完成后由 Codex 运行文本与任务卡检查。
- 残留风险：两份物品档案仍不可作为正式掉落或悬赏内容前置；`C-GZ-BOUNTY-01` 继续受 `C-GZ-ENEMY-01` 与本卡双重阻塞。
- 建议下一步：按上述最小范围重写后，重新标记「⚠️ 已修改/未审核」、转为 `codex_review` 并提交复审；不要修改 CSV、Language、asset、背包、掉落、悬赏、商店或存档代码。

### C-GZ-ENEMY-01 · 石甲兽正式怪物档案（复审不通过，待定向返工）

- 方向：Codex / ChatGPT5.5 → DeepSeek V4 Pro 定向返工
- 任务：`C-GZ-ENEMY-01`
- 复审目标提交：`3d984a0114c680669e2de5ac0cd4b9b37c22d7e2`；问题编号：`REV-C-GZ-ENEMY-01-02`
- 复审结论：不通过。`石甲兽.txt` 仍把 CSV 候选写成当前结构化掉落结算；实际敌人导入不消费 `dropTable`，战斗结算仍按 `realmMultiplier` 返回硬编码掉落。战斗识别段还保留任务卡未授权的 CT、暴击、闪避和魂盾断言。
- 返工范围：仅重写 `docs/怪物/关陇玄域/石甲兽.txt`。保留稳定 ID、妖兽类型、练气期低阶、关中野外单敌用途、`ai_melee` 最小行为、战斗模板引用、无装备能力和 CSV 候选 ID；删除当前结构化掉落／CSV 消费断言及范围外 CT、暴击、闪避、魂盾断言。掉落只保留候选 ID，并说明概率与数量由 `N-GZ-REWARD-01` 锁定。
- 已验证：Codex 已独立对照任务卡、`REV-C-GZ-ENEMY-01-01`、返工提交、`Enemies.csv`／`Language.csv`、现行 asset、EnemyData 框架、`adventure_guanzhong_wild.txt` 与当前导入／掉落代码；`check-review-text.ps1 -Paths docs,开发管理`、`check-task-cards.ps1 -TaskId C-GZ-ENEMY-01 -Postcondition CodexClosedOrNonReady` 和 `git diff --check` 均通过。
- 未验证：未运行 BattleSim（未修改数值或模拟器）；未运行 `check-data-chain.ps1`（未改 CSV／asset）。
- 残留风险：在定向返工完成并再次复审前，石甲兽档案不得作为 `EnemyData`、掉落或悬赏运行时行为的事实源。
- 建议下一步：按上述最小范围返工后，以同一卡标记「⚠️ 已修改/未审核」并转为 `codex_review`；不得修改 CSV、Language、asset、场景、AI 或战斗代码。

## 短格式
