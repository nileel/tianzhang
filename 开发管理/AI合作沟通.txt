# AI合作沟通（⚠️ 已修改/未审核；修改方：DeepSeek V4 Pro 0813；变更范围：C-JD-SIXROAD-RENAME-01 返工交接登记）

> 用途：本文件只记录当前有效交接线索、疑点、待判断项和协作备忘。
> 历史过程记录按日期归档至 `开发管理/AI合作归档/`；当前调度事实以 `开发管理/当前任务队列.txt` 与匹配任务卡为准。

## 当前交接队列

- [C-JD-SIXROAD-RENAME-01] 六识道路更名与重定位（闻识保留）——返工完成，转入待复审，请求 Codex 独立复审。
  - 方向：按 2026-08-20 Codex 复审正式提交 `c49c590739ca44fbe26ff27ab8a8b204864942d4` 的部分通过结论返工，只处理未通过审核清单列明的两项，已通过范围未扩大修改。
  - 任务/提交范围：2026-08-20 用户选择 DeepSeek V4 Pro 0813 返工（同一卡 `external_execute/deepseek/ready` 重新入队）；手动纯 `1` 选中后在 `.worktrees/manual/C-JD-SIXROAD-RENAME-01-rework-01` 任务分支实施，经 `tools/invoke-project-integration.ps1` 排他锁 fast-forward 集成。
  - 修改文件：`开发管理/任务卡/C-HS-YY-JD-01G.txt`（补齐闻识根本权限与源位直接操作，原为空）、`C-HS-YY-JD-01H.txt`（临时代号前缀改为现行「灵-」）、`C-HS-YY-JD-01I.txt`（不可越界项、源位理念与临时代号按现行矩阵 `ROAD_SOUL` 行同步）、`C-HS-YY-JD-01J.txt`（临时代号前缀改为现行「形-」）；`开发管理/C-LORE-01-旧口径迁移映射表.txt` §八 新增十二个改名 positionId 确定性迁移策略（既有占据/候选证位进度/当前装配/持续战棋实例/主场景工程/山河实例六类逐格处理）与退役/合并/重定义效果读档策略；`docs/基础设定/金丹位格/金丹位格索引.txt` §五迁移注记改指确定性策略；本卡 META 转 `codex_review/codex/ready`；`当前任务队列.txt` 与 backlog 投影同步；`未通过审核清单.txt` 状态行更新。
  - 已验证：五张下游卡按现行矩阵 §二/§五 逐行核对，无旧道路名/road_id/effect_id/旧临时代号残留；`check-task-cards.ps1`（ExternalPendingReview 通过）、`check-review-text.ps1 -Paths docs,开发管理`、`check-pending-whitespace.ps1`、`git diff --check` 通过；旧名全文扫描仅剩任务卡验证节自身扫描清单、迁移映射表与历史审核证据。
  - 未验证：未运行 BattleSim（不涉及数值/运行时）；未运行 check-data-chain（无 CSV/asset 变化）；未运行 Unity 构建/测试（docs 与管理文件变更）。
  - 残留风险：①魂识 3 席位的位置专属成就进度按迁移策略清零（旧成就以退役吸收类效果为对象，不折算为新效果成就），采用「清零」而非其他折算方式属产品语义裁决，请 Codex 确认；②迁移策略对退役效果关闭时点取「读档时关闭」（而非周期边界），实现层以运行时落地为准；③01F 本轮无改动（已与新口径一致），01G 仅补两处空字段。
  - 请求判断：请 Codex 独立复审本次返工 diff，重点确认：五张下游卡与现行矩阵逐项一致；12 个 positionId 六类存档对象的确定性处理是否可执行且无互相回指；退役/合并/重定义效果的读档、关闭与迁移策略是否满足存档迁移要求。
  - 建议下一步：复审通过后按审核规则关闭本卡并归档；未通过按普通返工处理，不设置独立重命名阶段。