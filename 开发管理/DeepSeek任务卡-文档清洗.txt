# DeepSeek任务卡：文档清洗（✅ 已审核）

> 2026-06-17 Codex / ChatGPT5.5 复审通过：由 `DeepSeek工作提示词.txt` 拆分。依据：40d5887。

## 适用范围

修复 `.txt` 中的转义残留、编码污染、字段缺失、编号错乱、表格格式问题。

## 必读

- `AGENTS.md`
- `开发管理/审核入口.txt`
- 本次任务涉及的具体文件
- 若清洗项来自未通过审核清单，读取 `开发管理/未通过审核清单.txt` 对应编号

## 执行要求

1. 只修复明确的文本污染、转义残留、编码异常、编号错乱、表格格式问题，不顺手改设定内容。
2. 对每个疑似污染符号逐项判断语义，例如 `?` 可能是污染的 `−`，也可能是半星占位或问号；不能批量无脑替换。
3. 修改前先列出待修文件、污染类型、预计替换规则和不会处理的边界情况。
4. 修改后用 `rg` 复查残留，例如 `?`、`` `r ``、`` `n ``、`\r\n`、乱码符号。
5. 修改状态统一标为「⚠️ 已修改/未审核；修改方：DeepSeek；变更范围：...」。
6. 输出最后结果时列出每个文件的变更摘要、复查命令、未处理风险。
