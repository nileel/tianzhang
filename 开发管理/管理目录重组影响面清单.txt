# 管理目录重组影响面清单（M-MGMT-01A）

## 当前状态（2026-08-09）

- 负责人已选择 `M-MGMT-01B` 的 **C：不批准**；`M-MGMT-01` 已关闭，不实施目录重组，所有现有管理路径保持不变。
- 本文件仅保留为历史影响面扫描证据，不再定义未来实施边界。未来若重新提出目录治理需求，须基于当时事实建立新任务和新扫描，不得沿用本清单的旧基线。

## 原基线结论（历史）

- 本清单原用于定义 `M-MGMT-01` 的实施边界和检查口径；截至本卡完成，未获批目录重组，未移动、重命名、删除或批量改写任何既有路径。
- 基线扫描在新建本文件前执行：`rg -n "开发管理/" AGENTS.md CLAUDE.md tools 开发管理`。结果为 541 条字面量命中、98 个源文件（仓库入口 2、`tools/` 6、`开发管理/` 90）。本文件写入后重跑同一命令会额外命中本清单自身的证据路径，不能与 541 条基线计数直接比较。
- 运行基线为 `d66fa11`；工作区已有 `tools/check-automation-workflow.ps1`、`tools/test-check-automation-workflow.ps1` 与 `开发管理/自动工作流控制器提示词.txt` 的无关未提交改动，均不在本卡授权路径内，也未纳入本卡提交。
- 分类含义：**规则**是稳定流程或路由约束，**当前**是执行中的事实、队列或协作状态，**归档**是历史材料。仓库根入口和测试夹具不是管理目录迁移目标。

## 字面量读取方与写入方

| 源文件／角色 | 字面量角色 | 影响面与后续处理 |
|---|---|---|
| `AGENTS.md`、`CLAUDE.md` | 读取方：为责任方路由规则、队列、审核、交接与自动工作流入口 | 迁移时最后更新其稳定入口；两者是仓库根文件，不归入 `规则/当前/归档` 目标目录。 |
| `tools/invoke-codex-responsibility.ps1` | 读取方：把纯 `1`、纯 `2`、队列维护的入口路径写入责任方指令 | 更新必须与对应入口的实际新路径同一提交完成；不得在调用器增加兼容分支。 |
| `tools/check-automation-workflow.ps1` | 读取方：以四个固定路径读取控制器提示词、规则、状态和简报提示词 | 更新后运行其直接检查；它是自动化路径契约，不得靠运行时回退掩盖错误。 |
| `tools/check-pwsh-runtime.ps1` | 读取方：固定读取管理规则文件，并以 `开发管理/任务列表` 为目录根枚举所有 `.txt` | 目录级解析是动态依赖，须先给出唯一新根路径，再同步检查器和测试；不能只替换若干字面量。 |
| `tools/test-check-automation-workflow.ps1`、`tools/test-check-pwsh-runtime.ps1` | 测试夹具写入方：只向临时测试仓库写入管理路径，不写项目当前状态 | 与被测脚本同一切片更新；夹具路径不得被误当作生产写入方。 |
| `tools/test-external-ai-self-commit.ps1` | 测试夹具读／写方：在临时仓库验证当前队列和交接文件的路径隔离 | 若当前队列或交接入口改名，须同步更新其 canary 和提交路径断言。 |
| `开发管理/` 下命中的 90 个文档 | 文档读取方／链接方：当前入口互相路由，历史材料链接当前或归档材料 | 未来实施卡必须复跑本清单的基线命令，逐一将拟改写来源列入 `ExpectedPaths`；不把业务事实源扩展进来。 |

## 稳定目标路径分类与迁移次序

| 稳定路径 | 当前读／写语义 | 目标分类 | 迁移次序 |
|---|---|---|---|
| `开发管理/开发-技术经验.txt`、`状态与建议维护规则.txt`、`自动工作流规则.txt`、`自动工作流控制器提示词.txt`、`自动化简报提示词.txt`、`总结规则.txt` | 稳定规则与自动化路由读取；当前无项目内写入器应在重组中单独修改它们 | 规则 | 2：先固定唯一新映射，再与脚本契约同步改写 |
| `开发管理/审核入口.txt`、`审核规则.txt`、`AI协作规则.txt`、`DeepSeek工作提示词.txt`、`DeepSeek任务卡-局部代码实现.txt`、`DeepSeek任务卡-批量设计内容.txt`、`DeepSeek任务卡-文档清洗.txt`、`DeepSeek任务卡-CSV数据链路.txt` | 审核、交接、职责和外部执行路由的读取方；其中规则定义当前写入边界 | 规则 | 2：与根入口、调用器和审核路由同时更新 |
| `开发管理/当前任务队列.txt`、`设计-当前状态.txt`、`开发优先级.txt`、`设计总结.txt`、`开发-下一步建议.txt`、`设计-下一步建议.txt` | 当前队列、状态与计划事实；责任方按规则写入其中允许的文件 | 当前 | 3：规则及调用器路径已落位后，按任务状态维护规则迁移 |
| `开发管理/任务列表/场景与Unity任务.txt`、`内容设计任务.txt`、`数据链路任务.txt`、`数值与战斗任务.txt`、`审核与交接任务.txt` | 当前 backlog／任务卡读取与维护；`任务列表/` 同时被 `check-pwsh-runtime.ps1` 动态枚举 | 当前 | 3：先处理目录枚举根和测试，再移动完整目录；不拆成第二套任务目录 |
| `开发管理/未通过审核清单.txt`、`AI合作沟通.txt`、`自动工作流状态.txt` | 当前复审、交接和长期自动化事实；规则限定其写入方 | 当前 | 3：与审核入口、自动工作流契约和路径隔离检查一起迁移 |
| `开发管理/2.5D共享空间查询契约对齐方案.txt`、`2.5D战棋原型规则查询所有者记录.txt`、`2.5D战棋原型验证清单.txt`、`2v2范围技能协同走位与团队AI参数事实源及决策输入.txt`、`场景架构Unity现状清单.txt`、`高阶内容现行引用分类与决策输入.txt`、`古修术法神通缺口清单.txt`、`炼神术法神通资料片隔离验证记录.txt`、`普通NPC首批候选清单.txt`、`散修通用内容补强清单.txt`、`运行时所有者记录-TQ-060.txt`、`C-LORE-01-旧口径迁移映射表.txt`、`Codex技能筛选结果.txt`、`docs-csv-asset-alignment.txt`、`NPC叙事字段检查清单.txt` | 仍被当前任务卡或规则引用的活跃证据、决策输入和验证记录；不属于稳定规则 | 当前 | 4：逐份由 M-MGMT-01 的映射表确认位置；未分类文件不得移动 |
| `开发管理/任务归档/`、`AI合作归档/` | 历史任务、审核和交接链接；只为追溯，不得作为当前路由事实 | 归档 | 5：在所有当前入口及脚本通过后整体迁移；逐条验证活跃入口和归档内相对／绝对链接仍可访问 |
| `开发管理/任务列表/历史归档/` | 仅由 PowerShell 运行时检查的临时夹具覆盖到；不是当前项目的生产任务目录 | 归档／夹具前置 | 5：仅在实际存在并进入映射时处理；同步测试夹具，不把测试路径写入生产路由 |

## 原 M-MGMT-01 的授权边界（历史）

1. 负责人先批准唯一的“旧路径 → 新路径”映射和本清单的三类分类；未批准前不得开始移动目录。
2. 允许修改范围只能是映射中列出的管理文件、`AGENTS.md`、`CLAUDE.md`、上述 6 个 `tools/` 文件及其直接测试。每次提交的 `ExpectedPaths` 必须由重新扫描得到，不能用目录级通配扩大范围。
3. 禁止修改用户级自动化配置、私有 runtime、租约、任务调度状态、业务 docs／CSV／Unity／BattleSim 事实，禁止增加重定向、默认路径、兼容层或第二套目录。
4. 若 `任务列表/` 以外还发现动态路径拼接、外部配置中的路径，或归档链接无法在不扩展到业务事实源的情况下恢复可访问性，停止 M-MGMT-01，保留扫描证据并请求负责人决定。

## 原 M-MGMT-01 检查口径（历史）

1. 修改前：重新执行 `rg -n "开发管理/" AGENTS.md CLAUDE.md tools 开发管理`，保存待改写源文件及其新旧映射；运行 workspace guard，仅允许该列表中的精确路径。
2. 规则与当前路径更新后：运行 `pwsh -NoProfile -ExecutionPolicy Bypass -File tools/check-automation-workflow.ps1`、`pwsh -NoProfile -ExecutionPolicy Bypass -File tools/test-check-automation-workflow.ps1`、`pwsh -NoProfile -ExecutionPolicy Bypass -File tools/check-pwsh-runtime.ps1` 与 `pwsh -NoProfile -ExecutionPolicy Bypass -File tools/test-check-pwsh-runtime.ps1`。这些命令直接覆盖自动化和动态任务列表边界。
3. 归档迁移后：对映射中的活跃入口及归档链接执行精确存在性检查；不得以“历史文件不再读取”为由留下断链。
4. 提交前：运行 `pwsh -NoProfile -ExecutionPolicy Bypass -File tools/check-review-text.ps1 -Paths <本次精确路径>`、`tools/check-pending-whitespace.ps1 -ExpectedPaths <同一精确路径>`、workspace guard `Verify` 与 `git diff --cached --check`。最终再次执行基线 `rg` 命令，确保每个保留的旧路径均有明确的归档理由，不能以未扫描到为由删除。
