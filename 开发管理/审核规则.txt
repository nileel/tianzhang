# 审核规则（✅ 已审核）

## 基本规则

- **两步操作**：所有修改必须先「修改」→标注为「⚠️ 已修改/未审核」，再由另一个 AI（或同一 AI 在后续对话中）进行「审核」→标注为「✅ 已审核」后方可视为完成。任何 AI 不得将未审核的修改标记为已完成。
- **默认审核方**：用户未另行指定时，审核方默认定为 ChatGPT5.5 / Codex；执行修改的 AI 不得在同一轮对自己的修改标记「✅ 已审核」。
- **审核追踪**：未审核标记必须附修改方、提交号或变更范围；审核方优先按 git diff、指定提交和文件清单复核，不做无差别全仓重读。设计内容须核对对应规范与 docs 原文；数值结论须运行 BattleSim；代码改动须按影响面运行相关测试。
- **审核记录归档**：审核通过后，应移除文档内的临时审核记录；文档只保留当前有效内容和必要的简短状态，详细审核过程、验证命令、修改方/审核方与依据应写入 git commit log、PR 描述或审核回复，不在正文中长期累积。

## 未通过审核清单

> 用途：记录 2026-06-16 对既有未审核内容复核后的未通过项，方便后续逐项修复。处理并审核通过后，应按“审核记录归档”规则从本清单移除。

1. `开发管理/docs-csv-asset-alignment.txt` 口径错误：
   - 实际 docs/CSV 内容数量为功法 24、术法 75、神通 34；清单仍写术法 73、CSV 76、CSV 多 3 条无 doc。
   - Unity 资产不应只统计 `src/Assets/DataConfig`，实际生成目标在 `src/Assets/Data/`；当前约为功法 14、术法 54、神通 23，不是 0。
   - CSV→Unity `.asset` 仍缺功法 11、术法 28、神通 13；另有旧版/额外 asset 需判定保留或清理。
2. `docs/角色养成/术法设计规范.txt` 未清洁：
   - 第 26 行仍有 `` `n `` 转义残留。
   - 第 28 行仍有 `本命升格\ —\` 反斜杠残留。
3. `src/Assets/DataConfig/Language.csv` 有重复 key：
   - `desc_enemy_huangyesanxiu` 重复。
   - `chapter_qiushui` 重复。
4. `开发管理/设计-当前状态.txt` 统计与状态需校准：
   - 养成体系规模仍写术法 73、太一道庭 24；应按当前文件与 CSV 口径校准为术法 75、太一道庭 26。
   - “Unity `.asset` 仍为 0”“Language.csv 同步”“术法设计规范确认清洁”等说法与复核结果不一致。
5. `开发管理/设计-下一步建议.txt` 与 `开发管理/开发-下一步建议.txt` 的未审核条目需保留未通过状态：
   - A8 基础设定文本清洗、A9 docs→CSV→Unity asset 对齐清单、A10 管理文档状态校准、开发 A6 导入完整性检查，均需在上述问题修复并复核后再标 ✅。
6. 后续复核建议：
   - 修复后重新运行 CSV 行数、docs 文件数、Language 重复 key、CSV→asset 覆盖率检查。
   - 若涉及 Unity asset 生成，应通过 Unity `天章/导入全部配置` 或等价导入流程验证。
