# 关中悬赏状态机与计数验证记录

## U-BOUNTY-01A · 2026-08-02

- 状态机：独立 `BountyStateStore`（无实例即 `Available`）与纯规则 `BountyRuntime` 实现 `Accept`、`RecordDefeat`、`Claim`、`GetState`；只接受生产范围、`defeat_enemy` 与 `one_time`，非法城市入口、敌人、地点、状态、进度或重复请求均返回稳定原因且不改状态。
- 目录引用：`Accept` 接取校验除悬赏自身字段外，还要求发布城市 `issuerSettlementId` 与目标敌人 `targetEnemyId` 都能从同一 `ContentCatalogData` 解析（`TryGetSettlement` / `TryGetEnemy`）；任一缺失返回 `bounty_settlement_missing` / `bounty_target_enemy_missing` 且实例保持 `Available`。
- 击败计数：`AdventureSceneController` 在既有正式胜利结算点以已消费的结构化 `adventureId + enemyId` 至多登记一次；败北、错误敌人与错误地点不计数；掉落授予仍走原入口，不重做战斗。
- 原子领奖：`Claim` 先复验奖励结构与物品引用，再经 `InventoryGrantService.TryBuildGrant` 构造完整新库存快照；全部合法后才与 `Claimed` 状态由 `GameSession` 一次性替换（`ApplyGrant` + `BountyStates.ReplaceAll`）。任一校验失败时库存与悬赏状态均完全不变。
- 库存拆分：`InventoryGrantService` 将候选构造（`TryBuildGrant`）与替换（`ApplyGrant`）分开，`Grant` 行为保持不变，既有授予测试全部通过。
- 边界：未修改 `SessionStateSnapshots.cs`、schema、读档恢复、Bounty CSV / Language / asset、数值、掉落概率、堆叠上限、UI、城市功能分发或存档所有者；`BeginNewGame` / `ClearSession` 只清空会话内悬赏实例。

## 定向返工（REV-U-BOUNTY-01A-01 · 2026-08-02）

- 按审核反馈只在 `BountyRuntime.ValidateBountyForAccept` 补齐发布城市与目标敌人的目录引用失败关闭及稳定原因，不扩展到生产数据、Adventure、存档、UI、数值或兼容层。
- `BountyRuntimeTests.CreateCatalog` 默认纳入 `guanzhong_city` 据点与 `enemy_shijiahou` 敌人（新增 `includeSettlement` / `includeEnemy` 开关）；多实例测试重建目录时保留城市与敌人条目。
- 新增直接负例：`AcceptRejectsMissingIssuerSettlementReferenceWithoutMutation`（缺失发布城市 → `bounty_settlement_missing`，状态仍 `Available`）、`AcceptRejectsMissingTargetEnemyReferenceWithoutMutation`（缺失目标敌人 → `bounty_target_enemy_missing`，状态仍 `Available`）。

## 已执行验证（Unity 6000.3.18f1 EditMode）

- 本卡直接测试 40/40 通过：`BountyRuntimeTests` 15/15（四状态合法转换、全部非法转换、错误城市／敌人／地点、缺失城市与缺失目标敌人目录引用拒绝、单场重复登记、负数与超目标拒绝、奖励原子成功／失败、多实例隔离、新档清空）；`InventoryGrantServiceTests` 7/7（含新增 `TryBuildGrant` 拆分测试）；`TacticalGridModelTests` 4/4 与 `AdventureSceneControllerTests` 14/14（含新增正式胜利只登记一次、败北不登记，均经生产目录接取后验证）。
- 完整 EditMode：306 项，304 通过、2 失败，均为与本卡路径无关的既有失败：
  - `DataConfigImporterContentScopeTests.LianShenReservedAbilitiesRemainExcludedAtPlayerLoad`：与 U-ENEMY-01、U-ITEM-GRANT-01 记录一致的既有失败（缺少预期攻击档案排除日志）。
  - `CharterConflictRulesTests.AuthorizedGrantBindsEveryConflictIdentityField`：World 册界域期望 `PULSE_ADVANTAGE` 实际 `PULSE_NEUTRAL`，该测试仅引用 `TianZhang.World`，不涉及本卡任何修改路径。
- 任务卡列出的 `dotnet build src/TianZhang.EditModeTests.csproj` 不可运行：该 csproj 不在仓库（`git ls-files` 无此文件，磁盘亦无）；Unity EditMode 运行已编译全部项目 asmdef 程序集，作为本卡 C# 构建与测试闸门。
- `tools/check-review-text.ps1 -Paths 开发管理,src/Assets/Scripts,src/Assets/Tests`：通过，检查 249 个文件。
- `tools/check-pending-whitespace.ps1`（仅本卡 expectedPaths）：通过，检查 3 个文件；`git diff --cached --check` 由最终提交脚本执行。

## 后续

- `U-BOUNTY-01B` 实施 schema 3 悬赏持久化与迁移；`U-BOUNTY-01C` 接入悬赏板界面与城市入口。本卡悬赏实例仅限会话内，存档保存与恢复不在本卡范围。

## Codex 独立复审（2026-08-02）

- 复审业务提交 `b433dc464f71e0b56740a9452336af3448899529` 通过：`BountyRuntime.ValidateBountyForAccept` 在创建实例前通过同一 `ContentCatalogData.TryGetSettlement` / `TryGetEnemy` 校验发布城市与目标敌人，缺失时分别返回稳定原因且状态保持 `Available`；修复未扩展到 Adventure、存档、UI、生产数据或数值。
- 独立 Unity 6000.3.18f1 EditMode 完整执行 306 项，304 通过、2 失败；`BountyRuntimeTests` 15/15、`InventoryGrantServiceTests` 7/7、`TacticalGridModelTests` 4/4、`AdventureSceneControllerTests` 14/14 通过。两项失败仍为已登记且与本卡路径无关的 `CharterConflictRulesTests.AuthorizedGrantBindsEveryConflictIdentityField` 与 `DataConfigImporterContentScopeTests.LianShenReservedAbilitiesRemainExcludedAtPlayerLoad`。
- Unity 生成忽略的工程投影后，`dotnet build src/TianZhang.EditModeTests.csproj` 通过（0 警告、0 错误）。本卡复审通过并关闭，存档 schema 3 与迁移仍由 `U-BOUNTY-01B` 独立实施。

## U-BOUNTY-01B · 2026-08-03

- schema 3 投影：`GameSessionSaveData` 新增 `bounties[]`（只含 `bountyId`、`status`、`progress`），`GameSessionSnapshot.Capture` 按 `bountyId` 排序保存会话内悬赏实例；`GameSessionRestoredState` 携带悬赏快照。
- 迁移语义：版本 0／1／2 保持既有世界／场景、任务、背包、NPC 与筑基紫府迁移不变，悬赏集合恢复为空；版本 3 对合法悬赏状态无损往返。
- 恢复校验（先于任何会话集合替换，任一失败整个会话不变）：快照链路校验版本、空／重复悬赏 ID、非法状态（含存档中出现 `Available` 无实例态）与负进度；`GameSession.RestoreSaveData(data, catalog)` 再校验悬赏引用可由既有 `ContentCatalogData.TryGetBounty` 解析、进度不超过 `requiredCount` 目标。内容目录按既有单一存档边界以参数传入，不建立第二存档、全局单例目录或静默兼容读取层。
- 原子替换：全部校验通过后才经 `ReplaceAll` 一次性替换任务、背包、NPC 与悬赏集合；`BountyRuntime` 未修改，领取与战斗计数行为未被改写。
- 终态稳定性：恢复后的 `Claimed` 保持一次性终态，`Claim`／`RecordDefeat`／`Accept` 均稳定拒绝且不重复结算；恢复后的 `ObjectiveCompleted` 可恰好领奖一次。
- 边界：未修改 Bounty CSV／Language／asset、奖励参数、掉落概率、堆叠上限、UI、`QuestStateStore` 语义、Adventure 结算或数值；`U-BOUNTY-01C` 的 UI 接入边界未触碰。

## U-BOUNTY-01B 已执行验证（Unity 6000.3.18f1 EditMode，2026-08-03）

- 命令：`pwsh -NoProfile -ExecutionPolicy Bypass -File tools/run-unity-editmode-tests.ps1`（worktree 内 `src` 项目；首次运行重建 Library，共执行两轮，第二轮结果如下）。完整 EditMode：310 项，308 通过、2 失败；失败仍为与本卡路径无关的既有基线：`CharterConflictRulesTests.AuthorizedGrantBindsEveryConflictIdentityField`（World 册界域期望 `PULSE_ADVANTAGE` 实际 `PULSE_NEUTRAL`）与 `DataConfigImporterContentScopeTests.LianShenReservedAbilitiesRemainExcludedAtPlayerLoad`（缺少预期攻击档案排除日志）。脚本因该 2 项既有失败以退出码 2 结束，属其零失败门禁的预期行为。
- 本卡直接用例：`SessionStateSnapshotTests` 10/10（新增 `BountyInstancesRoundTripInCurrentSchemaAndLegacyVersionsRestoreEmpty`、`RestoreRejectsUnresolvableDuplicateOrIllegalBountyStateAtomically`）；`BountyRuntimeTests` 17/17（新增 `RestoredClaimedTerminalStateStaysStableAndRejectsReclaimAndReRecord`、`RestoredObjectiveCompletedCanBeClaimedExactlyOnceWithoutResettlement`）；`AdventureSceneControllerTests` 14/14、`TacticalGridModelTests` 4/4 等既有用例未回归。
- 首轮新增 1 项失败并已修正：`RestoreRejectsUnresolvableDuplicateOrIllegalBountyStateAtomically` 的超目标进度用例未把含悬赏的目录传入断言辅助函数，得到“引用不可解析”异常；修正为所有篡改用例统一传入同一目录后第二轮全部通过，生产代码行为未被修改。
- `pwsh -NoProfile -ExecutionPolicy Bypass -File tools/check-review-text.ps1 -Paths 开发管理,src/Assets/Scripts,src/Assets/Tests`：通过，检查 252 个文件。
- `pwsh -NoProfile -ExecutionPolicy Bypass -File tools/check-pending-whitespace.ps1`（本卡 candidate 路径）：通过，检查 5 个文件；`git diff --cached --check` 由 `tools/automation-finalize-commit.ps1` 在提交前执行。

## 后续

- `U-BOUNTY-01C` 接入悬赏板界面与城市入口；本卡存档事实仍只由 `GameSession` 与其既有快照链路拥有。

## Codex 独立复审与完成（2026-08-03）

- 独立复审正式业务提交 `3c14ae73ca5b5d363f36f5af84953b3c25b66413` 通过：schema 3 只投影 `bountyId`、`status`、`progress`；版本 0／1／2 保持原迁移语义并恢复空悬赏集合；版本 3 在任何会话集合替换前完成版本、ID、状态、进度、重复项和 `ContentCatalogData` 引用校验，未建立第二存档、全局目录或兼容读取层。
- 独立 Unity 6000.3.18f1 EditMode 完整执行 310 项，308 通过、2 失败；`SessionStateSnapshotTests` 10/10、`BountyRuntimeTests` 17/17 通过。两项失败仍为已登记且与本卡路径无关的 `CharterConflictRulesTests.AuthorizedGrantBindsEveryConflictIdentityField` 与 `DataConfigImporterContentScopeTests.LianShenReservedAbilitiesRemainExcludedAtPlayerLoad`。
- Unity 生成忽略的工程投影后，`dotnet build src/TianZhang.EditModeTests.csproj` 通过（0 警告、0 错误）。本卡新增代码中的临时未审核标记已移除，任务归档；`U-BOUNTY-01C` 前置已完成，后续由独立队列维护建立任务卡，本轮未实现 UI 或正式端到端闭环。

## U-BOUNTY-01B1 · 2026-08-04

- 组合失败关闭：`GameSession.ValidateRestoredBounties` 在既有内容引用与超目标进度校验后，按已解析 `BountyData.requiredCount` 补齐状态／进度组合约束——`Accepted` 只允许 `progress < requiredCount`，`ObjectiveCompleted`／`Claimed` 只允许 `progress == requiredCount`。这些组合之外的存档（`Accepted == target`、`ObjectiveCompleted < target`、`Claimed < target`）不能由现有状态机合法产生：`RecordDefeat` 在进度达到目标时同次转换为 `ObjectiveCompleted`，`Claim` 只接受 `progress == requiredCount` 并保持进度。任一非法组合在替换任何会话集合前失败，整个会话不变。
- 既有规则保持不变：空 ID、未知状态、`Available`、负进度、重复 ID、未知内容引用与超目标进度仍分别按原位置与异常类型拒绝；`catalog` 仍为必填，快照层与会话层所有权、状态迁移、领奖与计数行为未修改。
- 用例：`SessionStateSnapshotTests` 新增 `RestoreRejectsBountyStateProgressCombinationsTheRuntimeCannotProduce`（目标 1 下 `Accepted==target`、`Claimed<target`、`ObjectiveCompleted<target` 各拒绝一次，每次拒绝后会话 JSON 与基线一致）与 `RestoreAcceptsAcceptedProgressBeforeTargetAndRejectsItAtTarget`（目标 2 的 `Accepted` 进度 1 无损往返正例、进度达到目标负例）；既有原子拒绝用例的种子由 `Accepted/1` 调整为合法组合 `Accepted/0`，其全部篡改用例与异常类型不变。
- 边界：未修改 schema、保存字段、状态枚举、`BountyRuntime` 状态迁移、领奖、战斗计数、原子替换顺序、Bounty CSV／Language／asset、UI 或数值；未读取或复制旧 `51efd154-...` worktree 实现。

## U-BOUNTY-01B1 已执行验证（Unity 6000.3.18f1 EditMode，2026-08-04）

- 命令：`pwsh -NoProfile -ExecutionPolicy Bypass -File tools/run-unity-editmode-tests.ps1`（worktree 内 `src` 项目，首轮重建 Library）。Unity 进程退出码 2（其零失败门禁行为），完整 EditMode：333 项，331 通过、2 失败；失败仍为与本卡路径无关的既有基线：`CharterConflictRulesTests.AuthorizedGrantBindsEveryConflictIdentityField`（World 册界域期望 `PULSE_ADVANTAGE` 实际 `PULSE_NEUTRAL`）与 `DataConfigImporterContentScopeTests.LianShenReservedAbilitiesRemainExcludedAtPlayerLoad`（缺少预期攻击档案排除日志）。
- 本卡直接用例：`SessionStateSnapshotTests` 16/16（新增 `RestoreRejectsBountyStateProgressCombinationsTheRuntimeCannotProduce`、`RestoreAcceptsAcceptedProgressBeforeTargetAndRejectsItAtTarget` 均通过；既有 `BountyInstancesRoundTripInCurrentSchemaAndLegacyVersionsRestoreEmpty`、`RestoreRejectsUnresolvableDuplicateOrIllegalBountyStateAtomically` 未回归）；`BountyRuntimeTests` 17/17、`AdventureSceneControllerTests` 14/14、`TacticalGridModelTests` 4/4 等既有用例未回归。
- `pwsh -NoProfile -ExecutionPolicy Bypass -File tools/check-review-text.ps1 -Paths 开发管理,src/Assets/Scripts,src/Assets/Tests`：通过，检查 258 个文件。
- `pwsh -NoProfile -ExecutionPolicy Bypass -File tools/check-pending-whitespace.ps1 -ExpectedPaths 'src/Assets/Scripts/Game/GameSession.cs|src/Assets/Tests/EditMode/SessionStateSnapshotTests.cs|开发管理/关中悬赏状态机与计数验证记录.txt'`：通过，检查 3 个文件；`git diff --cached --check` 由 `tools/automation-finalize-commit.ps1` 在提交前执行。

## U-BOUNTY-01B1 · Codex 独立复审与完成（2026-08-04）

- 审核正式业务提交 `3cef310c812b7c2ec67497433d854f880e8fa1bb` 通过：`GameSession.ValidateRestoredBounties` 在 `GameSessionSnapshot.Restore` 已完成 DTO、ID、状态、负进度与重复项校验之后、任何会话集合替换之前，继续以同一必填 `ContentCatalogData` 解析每个 `BountyData`。它保留超目标进度拒绝，并补齐 `Accepted < requiredCount`、`ObjectiveCompleted == requiredCount`、`Claimed == requiredCount` 的组合约束；`BountyRuntime.RecordDefeat` 达标时同次改为 `ObjectiveCompleted`，`Claim` 只接受等于目标的完成态，未改状态迁移、领奖或计数。
- 独立验证：`pwsh -NoProfile -ExecutionPolicy Bypass -File tools/run-unity-editmode-tests.ps1` 执行 335 项，333 通过、2 项既有无关基线失败：`CharterConflictRulesTests.AuthorizedGrantBindsEveryConflictIdentityField` 与 `DataConfigImporterContentScopeTests.LianShenReservedAbilitiesRemainExcludedAtPlayerLoad`；直接 `SessionStateSnapshotTests` 16/16 与 `BountyRuntimeTests` 17/17 均通过。`dotnet build src/TianZhang.EditModeTests.csproj` 0 错误通过，保留 2 项无关 Charter 静态目录字段警告。
- 复审结论：通过。任务归档并移除队列／backlog 投影；未触及 schema、保存字段、状态枚举、状态迁移、领取、战斗计数、内容目录、UI、CSV、数值或经济。

## 后续

- `U-BOUNTY-01C` 仍作为独立阻塞任务接入悬赏板界面与城市入口；其是否恢复 ready 只由该任务卡决定，存档事实继续只由 `GameSession` 与其既有快照链路拥有。

## U-BOUNTY-01C · 2026-08-04

- 面板接入：新增独立 `BountyBoardView`（Settlement 程序集内、场景序列化引用），由 `SettlementSceneView` 持有；`SettlementSceneController` 只在 `bounty_board` 成功分发且返回已接入原因后经视图打开面板。面板只枚举当前正式据点由 `ContentCatalogData.GetBountiesByIssuer` 返回的悬赏，逐行显示稳定 ID、实际状态与进度（`id | 状态 | 进度/目标`），接取／领奖只把 `bountyId` 交给当前 `GameSession.AcceptBounty`／`ClaimBounty`，每次调用后从同一 `GameSession.GetBountyState` 刷新显示；不判断悬赏规则、不拥有实例状态，不伪造目标、奖励、成功日志、状态转换或默认条目。
- 分发器区分：`SettlementFeatureDispatcher` 的已接入原因由 pending-panel 占位原因 `bounty_board_entry_ready_pending_panel` 改为 `bounty_board_entry_opened`；未知、禁用、未注册或处理异常的 feature 仍返回稳定原因并失败关闭，不打开面板。
- 场景重建：`SceneBuilder.BuildSettlementScene` 创建 `BountyBoardPanel`（初始非激活）与标题、条目、结果文本及接取／领奖／关闭按钮，序列化引用绑定到 `BountyBoardView`，`SettlementSceneView` 再持有面板引用；重新生成场景后面板、视图、控制器与分发器引用完整，正式场景 meta GUID 与冒险入口未改变。
- 用例：新增 `BountyBoardViewTests` 6 项直接用例——关中唯一生产悬赏经功能按钮打开并接取、完成态可领奖并授予 `item_lingshi_low`、`Claimed` 不可重复领奖、非法／非生产／非当前据点请求显示实际稳定原因且不改状态、未知／禁用功能不打开面板、处理器异常失败关闭；`SceneArchitectureEditorTests` 的分发器用例改断言已接入原因，场景构建用例扩展证明面板、视图和序列化引用存在。
- 边界：未修改 `GameSession`、`BountyRuntime`、`BountyStateStore`、schema 3、内容目录、CSV、Language、asset、奖励参数、掉落概率、堆叠上限、战斗计数、Adventure 结算或世界／副本返回所有者；未增加第二 Canvas、第二据点、随机悬赏池、每日刷新或新的 BountyData。

## U-BOUNTY-01C 已执行验证（Unity 6000.3.18f1 EditMode，2026-08-04）

- 命令：`pwsh -NoProfile -ExecutionPolicy Bypass -File tools/run-unity-editmode-tests.ps1`（worktree 内 `src` 项目；首轮重建 Library，终轮结果如下）。完整 EditMode：341 项，339 通过、2 失败；失败仍为与本卡路径无关的既有基线：`CharterConflictRulesTests.AuthorizedGrantBindsEveryConflictIdentityField`（World 册界域期望 `PULSE_ADVANTAGE` 实际 `PULSE_NEUTRAL`）与 `DataConfigImporterContentScopeTests.LianShenReservedAbilitiesRemainExcludedAtPlayerLoad`（缺少预期攻击档案排除日志）。脚本因该 2 项既有失败以非零退出，属其零失败门禁的预期行为。
- 本卡直接用例：`BountyBoardViewTests` 6/6、`SceneArchitectureEditorTests` 22/22（含分发器已接入原因与场景序列化引用扩展用例）、`BountyRuntimeTests` 17/17、`SessionStateSnapshotTests` 16/16、`AdventureSceneControllerTests` 14/14、`TacticalGridModelTests` 4/4 全部通过。实现迭代中修正两处测试问题并如实重跑：按钮监听改为 `Show` 时绑定（场景保存重开后不丢失），领奖用例先重新打开面板刷新完成态显示；终轮无本卡失败。
- `dotnet build src/TianZhang.EditModeTests.csproj`：0 错误通过，保留 2 项无关 Charter 静态目录字段警告（与 U-BOUNTY-01B1 记录一致）。
- 场景重建：以同一 Unity 6000.3.18f1 批处理执行 `-executeMethod TianZhang.Editor.SceneBuilder.BuildSettlementScene` 生成 `SettlementScene.unity`，面板初始非激活，视图与面板序列化引用完整，场景 meta GUID 不变。
- `pwsh -NoProfile -ExecutionPolicy Bypass -File tools/check-review-text.ps1 -Paths 开发管理,src/Assets/Scenes,src/Assets/Scripts,src/Assets/Tests`：通过，检查 258 个文件。
- `pwsh -NoProfile -ExecutionPolicy Bypass -File tools/check-pending-whitespace.ps1`（本卡 candidate 路径）：通过，检查 11 个文件；`git diff --cached --check` 由 `tools/automation-finalize-commit.ps1` 在提交前执行。

## 后续

- `U-GZ-FORMAL-E2E-01` 验证接取、冒险、击败、掉落、返回、领奖和保存／读取的正式闭环；本卡悬赏事实仍只由 `GameSession` 与其既有快照链路拥有。
