# EXP-<DOMAIN>-<NNN> · <单一风险短标题>

## 状态
状态（candidate／active／review_required／retired）、最近核验日期、适用版本或事实源版本。

## 开工前
本段正文控制在 120～200 个 Unicode 字符（不含标题），只写开工前最需要知道的内容。
- 适用条件：
- 排除条件：
- 修改前必须定位的路径、符号或实体：
- 禁止采用的做法：
- 必须执行的检查：
- 停止条件：

## 正确处理
最短且已验证的处理顺序。

## 根因与弯路
已发生的问题、已证实根因和失败做法。

## 证据
来源任务、提交、测试、运行记录或审核证据。

## 失效条件
哪些架构、文件、接口、版本或规则变化后必须重新核验。
