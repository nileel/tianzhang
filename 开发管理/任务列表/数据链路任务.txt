# 数据链路任务列表（✅ 已审核）

> 本文件只保留未完成、阻塞或冻结的 CSV、Unity asset、导入器和检查器任务。当前可执行切片以 `开发管理/当前任务队列.txt` 为准；已完成记录可由 Git 与 `开发管理/任务归档/` 追溯。

## 当前风险入口

- TQ-057 与 TQ-059 已完成并归档；当前 G3 严格检查为功法 29/29、术法 83/83、神通 31/31。
- 三份历史扫描证据已归档：`开发管理/任务归档/CSV高阶内容定位表.txt`、`开发管理/任务归档/realm_lianshen专项检查.txt`、`开发管理/任务归档/炼虚与高阶内容影响面清单.txt`。它们只提供调查线索，不是当前事实源。
- `check-data-chain.ps1` 的 `Missing 0` 只表示机械覆盖，不代表 G3 或数据链可信；当前对齐快照见 `开发管理/docs-csv-asset-alignment.txt`。
- G3 已由 TQ-071 独立复核；除批准的关中薄内容切片外，仍禁止按数据规模继续扩写。`sourceAffiliation` 只表示内容来源，不是角色阵营限制。

## 任务

| ID | 优先级 | 主责 | 状态投影 | 阻塞于 | 摘要 | 任务卡 |
|----|--------|------|----------|--------|------|--------|
| D-REALM-02 | P2 | Codex | 阻塞（D-REALM-DEC-02～04） | — | 高阶内容最终处理父任务；D-REALM-DEC-01 已选择资料片隔离，其他影响根仍待决定。 | — |
| D-FPD-MIGRATE-01 | P1 | Codex | 阻塞（D-FPD-SCHEMA-01、C-FPD-GONGFA-01、C-FPD-SPELL-01、C-FPD-SHENTONG-01、C-FPD-EXPAND-01、N-FPD-GROWTH-01、N-FPD-MANSION-01、N-FPD-CULTIVATE-01、N-FPD-DANXIANG-01） | — | 将确认后的内容迁入 CSV/asset，并删除旧五段／道基品级字段的现行语义。 | — |
| D-JD-SCHEMA-01 | P1 | Codex | 阻塞 | D-JD-SCHEMA-01A、D-JD-SCHEMA-01B、D-JD-SAMPLE-01 | 金丹数据父任务；静态契约、导入失败关闭与样例均完成后才关闭。 | — |
| D-JD-SCHEMA-01A | P1 | Codex | 待建卡 | — | 金丹道路、效果、三实位、丹枢／丹相、主辅承载与账本静态契约。 | — |
| D-JD-SCHEMA-01B | P1 | Codex | 阻塞 | D-JD-SCHEMA-01A | 金丹数据对象、导入器、检查器、正反 fixture 与失败关闭。 | — |
| D-JD-SAMPLE-01 | P1 | Codex | 阻塞 | D-JD-SCHEMA-01B | 一府一位、三府三位、五府三位的最小有效样例与非法样例。 | — |
| D-TZ-CHARTER-SCHEMA-01 | P1 | Codex | 冻结（M7、M8 实现授权） | — | 建立地区规则条目、遗物权限、册界节点、授权、覆盖范围与当前状态的独立数据契约；不得把长期世界状态塞入 `EnvironmentProfileData`。 | — |
| D-TZ-CHARTER-SAMPLE-01 | P1 | Codex | 冻结（D-TZ-CHARTER-SCHEMA-01、M7、M8 实现授权） | — | 迁入一个旧门禁、一个公共设施、一个册界节点和一条五行地纪的有效纵向样例及非法 fixture。 | — |
| D-COMBAT-02 | P3 | Codex | 阻塞（U-COMBAT-01） | — | 建立 BattleSim/Unity 共用攻击档案 fixture 与差异检查；攻击档案数据契约已归档。 | — |

## G3 任务执行边界

- TQ-059 已固定：`realmReq`、`elementReq` 为 `runtime_gate`；术法／神通原 `affiliation` 已改名为 metadata-only 的 `sourceAffiliation`。
- D-COMBAT-01/02 必须以 CSV/asset 为唯一权威来源，坏数据非 0 退出，BattleSim 与 Unity 同一档案的类型、元素、距离和目标语义一致。

## 筑基、紫府与丹相后续数据任务卡

### D-JD-SCHEMA-01 · 金丹数据父任务

- 子任务：`D-JD-SCHEMA-01A` 锁定静态契约；`D-JD-SCHEMA-01B` 实现导入与失败关闭；`D-JD-SAMPLE-01` 建立可消费样例。
- 状态：父任务不直接进入队列；三个子任务均完成并归档后才关闭。
- 禁止：不得把 51 项正式内容档案、生产平衡或 Unity 玩法实现并入本父任务。

### D-JD-SCHEMA-01A · 金丹静态数据契约

- 依赖：`D-FPD-SCHEMA-01` 已完成；进入队列前仍须建立并核验 `D-JD-SCHEMA-01A` 的独立任务卡。
- 必读：`docs/superpowers/specs/2026-07-19-jindan-position-proof-and-contestation-design.md`、`docs/superpowers/specs/2026-07-20-foundation-purple-mansion-danxiang-integration-design.md`、`docs/基础设定/金丹基础效果装配与冲突规则.txt` 与现有金丹数据字段。
- 预期路径：新建 `docs/superpowers/specs/2026-07-25-jindan-static-data-contract.md`；建卡时展开直接相关数据模型和 fixture 路径。
- 交付：定义道路、基础效果、三实位、唯一丹枢、唯一丹相、全部紫府输入、主辅承载、`abilityInstanceId`、资源／冷却账本、冲突引用及表现／本地化稳定键。
- 禁止：不等待正式显示名写入数据；未确认名称只通过稳定键引用。不开发表、导入器、BattleSim 行为或生产数值。
- 验证：逐项对齐位格、装配、唯一实例和主辅规则；契约能表达一府一位、三府三位和五府三位而不生成第四实位或第二丹相。
- 完成条件：`D-JD-SCHEMA-01B` 可仅按本契约实现数据导入与失败关闭，不需要创作规则。
- 停止条件：规则事实冲突，或契约必须引入第二丹枢／丹相、超过三实位、共享可变账本或旧九品兼容语义。

### D-JD-SCHEMA-01B · 金丹导入与失败关闭

- 依赖：`D-JD-SCHEMA-01A` 完成并归档。
- 必读：`D-JD-SCHEMA-01A` 静态契约；现有数据模型、导入器、检查器和直接测试。
- 预期路径：相关 CSV／asset、数据对象、`DataConfigImporter`、`tools/check-data-chain.ps1` 与直接相关 EditMode 测试；建卡时实时展开字面量路径。
- 交付：实现数据对象、导入器、检查器、正反 fixture 和稳定错误原因。
- 失败关闭：未知道路／效果／位格、同一实例重复结算、第二丹枢或第二丹相、超过三实位、缺少账本所有者、非法主辅引用与旧九品静默兼容。
- 禁止：不生产 51 项正式内容档案，不填平衡值，不自动修复或截断非法输入。
- 验证：相关 .NET／EditMode、`check-data-chain.ps1`、正反 fixture、文本检查和差异检查。
- 完成条件：合法数据完整导入，所有非法 fixture 以稳定原因整表失败，随后解锁 `D-JD-SAMPLE-01`。
- 停止条件：实现需要改变静态契约、补默认值、保留旧 schema fallback 或建立第二导入链。

### D-JD-SAMPLE-01 · 金丹最小有效与非法样例

- 依赖：`D-JD-SCHEMA-01B` 完成并归档。
- 必读：`D-JD-SCHEMA-01A` 契约、`D-JD-SCHEMA-01B` 导入边界，以及金丹装配和全部紫府输入事实。
- 预期路径：`D-JD-SCHEMA-01B` 建立的 fixture 目录、直接数据测试与本任务验证记录；建卡时展开实际路径。
- 交付：至少一组覆盖一府一位、三府三位、五府三位的有效 fixture，以及重复实例、越权位格、第二丹相、非法主辅关系和缺失账本所有者的失败 fixture。
- 禁止：样例不代表 51 项正式玩法档案、完整内容迁移、生产平衡或 UI；不得用虚构默认值使非法输入通过。
- 验证：三个有效组合完整导入并保持唯一实例账本；每个非法样例以 `D-JD-SCHEMA-01B` 定义的稳定原因失败。
- 完成条件：后续 `N-JD-RULE-01A` 可只消费该样例接入 BattleSim，不硬编码缺失内容；同时关闭父任务 `D-JD-SCHEMA-01`。
- 停止条件：现有契约无法表达三个有效组合，或必须先生产完整内容／数值才能形成最小 fixture。

### D-FPD-MIGRATE-01 · 内容数据与角色状态迁移

- 依赖：D-FPD-SCHEMA-01；C-FPD-NAME-01、C-FPD-MANSION-01、C-FPD-CULTIVATE-01、C-FPD-EXPAND-01、C-FPD-GONGFA-01、C-FPD-SPELL-01、C-FPD-SHENTONG-01 已确认；N-FPD-GROWTH-01、N-FPD-MANSION-01、N-FPD-CULTIVATE-01、N-FPD-DANXIANG-01 已锁定。
- 预期路径：迁移计划 M7 的全部功法／术法／神通／道具文件、`src/Assets/DataConfig/` 对应 CSV、`src/Assets/Data/` 对应 asset、导入器、检查器和直接测试；进入当前队列前必须扫描并列出每个字面量路径。
- 范围：迁移四阶段成长、筑基方案、容量效果、紫府状态、升格方案、镇府神通、主／辅承载和唯一丹相引用；太一道庭字段映射为同一契约。
- 禁止：保留旧字段作为静默兼容入口、用默认值掩盖缺失内容、在导入阶段创作机制或数值、自动截断非法装备／容量数据。
- 验证：严格数据链检查、相关 Unity EditMode、CSV/asset 数量与 ID 对齐、旧字段全局扫描、`git diff --check`；数值总回归由 N-FPD-REGRESSION-01 执行。
- 完成条件：docs、CSV、asset、BattleSim fixture 与 Unity 数据对象对同一角色给出一致的四阶段、容量、紫府、镇府神通和丹相输入结果。
- 停止条件：任一内容实例或数值仍未确认，或迁移需要猜测默认值、截断非法数据或保留双 schema。

## 《天章》册界体系后续数据任务卡

共同事实源：`docs/superpowers/plans/2026-07-22-tianzhang-cosmology-story-and-rules-migration.md`、`docs/基础设定/册界天章与地区规则交互规则.txt`、三项核心遗产文件及 M7 完成品。两项任务均属于 M8，实现授权前不得进入当前任务队列。

### D-TZ-CHARTER-SCHEMA-01 · 册界体系数据契约

- 依赖与状态：M7 父任务完成并通过复审、用户明确授权 M8；当前冻结。C-ENV-PROFILE-01 与 D-ENV-SCHEMA-01 只提供环境档案语义，不代替本任务的地区长期状态契约。
- 预期路径：新建 `docs/superpowers/specs/2026-07-22-tianzhang-charter-data-contract.md`；修改 `src/Assets/DataConfig/`、对应 ScriptableObject 数据模型、`DataConfigImporter`、`tools/check-data-chain.ps1` 与直接相关 EditMode 测试。进入当前队列前必须实时扫描并展开全部字面量路径和新表名。
- 数据边界：分别定义静态条目、遗物权限要求、册界节点、组织授权与传播边界档案，以及玩家已登记条目、界印状态、节点状态、当前覆盖集合、条目／节点占用和当前地区规则等动态状态；定义层与状态层不得混表。
- 必需字段：完整承载 M4 的十八个规则字段，并为门禁目标识别、公共设施受益者／管理者、节点连通、版本化授权、现实供给、正负原子提交、金丹挑战资格和元婴受锚提供稳定 ID 引用。战场 `environmentProfile` 只能作为输出引用，不能反向成为地区状态所有者。
- 失败关闭：未知条目／节点／授权／现象／变量／环境档案引用，覆盖集合越过传播边界，缺少正或负提交，定义与状态 ID 混用，以及旧字段静默兼容均拒绝整表导入。
- 验证：使用正反 fixture 证明整表失败关闭；运行直接相关 .NET／EditMode、`check-data-chain.ps1`、精确文本检查、待提交空白检查和 `git diff --check`。不涉及数值时不运行 BattleSim。
- 完成条件：Unity 数据层能无歧义读取条目、遗物、节点、授权、覆盖与当前状态，并且长期地区状态与 `EnvironmentProfileData` 保持单向引用边界；不得把 schema 完成写成规则已可游玩。
- 停止条件：契约需要完整九域天气模拟、动态经济产出、任意规则编辑器、第二套环境档案，或必须先猜测倍率、概率和价格。

### D-TZ-CHARTER-SAMPLE-01 · 册界纵向样例数据

- 依赖与状态：D-TZ-CHARTER-SCHEMA-01 完成；M7 十条内容与 C-ENV-PROFILE-01 已复审；用户明确授权 M8。当前冻结。
- 预期路径：D-TZ-CHARTER-SCHEMA-01 建立的 CSV、asset、导入器 fixture 与直接相关 EditMode 测试；进入当前队列前从现行门派、地图和遗物事实源选定一个已有门禁、一个已有公共设施和一个已有册界节点，并展开所有字面量路径。
- 有效样例：门禁只验证《开阖九章》的目标识别与失败原因；公共设施只验证太玄界印的管理者、受益者及守卫／档案响应；节点只验证授权、连通和据点级覆盖；规则固定选用已有结构走查的 `水府地纪`，只输出一个现有 `environmentProfile` 引用。
- 非法 fixture：至少覆盖无通行许可、只有通行而无管理权、无授权、节点断开、现实供给不足、覆盖越界、正负提交不完整、金丹挑战资格缺失和元婴受锚仍尝试覆盖。
- 边界：只引用现行地点、设施和条目，不创作新任务、NPC、九域分布或数值；样例数据不是整域生产配置，也不建立每日经济模拟。
- 验证：有效样例完整导入，逐个非法 fixture 以稳定错误码失败；运行 `check-data-chain.ps1`、相关 EditMode／构建、精确文本与空白检查、`git diff --check`。只有引入实际倍率、概率或资源产量时才增加数值验证。
- 完成条件：后续运行时任务可只消费这一组数据验证通行、归属、节点、条目和环境档案输出，不需要硬编码内容或补默认值。
- 停止条件：现行事实源没有可用门禁／设施／节点，`水府地纪` 没有合法环境档案，或样例必须扩展为完整地区内容；记录缺口并回到对应内容任务。

## TQ-057 最终批准口径

以下五项决定是 TQ-057 的最终实施事实。执行时不得重新询问、缩减或扩大决定边界，也不得迁移旧 ledger 的 provider 哈希或消息证据。

### DEC-20260715-35ACB87E6C10 · 保留并补齐 11 份古修术法

- 决定正文：选择 B：按既有文档补齐 CSV、语言键和 Unity asset
- 影响根：`src/Assets/DataConfig`、`src/Assets/Data/Spells`、`docs/角色养成/术法`。
- 迁移事实：补齐 `Spells.csv`、`Language.csv` 与全部 11 份古修术法 Unity asset。
- 兼容事实：保留 11 份古修术法文档，不删除已批准内容。
- Required checks：`data-chain`、`unity-editmode-related`、`pending-whitespace`、`cached-diff-check`。

### DEC-20260715-75D7BA2AF210 · 迁移双伤害倍率

- 决定正文：选择 A：为所有攻击术法增加明确的 physicalDamageMultiplier 与 soulDamageMultiplier，并迁移现有单通道数据
- 影响根：`src/Assets/DataConfig/Spells.csv`、`src/Assets/Scripts/Editor/DataConfigImporter.cs`、`src/Assets/Scripts/Combat/SpellData.cs`、`src/Assets/Scripts/Combat/CombatResolver.cs`、`src/Assets/Tests/EditMode`、全部现存 `src/Assets/Data/Spells/*.asset`，以及任何硬编码术法 schema 的 `tools/check-data-chain.ps1`。
- 迁移事实：迁移现有单通道术法数据到 `physicalDamageMultiplier` 与 `soulDamageMultiplier`；实施清单必须覆盖发现到的全部 `src/Assets/Data/Spells/*.asset`。
- 兼容事实：现有攻击术法迁移后保持原单通道伤害语义。
- Required checks：`data-chain`、`unity-editmode-related`、`pending-whitespace`、`cached-diff-check`。

### DEC-20260714-29A5D1356CC8 · 补齐炼神语言键

- 决定正文：选择 A：为 realm_lianshen 补齐语言键并保留有效引用。
- 影响根：`src/Assets/DataConfig/Language.csv`。
- 迁移事实：补齐 `realm_lianshen` 语言键。
- 兼容事实：保留现有有效 `realm_lianshen` 引用。
- Required checks：`data-chain`、`pending-whitespace`、`cached-diff-check`。

### DEC-20260714-320075D033A5 · 六部功法只删废止段

- 决定正文：选择 A：六部仍含有效低阶成长段的功法只删除废止的 realm_lianxu 段并同步 asset，保留其他内容。
- 影响根：`src/Assets/DataConfig/GongFa.csv`、`docs/角色养成/功法`、`src/Assets/Data/GongFa`。
- 迁移事实：六部功法只删除 `realm_lianxu` 段并同步对应 asset。
- 兼容事实：保留六部功法的其他有效低阶成长段。
- Required checks：`data-chain`、`unity-editmode-related`、`pending-whitespace`、`cached-diff-check`。

### DEC-20260713-A07FA708DB22 · 删除无效境界引用

- 决定正文：选择 A：删除仍引用已删除或未注册境界的 CSV 行、对应文档和 Unity asset。
- 影响根：`src/Assets/DataConfig`、`docs/角色养成`、`src/Assets/Data`。
- 迁移事实：删除仍引用已删除或未注册境界的 CSV 行、文档和 Unity asset。
- 兼容事实：不删除仍引用有效境界的数据。
- Required checks：`data-chain`、`unity-editmode-related`、`pending-whitespace`、`cached-diff-check`。

## 默认验证

- `pwsh -NoProfile -ExecutionPolicy Bypass -File tools/check-data-chain.ps1`
- `pwsh -NoProfile -ExecutionPolicy Bypass -File tools/check-data-chain.ps1 -FailOnMissingAssets`
- `dotnet build src/TianZhang.EditModeTests.csproj`
- `pwsh -NoProfile -ExecutionPolicy Bypass -File tools/check-review-text.ps1 -Paths 开发管理,tools,src/Assets/DataConfig,src/Assets/Scripts`
- `git diff --check`

## 边界

- 不因 `contentScope` 自动删除 CSV、asset 或 docs 内容。
- 不把 `realm_lianshen` 扩为玩家境界；缺失 Language key 必须补齐、隔离或停用，不能保持激活且只告警。
- 不得扩展设计语义或把历史扫描报告当作当前闭环证据。
