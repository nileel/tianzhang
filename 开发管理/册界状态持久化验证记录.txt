# 册界状态持久化验证记录

任务：`U-TZ-CHARTER-SAVE-01`（返工 candidate，待 Codex 独立复核）
修改方：DeepSeek V4 Flash

## 返工（未通过清单 DEC-20260804-REV6C24F5E231D0）

- Codex 复审不通过项：`CharterRuntimeStateData.TryValidate` 未拒绝重复的节点状态、组织授权
  版本或当前覆盖稳定 ID；`CharterRuleCatalogValidator.TryValidateCatalog` 也未独立拒绝重复的
  授权、边界、提交或冲突目录记录。
- 已按返工边界补齐（未新增目录、默认值、兼容读取层，未改变规则结算）：
  - `TryValidateNodeStates` 维护已见 nodeId 集合，重复稳定 ID 拒绝为
    `DuplicateNode`（`charter_state_duplicate_node`）。
  - `TryValidateAuthorizationStates` 维护已见 authorizationVersionId 集合，重复稳定 ID 拒绝为
    `DuplicateAuthorization`（`charter_state_duplicate_authorization`）。
  - `TryValidateCoverage` 维护已见覆盖 ID 集合，重复稳定 ID 拒绝为
    `DuplicateCoverage`（`charter_state_duplicate_coverage`）。
  - `TryValidateCatalog` 在授权、边界、提交、冲突四个记录数组各自维护已见键集合，重复记录
    拒绝为 `DuplicateCatalogId`（提交记录保持 `AtomicCommitIncomplete` 既有族）。
- 新增 EditMode 反例：`StaticCatalogValidatorRejectsDuplicateAuthorityBoundaryCommitAndConflictRecords`、
  `DynamicStateRejectsDuplicateNodeAuthorizationAndCoverageStableIds`；
  `CharterSaveRestoreRejectsInvalidPayloadsAtomically` 增加重复节点、重复组织授权版本、
  重复当前覆盖的读档失败关闭用例。

## 覆盖结论

- 已建立唯一玩家运行时静态目录 `Assets/Data/CharterRuleStaticCatalog/CharterRuleStaticCatalog.asset`：
  `CharterRuleStaticCatalogData` 只直接引用已导入 `CharterRuleDefinitionData` 并保存一份批准的
  `CharterRuleReferenceCatalog`；`ContentCatalogData` 只保存该 asset 的单一引用并暴露失败关闭的
  `TryGetCharterRuleStaticCatalog`。导入器 `ImportCharterRuleDefinitions` 只读取该 asset 的批准
  目录验证 CSV，已删除 `CreateProductionCharterRuleReferenceCatalog` 硬编码目录；十八字段与全部
  外部引用由 `CharterRuleCatalogValidator` 这一共享校验解析，导入器与玩家运行时调用同一实现。
- `GameSession` 只持有一个 `CharterRuntimeStateData` 动态状态；schema 升至 4，
  `GameSessionSaveData`／`GameSessionRestoredState` 成对记录 `hasCharterRuntimeState`、
  `charterDefinitionCatalogVersion` 与深复制状态。schema 0～3 只恢复明确未接入状态；schema 4 的
  错误 presence 组合、零／未知定义目录版本一律失败关闭。
- 恢复前完成唯一静态目录、定义目录版本、定义／状态分层与完整动态状态校验（条目、节点、授权版本、
  覆盖、占用、供给、正负提交、当前地区规则及条目与定义的边界／授权／提交配对关系），全部通过后才
  与既有会话集合一次原子替换；读档不调用规则事务，不重放供给、占用、提交、冲突或事件。

## 已执行验证（返工轮）

- `tools/check-data-chain.ps1`：通过（docs/csv counts、language keys、check-data-chain: OK）。
- `tools/check-review-text.ps1 -Paths 开发管理,docs,src/Assets/Scripts,src/Assets/Tests`：通过（659 files）。
- `tools/check-pending-whitespace.ps1 -ExpectedPaths <candidate 全路径>`：通过（6 files checked）。
- `git diff --cached --check`：通过（正式 candidate 提交时）。
- `tools/run-unity-editmode-tests.ps1`：335 执行 / 333 通过 / 2 失败（退出码 2 为脚本零失败
  门禁的预期行为）。返工新增反例 `StaticCatalogValidatorRejectsDuplicateAuthorityBoundaryCommitAndConflictRecords`、
  `DynamicStateRejectsDuplicateNodeAuthorizationAndCoverageStableIds` 全部通过；扩展后的
  `CharterSaveRestoreRejectsInvalidPayloadsAtomically`（重复节点、重复组织授权版本、重复当前
  覆盖）通过；schema 4 往返、schema 0～3 未接入、原子拒绝、不重复结算等既有直接用例全数保持。
- 2 项失败均为已登记且与本卡路径无交集的既有基线（多份验证记录与 `AI合作沟通.txt` 已登记）：
  `CharterConflictRulesTests.AuthorizedGrantBindsEveryConflictIdentityField`（shared 对等储备
  返回 `PULSE_NEUTRAL`，断言 `PULSE_ADVANTAGE`，文件来自 `D-TZ-CHARTER-CONFLICT-02`，不在本卡
  路径）与 `DataConfigImporterContentScopeTests.LianShenReservedAbilitiesRemainExcludedAtPlayerLoad`
  （旧日志断言与现行加载器不一致）。
- `tools/check-task-cards.ps1 -TaskId U-TZ-CHARTER-SAVE-01 -Postcondition ExternalPendingReview`：
  当前 candidate 阶段按预期不满足——卡片仍为 `external_execute/deepseek/ready`，该后置条件要求
  卡片已转换为 `codex_review/codex/ready`；转换由共享 Windows 入口在正式提交中与 pending review
  投影原子完成，须由共享入口在转换后复跑通过。

## 已知验证边界

- 全量 EditMode 的 2 项既有基线失败（见上方）不属本卡路径，未修复、未掩盖；共享冲突契约
  所有者（Codex）需按既有登记裁决 `PULSE_ADVANTAGE` 断言与对等储备实现的不一致。
- 共享入口正式提交后需复跑 `check-task-cards.ps1 -TaskId U-TZ-CHARTER-SAVE-01 -Postcondition
  ExternalPendingReview` 与 `check-review-text.ps1`，再交由 Codex 独立复核返工结果。
