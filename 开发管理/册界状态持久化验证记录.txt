# 册界状态持久化验证记录

任务：`U-TZ-CHARTER-SAVE-01`（candidate，待 Codex 独立复审）
修改方：DeepSeek V4 Flash

## 覆盖结论

- 已建立唯一玩家运行时静态目录 `Assets/Data/CharterRuleStaticCatalog/CharterRuleStaticCatalog.asset`：
  `CharterRuleStaticCatalogData` 只直接引用已导入 `CharterRuleDefinitionData` 并保存一份批准的
  `CharterRuleReferenceCatalog`；`ContentCatalogData` 只保存该 asset 的单一引用并暴露失败关闭的
  `TryGetCharterRuleStaticCatalog`。导入器 `ImportCharterRuleDefinitions` 只读取该 asset 的批准
  目录验证 CSV，已删除 `CreateProductionCharterRuleReferenceCatalog` 硬编码目录；十八字段与全部
  外部引用由 `CharterRuleCatalogValidator` 这一共享校验解析，导入器与玩家运行时调用同一实现。
- `GameSession` 只持有一个 `CharterRuntimeStateData` 动态状态；schema 升至 4，
  `GameSessionSaveData`／`GameSessionRestoredState` 成对记录 `hasCharterRuntimeState`、
  `charterDefinitionCatalogVersion` 与深复制状态。schema 0～3 只恢复明确未接入状态；schema 4 的
  错误 presence 组合、零／未知定义目录版本一律失败关闭。
- 恢复前完成唯一静态目录、定义目录版本、定义／状态分层与完整动态状态校验（条目、节点、授权版本、
  覆盖、占用、供给、正负提交、当前地区规则及条目与定义的边界／授权／提交配对关系），全部通过后才
  与既有会话集合一次原子替换；读档不调用规则事务，不重放供给、占用、提交、冲突或事件。

## 已执行验证

- `tools/check-data-chain.ps1`：通过（docs/csv counts、language keys、check-data-chain: OK）。
- `tools/check-review-text.ps1 -Paths 开发管理,docs,src/Assets/Scripts,src/Assets/Tests`：通过（658 files）。
- `tools/check-pending-whitespace.ps1 -ExpectedPaths <candidate 全路径>`：通过（18 files checked）。
- `git diff --cached --check`：通过（正式 candidate 提交时）。
- `tools/run-unity-editmode-tests.ps1`：331 执行 / 329 通过 / 2 失败。全部直接用例通过：
  生产静态目录（`ProductionStaticCatalogResolvesOnlyTheImportedDefinitionAndTheApprovedDirectory`、
  `StaticCatalogValidatorRejectsUndeclaredDuplicateOrZeroVersionDirectories`、
  `ContentCatalogDataExposesOnlyTheSingleApprovedStaticCatalogFailClosed`）、schema 4 往返深复制
  （`CharterRuntimeStateRoundTripsInSchemaFourAndLegacyVersionsRestoreUnaccessed`）、schema 0～3
  明确未接入、版本／presence／稳定 ID／提交篡改失败关闭与原子拒绝
  （`CharterSaveRestoreRejectsInvalidPayloadsAtomically`）、重复读取不重复结算
  （`CharterRestoreDoesNotReSettleAllocatedSuppliesOccupanciesOrCommits`）、缺静态目录失败关闭
  （`CharterPresenceRestoreFailsClosedWithoutTheStaticCatalog`）、条目边界／授权／提交配对关系
  负例（`DynamicStateRejectsNodesOrCoverageOutsideTheEntryDefinitionBoundary`）、导入器共用
  Runtime 校验（`ProductionCharterRuleDefinitionImportsTheApprovedWaterBureauChronicleAndProjectsItsAsset`
  等原有用例全数保持）。曾因 `JsonUtility` 把 JSON null 反序列化为默认实例触发 presence 误判，
  已按既有 `IsAbsentFoundationPurpleMansionState` 惯例以 `IsAbsentCharterRuntimeState` 明确
  空 payload 语义后全数通过。
- 2 项失败均为已登记且与本卡路径无交集的既有基线（多份验证记录与 `AI合作沟通.txt` 已登记）：
  `CharterConflictRulesTests.AuthorizedGrantBindsEveryConflictIdentityField`（shared 对等储备
  返回 `PULSE_NEUTRAL`，断言 `PULSE_ADVANTAGE`，文件来自 `D-TZ-CHARTER-CONFLICT-02`，不在本卡
  路径）与 `DataConfigImporterContentScopeTests.LianShenReservedAbilitiesRemainExcludedAtPlayerLoad`
  （旧日志断言与现行加载器不一致）。运行脚本因此以退出码 2 结束，属其零失败门禁的预期行为。
- `tools/check-task-cards.ps1 -TaskId U-TZ-CHARTER-SAVE-01 -Postcondition ExternalPendingReview`：
  当前 candidate 阶段按预期不通过——卡片仍为 `external_execute/deepseek/ready`，该后置条件要求
  卡片已转换为 `codex_review/codex/ready`；转换由共享 Windows 入口在正式提交中与 pending review
  投影原子完成，须由共享入口在转换后复跑通过。

## 已知验证边界

- 全量 EditMode 的 2 项既有基线失败（见上方）不属本卡路径，未修复、未掩盖；共享冲突契约
  所有者（Codex）需按既有登记裁决 `PULSE_ADVANTAGE` 断言与对等储备实现的不一致。
- 共享入口正式提交后需复跑 `check-task-cards.ps1 -TaskId U-TZ-CHARTER-SAVE-01 -Postcondition
  ExternalPendingReview` 与 `check-review-text.ps1`，再交由 Codex 独立复审。
