# AI协作规则

当本项目同时使用 Codex / ChatGPT5.5 与 DeepSeek V4 Flash 时，按本文件分工。用户当次明确指令优先，但不得违反 `AGENTS.md`。

## 总原则

- 设定与设计内容以 `docs/` 原文为准；进度与阶段判断以 `开发管理/` 为准；数值结论以 `simulations/BattleSim` 实际运行结果为准；Unity 实现以 `src/` 当前代码为准。
- 纯 `1` / `2` 都先读 `开发管理/当前任务队列.txt`；选中 ID 后以 `开发管理/任务卡/<ID>.txt` 为活跃任务唯一事实，纯 `2` 再读 `开发管理/审核入口.txt` 和对应证据。
- `当前任务队列.txt` 只保存有序 ready 投影；`设计-当前状态.txt` 只放已验证事实和当前风险；分线 backlog 与建议索引的维护细则见 `开发管理/状态与建议维护规则.txt`。
- 任务卡的 `必读` / `必查范围` 统一按 `开发管理/状态与建议维护规则.txt` 解释：先定位相关章节、符号、实体或用途，再读取当前步骤需要的原文；路径级条目不默认等于整份加载，修改前仍读取目标完整逻辑单元。
- 交接材料只记录当前线索、疑点和待判断项，不能替代 docs、管理文件、BattleSim、Unity 代码或 git diff。
- 普通定时调度先读 `开发管理/自动工作流规则.txt`。
- 控制器调度：`Show` 返回 existing recovery 时优先路由到 `开发管理/自动工作流恢复规则.txt`。
- 普通责任方：实际到达新的用户决定事件时才即时只读取 `创建决定恢复`；未到达决定事件时不得读取恢复规则。

## 主责

### Codex / ChatGPT5.5

- 未决需求、跨文档系统语义、核心架构、公共接口和数据所有者的定稿。
- 任务拆分、优先级、风险边界、验收条件，以及 BattleSim→Unity 同步策略。
- BattleSim 数值目标与最终结论、存档／安全／自动化控制面的方案和恢复决策。
- DeepSeek 交付的独立复审、冲突裁决和高风险结果签收。

### DeepSeek V4 Flash

- 按规范批量生产内容；每项完成品单独成文件。
- 文档清洗、CSV / Language / asset 对齐、机械一致性检查。
- 事实源、设计和验收条件已锁定的 Unity / BattleSim / 工具实现、bug fix、重构与迁移；不再按规模或跨文件数量限制，直接 build / test 必须闭合。
- 跨文件、跨模块 Code Agent 根因诊断与实施、全链回归、边界用例、批量迁移和素材规模扩展。
- 在 Codex 已冻结 schema、回滚边界和验收矩阵后，执行存档、安全或自动化控制面中的明确实现切片；完成后必须交 Codex 独立复审。

### 能力校准后的分派边界

- 官方 2026-07-31 更新将 API 实际版本升级为 `DeepSeek-V4-Flash-0731`，模型名仍为 `deepseek-v4-flash`；官方给出的 Agent 成绩包括 Terminal Bench 2.1 82.7、NL2Repo 54.2、DeepSWE 54.4、Toolathlon verified 70.3、Automation Bench 25.1，并明确说明远超 V4-Pro-Preview。公开 Code Agent 基准使用 DeepSeek Harness 极简模式、`max`、`top_p=0.95`、`temperature=1.0`；本项目 Claude CLI 调用保持 `max`，其余两项只视为该基准口径，不作为 Anthropic 兼容调用的必需路由参数。
- 依据 2026-07-31 `DeepSeek-V4-Flash-0731` 正式版的 Agent 能力校准：只要任务卡已冻结事实源、目标语义、授权路径、停止条件和可运行验收，就默认 `external_execute / deepseek`；适用于小型到大型、单模块到跨模块的代码与数据工作。
- Codex 默认不再保留已锁定规格下的实现主责，只保留未决架构／语义／所有者选择、任务拆分、数值目标与结论、高风险恢复决策及最终复审。存档、安全和自动化控制面可由 DeepSeek 执行，但必须先由 Codex 锁定方案、回滚与验收，且不得由 DeepSeek 自审。
- 任务实施中若发现必须改变已锁定语义、扩大授权路径或重新选择所有者，DeepSeek 立即停止并交回 Codex，不叠加兼容分支或自行重构工作流。

### Claude CLI / WF3-CLAUDE-ONE

WF3 定时器暂停；未来人工恢复或用户明确调用时，Claude CLI 仅有 DeepSeek / Claude 执行范围，不是 Codex 主责角色：

1. 先读取当前进程的 `ANTHROPIC_BASE_URL`；为空时只补读 `~/.claude/settings.json` 的同名字段。`http://127.0.0.1:15721` 同源地址（含 `/claude-desktop` 路径）的实际身份与修改方统一为 `DeepSeek V4 Flash`，不得自称 Codex 或 Claude。
2. 其他 Claude CLI 环境的实际身份与修改方为 Claude Code。
3. 普通选题只消费 `route=external_execute`、`dispatchState=ready` 且 owner 与执行器一致的有序队列卡；映射固定为 `owner=deepseek -> DeepSeek V4 Flash`、`owner=claude -> native Claude Code`。原生 Claude Code 读取 `开发管理/DeepSeek工作提示词.txt` 时，只继承任务路由、边界和交接格式。
4. `owner=codex`、`route=codex_review`、非 ready 或未明确授权的任务不可执行。用户当次明确指派原生 Claude Code 的具体手动任务仍可执行，但该例外不得改变自动 wrapper 行为。
5. 手动选题前在输出与 automation memory 记录实际身份、修改方及候选任务 ID / route / owner / dispatchState；无合格候选则记录 `skipped_cleanly` 后退出，不改项目文件或创建替代任务。

### 每小时控制器内的 DeepSeek lane wrapper

`tzg-hourly-controller` 通过通用 `lanes[]` 同时运行独立 Codex 与 DeepSeek Worker。外部 wrapper 只消费协调器已经 claim 的 `external_execute/owner=deepseek` 同一任务卡；`owner=deepseek -> DeepSeek V4 Flash`，不得重新扫描候选、改派 owner 或领取复审。

协调器取得唯一 lease、核对 `workerPaths/coordinatorPaths` 并创建 DeepSeek 专用 worktree 后，才预检 Claude CLI、`ANTHROPIC_BASE_URL` 和代理身份。DeepSeek Worker：

1. 只在传入 worktree 和 `workerPaths` 内实施、验证并标记「⚠️ 已修改/未审核」；不得修改 task card、队列、backlog 或交接文件。
2. 若 `stateReason` 命中 `REV-*`，仍只读取审核入口和同编号最新返工证据，遵守收窄边界。
3. 成功时创建一个只含 `workerPaths` 的内部候选提交，返回 candidate SHA、实际路径、验证、正式提交事实、`codex_review/codex/ready` transition 和 `coordinatorPaths` 内的完整机械变更提案；候选不使用正式 Automation 元数据，不通知。
4. 固定串行集成器重新核验 claim、父提交、路径、人工变化和任务投影后，应用候选与管理提案，创建 `State: pending_review` 的 canonical businessCommit；随后只更新 `开发管理/AI合作沟通.txt` 创建 handoffCommit。handoff 不使用 Automation 标记，不重复领域验证。
5. completed、needs_decision、blocked、failed 都绑定 batchId/laneId/taskId/identity/session。DeepSeek 不得自审、扩大授权、另行派发、stash、reset、checkout、clean 或推送。

只有固定集成器可以运行 `ExternalPendingReview` 后置条件、创建正式两个提交、更新 runtime、释放协调器 lease 和发送结果通知。

## 验证成本控制（适用于所有工作）

1. 默认使用与风险、改动范围和失败后果相称的最小充分验证集，只运行能直接证明本轮产物正确的检查；普通文档、局部规则或小范围实现不默认运行全项目回归，也不默认安排规格复审与质量复审各一轮。
2. 在任务选定时一次性列出必需验证，完成修改后尽量合并执行一次。若相关代码、事实源、配置、依赖、Git 基线和实际执行目标均未变化，已通过的同范围检查不得因流程阶段切换而重复运行。
3. 合并、切换实际执行目标或更新外部配置后，只对受影响边界做一次针对性复验；不得把“重新确认”扩大为无关的全套测试。检查失败时先定位失败层，只重跑修复所影响的检查和最终必要门禁。
4. 只有以下情形才升级为全量回归、独立复审或多阶段验证：共享基础设施与自动化控制面；安全、权限、写入隔离与恢复；不可逆删除或迁移；核心架构、公共接口、数据语义与数值平衡；DeepSeek/外部交接的正式复审；事实源冲突、已有失败证据；用户明确要求。升级时也应合并检查、避免对未变化范围重复取证。
5. 最小验证不得跳过领域硬约束：数值平衡仍运行 BattleSim，Unity/C# 仍运行任务直接涉及的 build/test，审核路由仍按审核规则执行，提交前的路径隔离、行尾空白与 Git 差异检查仍保留。

## 交接

1. 派工时明确主责、任务类型、允许修改范围、必读事实源和验证命令；外部责任方只接受有序队列已选中的 `external_execute` 同一任务卡。
2. DeepSeek 的修改只能标「⚠️ 已修改/未审核」，不得预填审核方结论；自动 lane 只创建 worker candidate，由固定集成器创建正式 `businessCommit` + `handoffCommit`。手动 DeepSeek 仍按手动 worktree 与交接边界提交。
3. 需要审核或判断时，DeepSeek 在 `AI合作沟通.txt` 写入短 ID、方向、任务/提交范围、修改文件、已验证、未验证、残留风险、请求判断和建议下一步。
4. Codex 消费交接时先读审核入口和匹配条目，再以 diff、事实源和可重复命令独立验证。
5. 长期阻塞项进入未通过清单或 backlog；已处理且无后续价值的交接条目移入归档。

## 快捷工作流：`1` / `2`

当用户消息去除首尾空白后严格等于 `1` 时，按有序队列推进一个当前 AI 可执行的执行型任务，不做复审。当消息严格等于 `2` 时，Codex / ChatGPT5.5 按有序队列推进一个复审任务；DeepSeek 只可补齐交接材料，不得自审。其他消息按用户明确要求执行。

### 通用步骤

1. 先确认身份：Codex / ChatGPT5.5 通过 Node REPL 读取 `nodeRepl.requestMeta['x-codex-turn-metadata'].model`；每小时控制器经 `tools/codex-cli-session.ps1` 启动的 Codex 责任方使用控制器以 `-Model` 和首条 stdin 输入传递的核验结果，不因 `codex exec` 不继承 request metadata 而重复阻塞；DeepSeek 按 `开发管理/DeepSeek工作提示词.txt` 的身份锚定；WF3-CLAUDE-ONE 还执行本文件的身份与授权自检。
2. 纯 `1` / `2` 先读取 `AGENTS.md` 与 `开发管理/当前任务队列.txt`；选中行后读取同一 ID 的 `开发管理/任务卡/<ID>.txt`。纯 `2` 再按审核入口加载该项需要的未通过条目、交接证据和目标 diff；DeepSeek 还读其工作提示词。
3. 检查 `git status --short`；存在无关改动时只处理本轮相关文件，绝不 stage 无关改动。
4. 纯 `1` 从队首向后选择当前 AI 可执行的第一项 `codex_execute` 或 `external_execute`；不消费 `codex_review`。
5. 手动 Codex / DeepSeek / Claude 纯 `1` 先跳过 `Show.state.batch.lanes[].taskClaim` 已认领任务；选中 ready 任务后、任何项目写入前，必须在 `.worktrees/` 创建任务分支 worktree。合并前重新 `Show` 并重读卡与队列；只有 `lease=null`、`batch=null`、原投影未变且目标路径不与主工作区人工改动冲突时才集成。自动 Worker 使用协调器传入的独立 worktree，只提交 worker candidate；不得直接使用主工作区。
6. 纯 `2` 从队首向后选择第一项 `codex_review`；DeepSeek / Claude 不得自审。
7. 路径冲突或执行器暂不可用时可在本轮继续检查下一行，但不修改顺序；明确逻辑阻塞必须先转状态并移出队列。
8. 队列为空时才读取分线 backlog 进行一次维护，本轮不执行新任务。
9. 选定真实对象后、读取实施细则前，调用 `tools.codex_app__set_thread_title`：`1` 为 `TZG｜<中文简述>`，`2` 为 `TZG｜复审：<中文简述>`。简述使用任务卡或复审事实源的中文标题；无候选或改名失败不阻断流程。
10. 命中审核、未审核、交接或复审路由时，按审核入口读取细则与相应事实源；普通执行任务按任务卡必查范围先定位、后读取，发现新依赖时补读，修改前读取目标完整逻辑单元。不得处理另一个 AI 主责的高风险任务，应写入交接请求。
11. 完成后按“验证成本控制”选择最小充分验证集并合并执行一次，只提交本轮文件；完成队列任务时同步更新同一任务卡、backlog 和 `当前任务队列.txt`。由 `tzg-hourly-controller` 启动的 Codex 执行与复审只形成候选提交，再由固定串行集成器创建 canonical 元数据提交；QueueMaintenance 与旧 decision/interruption Recovery 仍使用 `tools/invoke-codex-responsibility.ps1`。自动化提交的 `CommitMessage` 只传单行标题，并以 `AutomationTask`、`AutomationState=completed`、`AutomationResult`、`AutomationImpact`、`AutomationVerify`、`AutomationPlain` 分别传入事实字段，后四项严格使用 `问题=<原问题>；完成=<具体交付>`、`影响=<实际行为变化>；边界=<明确未涉及范围>`、`验证=<关键检查与结果>；后续=<解锁项、剩余依赖或下一状态>`、`发生=<负责人短句>；影响=<负责人短句>；需要=<负责人短句>` 的单行结构。九个子字段都非空且由本任务事实支持；三个通俗子字段各不超过 200 个 Unicode code point，明确说明发生了什么、实际影响以及负责人是否需要操作。finalizer 固定生成 `Automation: tzg-hourly-controller` 正文。`AutomationState=completed` 只表示责任方提交闭环，不表示任务卡已完成。旧 Codex 固定调用器只接受 QueueMaintenance 与 Recovery；task-bearing Recovery 使用 `CodexClosedOrNonReady`，QueueMaintenance 与 maintenance recovery 使用全局 `readyCount`。`readyCount>0` 才是 `refilled`；干净的零候选维护记录既有 `blocked/no_runnable_candidate` 且不制造提交。控制器和简报读取返回的 `taskState`、`readyCount` 或同一提交中的任务投影。

### 角色例外

- Codex / ChatGPT5.5 的 `1` 按队列顺序处理其主责执行型任务，不消费 `codex_review`；其自产工作完成必要验证后可闭环。
- Codex / ChatGPT5.5 的 `2` 按队列顺序处理第一项 `codex_review`；通过后按审核规则关闭，未通过则更新未通过清单。
- DeepSeek 的 `1` 只做其主责执行型任务；所有修改标为未审核，按外部两提交边界完成业务提交与交接提交。
- DeepSeek 的 `2` 不得复审自己；仅补齐交接材料、验证输出和请求复审范围。

## 冲突处理

用户当次明确指令 > `AGENTS.md` > `开发管理/` 当前状态 > `docs/` 原文 > BattleSim / Unity 实际代码。数值平衡结论必须有 BattleSim 运行结果；发现 docs、BattleSim、Unity 不一致时，先指出冲突并确定主事实源，再同步相关文件。
