# 天章 — 当前开发优先级

> 任务选择先读 `开发管理/当前任务队列.txt`；三道可信度闸门的任务定义、依赖和验收以 `docs/superpowers/specs/2026-07-10-trust-gates-and-vertical-slice-task-system-design.md` 为准。

## 当前决策

- 内容冻结：G1、G2、G3 均通过且 TQ-071 完成前，不把新的剧情、角色、功法、术法、神通或世界设定扩写补入当前队列。
- 既有数据纠错、运行时字段补齐、验证和复核不属于内容扩写，但不得借此扩大内容范围。
- 当前工作只从依赖已满足的任务卡中领取；任务状态、主责与完成条件只认当前队列。

## 三道 P0 闸门

| 闸门 | 当前状态 | 尚余入口 | 通过条件 |
|------|----------|----------|----------|
| G1 正式构建链 | 通过 | 无 | 已由 TQ-063 的正式 Build Settings 端到端冒烟证据满足。 |
| G2 BattleSim 数值可信度 | 通过 | TQ-055 归档 | 21 Build 具有合法输入、规定覆盖和可复现输出；全部极端结果均已分类。 |
| G3 数据链路语义可信度 | 等待 TQ-057 / TQ-059 | `开发管理/当前任务队列.txt` 的 TQ-057、TQ-059 | 既存矛盾清理完成，境界、元素、阵营字段具有唯一且可观察的语义。 |

## P1 解锁与任务入口

- G1 已通过，P1 架构治理可按队列并行推进；当前可选入口为 TQ-064（结构事实图）和 TQ-068（最小会话状态）。
- G3 与 TQ-071 全部满足前，不能解冻运行时薄内容切片，更不能恢复世界级内容扩写。
- 场景、数据、数值、内容与审核的候选任务分别位于 `开发管理/任务列表/`；只有满足依赖、范围可验证且可提交的条目才能补入当前队列。

## 归档

- 被替代的推进路线、资产盘点、机制同步表和状态日志见 `开发管理/任务归档/2026-07-12-当前状态瘦身前归档.txt`。
- 已完成的迁移差异表与数值方案见 `开发管理/任务归档/` 中同名文件；它们仅保留决策依据，不替代本文件或当前队列。
