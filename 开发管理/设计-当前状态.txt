# 设计当前状态（✅ 已审核）

> 本文件只记录可用于当前决策的已验证事实与风险。任务状态、依赖和验收以 `开发管理/当前任务队列.txt` 为准；详细规格以 `docs/superpowers/specs/2026-07-10-trust-gates-and-vertical-slice-task-system-design.md` 为准。

## 当前项目边界

- 游戏形态：2D 沙盒世界、战棋玩法的修仙游戏；战斗采用 CTB 与六角格。
- 玩家主线口径：凡人、练气、筑基五段（含紫府阶段）、金丹三段；高阶内容仅按现行 `docs/` 原文的 NPC、远期或资料片定位处理。
- 内容冻结有效：G1、G2、G3 与 TQ-071 未全部满足前，不扩写新的剧情、角色、功法、术法、神通或世界设定。

## 可信度闸门

| 闸门 | 状态 | 已验证事实 | 当前风险 |
|------|------|------------|----------|
| G1 正式构建链 | 通过 | TQ-063 已证明 Build Settings 正式链可从角色创建、出身与起点进入 Adventure，并返回据点且保持会话上下文；Unity batchmode 架构检查返回 0，EditMode 60/60 通过。 | 正式冒险所有者限定为 `AdventureScene`，旧探索场景不得重新进入可达链路。 |
| G2 BattleSim 数值可信度 | 等待 TQ-055 | TQ-044、TQ-052、TQ-053、TQ-054 已完成并满足 TQ-055 的前置条件。 | 尚无满足规定覆盖的 21 Build 重跑与极端结果分类；现有矩阵不得作为全局平衡结论。 |
| G3 数据链路语义可信度 | 等待 TQ-057 / TQ-059 | TQ-043、TQ-056、TQ-058 已完成；`contentScope` 已按失败关闭口径处理。 | 既存数据矛盾仍待 TQ-057 清理；TQ-059 尚须让 `realmReq`、`elementReq`、`affiliation` 具备可观察语义。 |

## 当前系统事实

### BattleSim

- BattleSim 是数值结论的唯一模拟验证入口；当前 G2 工作不得在重验过程中直接调参。
- 暴击倍率口径已固定为基础 ×1.50 之上的附加百分比点；固定回归为 `0 → 1.50`、`15 → 1.65`、元素 +10 → `1.75`。
- 2v2 CT 每周期按反应值累加，反应较高者先行动，同值按输入顺序稳定。
- 功法成长表完整性与星级回退已作为 G2 输入前置修复；最终覆盖、置信区间和极端结果结论以 TQ-055 输出为准。

### Unity 正式链

- 当前正式链的冒险运行时所有者为 `AdventureScene`；角色创建、出身、起点、据点、Adventure 与返回据点的会话上下文具有回归证据。
- 运行时所有者的直接事实源为 `开发管理/运行时所有者记录-TQ-060.txt`；结构治理任务必须重新扫描工作树，不以旧总览推断。

### 数据链路与有效规模

| 内容 | docs | CSV | Unity asset | 有效口径 |
|------|------|-----|-------------|----------|
| 功法 | 29 | 29 | 29 | 已对齐的现有内容规模；不表示新增内容获准。 |
| 术法 | 75 | 75 | 75 | 已对齐的现有内容规模；字段语义仍受 G3 约束。 |
| 神通 | 34 | 34 | 34 | 已对齐的现有内容规模；字段语义仍受 G3 约束。 |

- 数量对齐与 asset 存在不等于运行时限制有效；G3 通过前，不能以计数闭合推断数据语义可信。
- 设定原文始终以 `docs/` 下对应文件为准；本文件不替代设计规范或内容事实源。

## 当前风险与下一步

1. G2 未完成前，不得发布 BattleSim 平衡结论，也不得依据极端胜率调整设计方向。
2. G3 未完成前，字段不能继续以“已导入”伪装为有效的运行时限制或明确 metadata。
3. 内容冻结持续有效；P1 架构治理虽然可与 G2/G3 并行，但不构成内容解冻条件。
4. 执行任务从 `开发管理/当前任务队列.txt` 选取；分线任务只作为候选入口。

## 归档

旧版本、机制明细、历史矩阵与完成日志见 `开发管理/任务归档/2026-07-12-当前状态瘦身前归档.txt`。它们不作为当前状态判定依据。
