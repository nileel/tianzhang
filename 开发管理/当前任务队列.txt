# 当前任务队列（✅ 已审核）

> 2026-07-10 Codex / gpt-5.5：用户批准启用“三道 P0 可信度闸门 + 最小纵向切片”任务体系。重排前全文归档至 `开发管理/任务归档/2026-07-10-可信度闸门重排前队列归档.txt`；设计规格见 `docs/superpowers/specs/2026-07-10-trust-gates-and-vertical-slice-task-system-design.md`。
> 内容冻结：G1 正式构建链、G2 BattleSim 数值可信度、G3 数据链路语义可信度全部通过且 TQ-071 完成前，禁止把新的剧情、角色、功法、术法、神通或世界设定扩写补入本队列。
> 本文件只放近期可执行、可验证、可提交的任务切片；复审和交接任务走 `开发管理/审核入口.txt`。
> 2026-07-11 Codex / gpt-5.6-sol：TQ-049 已完成；修正 EditMode 夹具对 `Awake` 的错误依赖，冻结基线 47/47 通过，TQ-050 解除阻塞。
> 2026-07-11 Codex / gpt-5.6-terra：TQ-043 已完成并归档至 `开发管理/任务归档/2026-07-11-TQ-043-按表头解析导入器修复归档.txt`；TQ-056 解除阻塞。
> 2026-07-11 Codex / gpt-5.6-terra：TQ-050 已完成；新增权威 EditMode 入口及 XML 正反例测试，完整 Unity EditMode 54/54 通过，TQ-051 解除阻塞。
> 2026-07-11 Codex / gpt-5.6-terra：TQ-051 已完成；权威入口在通过结果后会限时等待 Unity 退出并恢复快照，真实 EditMode 54/54 通过且场景、Build Settings 哈希与工作区均无残留。TQ-060 解除阻塞，待下一轮队列维护补位。
> 2026-07-11 Codex / gpt-5.6-terra：队列维护将已解除阻塞的 TQ-060 及其已登记后继 TQ-061、TQ-062 补入近期队列；当前共 6 条，内容冻结保持不变。
> 2026-07-11 Codex / gpt-5.5：TQ-052 已完成并归档至 `开发管理/任务归档/2026-07-11-TQ-052-2v2CT反应方向归档.txt`；独立工具链提交 `0452830` 与本轮 CT 回归均已验证，TQ-053 解除阻塞。
> 2026-07-11 Codex / gpt-5.6-terra：TQ-053 已完成并归档至 `开发管理/任务归档/2026-07-11-TQ-053-统一暴击倍率语义归档.txt`；Unity EditMode 55/55、BattleSim 自测和默认模拟均通过，TQ-054 解除阻塞并补入近期队列；G2 尚未通过。
> 2026-07-11 Codex / gpt-5.6-terra：TQ-054 已完成并归档至 `开发管理/任务归档/2026-07-11-TQ-054-模拟Build输入角色创建规则归档.txt`；21 个 BattleSim Build 现在均通过镜像的创建点购校验，非法输入在矩阵前确定失败，TQ-044 解除阻塞；G2 尚未通过。
> 2026-07-11 Codex / gpt-5.6-terra：TQ-056 已完成并归档至 `开发管理/任务归档/2026-07-11-TQ-056-数据检查器错误分级归档.txt`；夹具正反例通过，生产检查器已失败关闭并显式列出历史数据矛盾。TQ-057、TQ-058 解除阻塞，TQ-059 仍等待二者。
> 2026-07-12 Codex / gpt-5.6-terra：TQ-060 已完成并归档至 `开发管理/任务归档/2026-07-12-TQ-060-冒险运行时所有者归档.txt`；`AdventureScene` 锁定为唯一正式冒险入口，`ExplorationScene` 定性为不可达旧原型。TQ-061、TQ-062 解除阻塞，必须先读 `开发管理/运行时所有者记录-TQ-060.txt`。
> 2026-07-12 Codex / gpt-5.6-terra：TQ-061 已完成并归档至 `开发管理/任务归档/2026-07-12-TQ-061-正式冒险战斗闭环归档.txt`；正式 `AdventureScene` 已接入最小六角地图、单敌遭遇、既有 `TacticalCombatController` 与胜败返回来源上下文。TQ-063 仍等待 TQ-062。

## 使用规则

1. 用户发送纯 `1` 时，先确认身份，再选择主责匹配、状态为“待处理”、依赖已满足的最高优先级任务。
2. 状态为“阻塞”的任务不得执行；前置完成后由队列维护工作流改为“待处理”。
3. 内容冻结期间 P0 闸门任务始终优先；当没有主责匹配且依赖已满足的 P0 闸门任务时，才可补已登记、依赖满足的 P1 架构/状态任务或 TQ-045 等明确后置非内容任务；禁止补冻结内容或提前执行 TQ-071。
4. 本文件保持 5–10 条任务；完成项归档，长期事实写入 `开发管理/设计-当前状态.txt`。
5. 用户发送纯 `2` 时读取 `开发管理/审核入口.txt`，不从本文件领取执行任务。

## 队列表头

| ID | 优先级 | 主责 | 类型 | 状态 | 任务 |
|----|--------|------|------|------|------|
| TQ-062 | P0 | Codex / gpt-5.5 | G1 Unity | 待处理 | U-SLICE-03：角色出身驱动起始节点 |
| TQ-057 | P0 | DeepSeek V4 Pro | G3 数据 | 待处理 | D-TRUST-02：清理现存数据矛盾 |
| TQ-058 | P0 | Codex / gpt-5.5 | G3 数据 | 待处理 | D-TRUST-03：contentScope 失败关闭与运行时过滤 |
| TQ-059 | P0 | Codex / gpt-5.5 | G3 数据 | 阻塞（TQ-057、TQ-058） | D-TRUST-04：接通境界、元素和阵营限制 |

## 不进入 `1` 队列的当前事项

- TQ-039：内容冻结。
- TQ-040：不单独执行；仅在 TQ-057 已登记且前置满足后由其吸收。
- TQ-045：G1 通过后再作为 P2 机制回归。
- TQ-055～TQ-071：仅在对应分线 backlog 已登记且依赖满足时补位；当前未登记项不得补位。

## 任务卡片

### TQ-057 · D-TRUST-02 清理现存数据矛盾

- 来源：可信度闸门规格 §6。
- 当前状态：待处理；依赖：TQ-056 已由 Codex 完成并验证。
- 主责：DeepSeek V4 Pro 执行；Codex / ChatGPT5.5 复审。
- 范围：对齐术法 docs/CSV/asset 数量，处理 `realm_lianshen` 缺失语言键与 `realm_lianxu` 删除后仍激活的数据，并吸收 TQ-040 的字段完整性复查；不得扩展内容或设定语义。
- 验证：`powershell -ExecutionPolicy Bypass -File tools/check-data-chain.ps1` 仅在剩余问题均有精确批准豁免或已清理时返回 0；`git diff --check`。
- 完成条件：TQ-056 所列历史数据矛盾已清理或被精确批准豁免，交接材料标明仍需 Codex / ChatGPT5.5 复审。

### TQ-058 · D-TRUST-03 contentScope 失败关闭与运行时过滤

- 来源：可信度闸门规格 §6。
- 当前状态：待处理；依赖：TQ-043、TQ-056 均已由 Codex 完成并验证。
- 主责：Codex / gpt-5.5。
- 必读：TQ-056 归档、`src/Assets/Scripts/Editor/DataConfigImporter.cs`、三类数据对象与实际 runtime 内容池消费者。
- 范围：缺失或非法 `contentScope` 必须导入失败；reserved 不进入玩家可获得池；补齐或隔离既有 asset 的缺失字段，不能静默默认为 player。
- 验证：建立允许/拒绝回归，运行相关 Unity 编译与 EditMode 测试、`powershell -ExecutionPolicy Bypass -File tools/check-data-chain.ps1`、`git diff --check`。
- 完成条件：CSV→asset→runtime 过滤链可观察且可测试，非法/旧值走记录的失败或隔离路径。

### TQ-059 · D-TRUST-04 接通境界、元素和阵营限制

- 来源：可信度闸门规格 §6。
- 当前状态：阻塞（TQ-057、TQ-058）；依赖：TQ-043、TQ-056、TQ-057、TQ-058。
- 主责：Codex / gpt-5.5。
- 范围：为 `realmReq`、`elementReq`、`affiliation` 分别选择并验证 runtime gate 或 metadata-only 模式，禁止字段继续伪装为有效限制。
- 验证：每个 runtime gate 具有允许与拒绝样例；运行 `powershell -ExecutionPolicy Bypass -File tools/check-data-chain.ps1` 与相关 Unity 回归。
- 完成条件：三个字段都有唯一、可观察的运行时或显式元数据口径，G3 才可进入出口检查。

### TQ-060 · U-SLICE-01 锁定冒险运行时唯一所有者

- 来源：可信度闸门规格 §4、`开发管理/任务列表/场景与Unity任务.txt`。
- 当前状态：待处理；依赖：TQ-051 已由 Codex 完成并验证（`37efe0e`、`9e9fe7d`）。
- 主责：Codex / gpt-5.5。
- 必读：`src/ProjectSettings/EditorBuildSettings.asset`、`src/Assets/Scenes/`、`src/Assets/Scripts/SceneBuilder.cs`、场景流转与冒险/探索/战斗入口相关脚本、`开发管理/任务列表/场景与Unity任务.txt`。
- 范围：证明正式 Build Settings、`AdventureScene`、旧 `ExplorationScene`、`SceneBuilder`、冒险控制器和战斗入口的真实调用链；为竞争入口决定迁移、适配或淘汰路径，禁止保留两个竞争的可达运行时入口。
- 验证：基于实时扫描产出运行时所有者记录、场景迁移边界及允许/禁止修改清单；运行 `dotnet build src/Assembly-CSharp.csproj`、`dotnet build src/TianZhang.EditModeTests.csproj`、`powershell -ExecutionPolicy Bypass -File tools/check-review-text.ps1 -Paths 开发管理,src/Assets/Scripts,src/Assets/Tests`、`git diff --check`。
- 完成条件：能从 StartMenu 的实际调用链唯一定位冒险场景、战斗入口和返回上下文；每个竞争候选都有保留或淘汰理由，且产物不得只靠推测或迁移计划。

### TQ-062 · U-SLICE-03 角色出身驱动起始节点

- 来源：可信度闸门规格 §4、`开发管理/任务列表/场景与Unity任务.txt`、`开发管理/运行时所有者记录-TQ-060.txt`。
- 当前状态：待处理；依赖：TQ-060 已完成。
- 主责：Codex / gpt-5.5。
- 必读：TQ-060 的运行时所有者记录、角色创建与 `GameSession.PlayerProfile`、世界节点选择及场景流转实现。
- 范围：移除新游戏流程对江左起点的硬编码；由角色创建的出身数据映射起始世界节点，并为非法或旧存档值提供显式回退。
- 验证：对当前两个可选出身和非法/旧值分别建立回归；运行相关 Unity 编译与 EditMode 测试、`powershell -ExecutionPolicy Bypass -File tools/check-review-text.ps1 -Paths 开发管理,src/Assets/Scripts,src/Assets/Tests`、`git diff --check`。
- 完成条件：当前两个可选出身进入各自起始节点；非法值走被记录的专用回退路径，不能静默伪装为合法出身值。
