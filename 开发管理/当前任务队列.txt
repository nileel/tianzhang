# 当前任务队列（✅ 已审核）

> 内容边界：TQ-071 只批准“关中：小族出身 → 关中城 → 关中野外的一场战斗”薄内容切片。其他剧情、角色、功法、术法、神通或世界设定扩写不得补入本队列；批准切片也须先建立完整任务卡。
> 本文件只放近期可执行、可验证、可提交的任务切片；复审和交接任务走 `开发管理/审核入口.txt`。
> 过程记录已归档至 `开发管理/任务归档/2026-07-12-管理入口瘦身前状态归档.txt`；当前状态以本文件为准。

## 队列表头

| ID | 优先级 | 主责 | 类型 | 状态 | 任务 |
|----|--------|------|------|------|------|
| U-2_5D-01A | P2 | Codex / gpt-5.6-terra | 场景与 Unity | 已完成 | 2.5D 战棋原型规则兼容验证清单 |
| U-2_5D-01B | P2 | Codex / gpt-5.6-terra | 场景与 Unity | 已完成 | 2.5D 原型规则查询所有者定位 |
| U-2_5D-01C | P2 | Codex / gpt-5.6-terra | 场景与 Unity | 已完成 | 共享空间查询契约与旧范围逻辑停用方案 |
| U-2_5D-01D | P2 | Codex / gpt-5.6-terra | 场景与 Unity | 待处理 | 共享空间查询契约实现与旧范围逻辑迁移 |
| N-GROUP-02A | P3 | Codex / gpt-5.6-terra | 数值与战斗 | 待处理 | 2v2 范围技能、协同走位与团队 AI 参数事实源与决策输入 |
| D-REALM-02A | P2 | Codex / gpt-5.6-terra | 数据链路 | 已完成 | 高阶内容现行引用分类与处理决策输入 |
| N-SLOT-01 | P2 | Codex / gpt-5.5 | 数值与战斗 | 阻塞（N-FPD-DANXIANG-01、N-FPD-MANSION-01） | 金丹槽位与丹枢加成数值方案 |

## 任务卡片

### U-2_5D-01A · 2.5D 战棋原型规则兼容验证清单

- 来源：`开发管理/任务列表/场景与Unity任务.txt` U-2_5D-01、`开发管理/设计-当前状态.txt` 的 G1 通过事实，以及 `docs/基础设定/战场空间与环境规则.txt` 第一、四、七、八节。
- 当前状态：已完成；依赖：G1 已通过，TQ-064／TQ-067 的结构与程序集边界已完成；无待决策。
- 主责：Codex / gpt-5.6-terra。
- 必读：`开发管理/开发-技术经验.txt`、`开发管理/任务列表/场景与Unity任务.txt`、`开发管理/设计-当前状态.txt`、`UNITY_STRUCTURE.md`、`docs/基础设定/战场空间与环境规则.txt` 与 `src/Assets/Scripts/Grid/TacticalGridModel.cs`。
- 预期修改路径：`开发管理/2.5D战棋原型验证清单.txt` 与 `开发管理/当前任务队列.txt`；执行前须确认这些路径不与人工改动冲突。
- 范围：只创建原型前的规则兼容验证清单，逐项固定同一 `TacticalGridModel`、`HexCoord` 单锚点占位、加权距离／范围／视线、`heightLevel` 与有向格边、实体障碍和 2D 回退的观察要求；清单明确 2.5D 仅为渲染表现，规则不得读取 Unity Transform。
- 禁止：不创建或修改 Unity 场景、渲染器、战斗代码、CTB、伤害、寻路、技能范围、数据资产或正式冒险入口；不把旧 `ExplorationScene` 重新接入正式链路。
- 验证：`pwsh -NoProfile -ExecutionPolicy Bypass -File tools/check-review-text.ps1 -Paths 开发管理/2.5D战棋原型验证清单.txt,开发管理/当前任务队列.txt`，以及 `git diff --check`。
- 完成条件：清单逐项覆盖 2D／2.5D 同一格位数据、Transform 非规则事实、单锚点占位、高度与有向格边通行、加权距离范围与视线、实体障碍、2D 回退和正式入口边界；每项均可由后续原型的可观察输出验证。
- 停止条件：任一要求需要新增未定义的高度层、三维多层占位、Transform 驱动规则或改变正式场景／战斗语义；停止并要求决策。
- 完成记录：已创建 `开发管理/2.5D战棋原型验证清单.txt`；它将当前 `TacticalGridModel` 未承载的有向格边、加权距离、范围与视线列为启动前必须定位既有唯一所有者的门槛，不把清单当作规则已实现结论。

### U-2_5D-01B · 2.5D 原型规则查询所有者定位

- 来源：`开发管理/任务列表/场景与Unity任务.txt` U-2_5D-01，以及已完成的 `开发管理/2.5D战棋原型验证清单.txt` 第一节和逐项兼容验证第 3～6 项。
- 当前状态：已完成；依赖：G1 已通过，TQ-064／TQ-067 已完成，U-2_5D-01A 已完成。定位记录确认 `HexBattlefield` 是有向格边、加权距离、视线和实体障碍所有者，但 BattleSim 加权＋视线范围判定与旧 Unity 原型的基础六角距离范围判定冲突，且无现成共享查询契约。负责人已通过 `DEC-20260722-U25D01B` 选择 A：新立共享查询契约对齐任务并停用旧范围逻辑。
- 主责：Codex / gpt-5.6-terra。
- 必读：`开发管理/开发-技术经验.txt`、`开发管理/任务列表/场景与Unity任务.txt`、`开发管理/设计-当前状态.txt`、`UNITY_STRUCTURE.md`、`开发管理/2.5D战棋原型验证清单.txt`、`src/Assets/Scripts/Grid/TacticalGridModel.cs` 与 `simulations/BattleSim/Program.cs`。
- 预期修改路径：新建 `开发管理/2.5D战棋原型规则查询所有者记录.txt` 与 `开发管理/当前任务队列.txt`；执行前须确认这些路径不与人工改动冲突。
- 范围：只对现有 `src/Assets/Scripts/`、`simulations/BattleSim/` 和上述事实源进行只读定位，分别记录有向格边、加权距离、范围、视线和实体障碍的唯一规则查询所有者。记录必须逐项给出实际文件、类型／成员、输入、输出或拒绝原因，以及它与 `HexCoord`、`HeightLevel`、占位的关系；不得把 `Transform`、Collider、Camera、Renderer、欧氏距离或模型间距列为规则所有者。
- 禁止：不创建或修改 Unity 场景、渲染器、战斗代码、BattleSim 规则、数据资产、正式入口或原型表现；不补造缺失 API、默认值、兼容分支、fixture 或高度／格边语义。
- 验证：`pwsh -NoProfile -ExecutionPolicy Bypass -File tools/check-review-text.ps1 -Paths 开发管理/2.5D战棋原型规则查询所有者记录.txt,开发管理/当前任务队列.txt`，以及 `git diff --check`。
- 完成条件：记录覆盖五类查询；每类都能回链到一个既有、可调用的唯一规则所有者及其精确位置，或明确列为缺失／冲突并附实际扫描范围。只有五类所有者均明确且不读取渲染状态时，才可把本卡改为已完成待归档；记录不得把定位结果当作 2.5D 原型已经实现。
- 停止条件：任一查询没有既有唯一所有者、多个所有者语义冲突、或要使其可用必须新增规则、Transform 驱动或正式链变更；保留定位记录，将本卡标为等待负责人决策，不修改业务代码。
- 完成记录：已创建 `开发管理/2.5D战棋原型规则查询所有者记录.txt`，逐项记录五类查询及实际冲突；负责人选择 A 后新增 U-2_5D-01C。本卡完成只表示定位与决策闭环，不表示共享所有者或 2.5D 原型已实现。

### U-2_5D-01C · 共享空间查询契约与旧范围逻辑停用方案

- 来源：U-2_5D-01B 的定位记录，以及负责人对 `DEC-20260722-U25D01B` 的选择 A。
- 当前状态：已完成；依赖：U-2_5D-01B 已完成；无待决策。
- 主责：Codex / gpt-5.6-terra。
- 必读：`开发管理/开发-技术经验.txt`、`开发管理/设计-当前状态.txt`、`UNITY_STRUCTURE.md`、`开发管理/2.5D战棋原型验证清单.txt`、`开发管理/2.5D战棋原型规则查询所有者记录.txt`、`docs/基础设定/战场空间与环境规则.txt`、`src/Assets/Scripts/Core/HexCoord.cs`、`src/Assets/Scripts/Core/HexGrid.cs`、`src/Assets/Scripts/Grid/TacticalGridModel.cs`、`src/Assets/Scripts/Combat/CombatResolver.cs`、`src/Assets/Scripts/Combat/TacticalCombatController.cs`、`src/Assets/Scripts/Combat/EnemyAI.cs`、当前 `.asmdef`／`.asmref`、`simulations/BattleSim/Battlefield.cs`、`simulations/BattleSim/Combat.cs`、`simulations/BattleSim/BattleSim.csproj` 与 `simulations/BattleSim/BattleSimSelfTests.cs`。
- 预期修改路径：新建 `开发管理/2.5D共享空间查询契约对齐方案.txt` 与更新 `开发管理/当前任务队列.txt`；执行前须确认这些路径不与人工改动冲突。
- 范围：只基于当前源码和程序集依赖确定可由 Unity 与 BattleSim 共同消费的唯一空间查询源代码所有者及最小迁移方案。方案必须逐项固定有向格边、固定点加权距离、范围、视线和实体障碍的类型／成员、输入、输出、拒绝原因，明确 Unity／BattleSim `HexCoord`、`TacticalGridModel.HeightLevel`、单锚点占位与实体障碍的映射，列出所有必须停用或迁移的 Unity `HexCoord.Distance` 范围调用，并为后续实现卡给出完整字面量修改路径和最小验证命令。
- 决策约束：共享契约对齐后，2.5D 原型不得再以 `HexGrid.GetTilesInRange`、`CombatResolver`、`TacticalCombatController` 或其他基础六角步数分支独立判定规则范围；BattleSim 现有加权距离、视线和拒绝原因不得被表现层或兼容分支降级。
- 禁止：本卡不修改 Unity／BattleSim 代码、程序集、数据资产、场景、渲染器、正式入口、CTB、伤害、技能数值或规则事实源；不预先创建接口、适配器、默认值、兼容分支、fixture 或第二套查询服务。
- 验证：`pwsh -NoProfile -ExecutionPolicy Bypass -File tools/check-review-text.ps1 -Paths 开发管理/2.5D共享空间查询契约对齐方案.txt,开发管理/当前任务队列.txt`，以及 `git diff --check`。
- 完成条件：方案基于实际程序集边界确定一个且仅一个源代码所有者，给出五类查询完整契约、Unity／BattleSim 数据流、旧调用停用清单、失败关闭策略、精确实现路径和最小充分验证；同时新增一个可直接执行且不再需要架构猜测的实现任务卡。方案不得把设计结论写成已实现事实。
- 停止条件：唯一所有者要求新增未定义的高度层、三维占位、格边类型或范围语义，要求改变正式冒险／战斗规则，或在现有程序集边界内只能形成两套独立实现；保留方案证据并请求负责人决定，不以适配层掩盖冲突。
- 完成记录：已创建 `开发管理/2.5D共享空间查询契约对齐方案.txt`。方案将唯一源代码所有者定为 `src/Assets/Scripts/Core/SpatialRules/` 的纯 C# 共享源，Unity 通过 Foundation 消费、BattleSim 以 linked compile 消费；并固定五类查询、数据映射、失败关闭、旧 Unity 基础距离停用清单及精确验证。新增 U-2_5D-01D 实现卡；本卡不表示 Unity/BattleSim 已迁移或 2.5D 原型已启动。

### U-2_5D-01D · 共享空间查询契约实现与旧范围逻辑迁移

- 来源：`开发管理/2.5D共享空间查询契约对齐方案.txt`，以及 `DEC-20260722-U25D01B = A`。
- 当前状态：待处理；依赖：U-2_5D-01C 已完成；无待决策。
- 主责：Codex / gpt-5.6-terra。
- 必读：`开发管理/开发-技术经验.txt`、`开发管理/设计-当前状态.txt`、`UNITY_STRUCTURE.md`、`开发管理/2.5D共享空间查询契约对齐方案.txt`、`docs/基础设定/战场空间与环境规则.txt`、当前 `src/Assets/Scripts/Core/HexCoord.cs`、`HexGrid.cs`、`Grid/TacticalGridModel.cs`、`Grid/EnvironmentProfileData.cs`、`Combat/CombatResolver.cs`、`Combat/TacticalCombatController.cs`、`Combat/EnemyAI.cs`、当前 `.asmdef`／`.asmref`、`simulations/BattleSim/Battlefield.cs`、`Combat.cs`、`BattleSim.csproj` 与 `BattleSimSelfTests.cs`。
- 预期修改路径：新建 `src/Assets/Scripts/Core/SpatialRules/SpatialHexCoord.cs`、`SpatialQueryTypes.cs`、`SpatialQueryBoard.cs`、`src/Assets/Scripts/Grid/SpatialQueryBoardFactory.cs`、`src/Assets/Tests/EditMode/SpatialQueryBoardTests.cs`；更新 `src/Assets/Scripts/Core/HexGrid.cs`、`src/Assets/Scripts/Grid/TacticalGridModel.cs`、`EnvironmentProfileData.cs`、`src/Assets/Scripts/Editor/DataConfigImporter.cs`、`src/Assets/DataConfig/EnvironmentProfiles.csv`、`src/Assets/DataConfig/README.txt`、`src/Assets/Tests/EditMode/EnvironmentProfileDataTests.cs`、`src/Assets/Scripts/Combat/CombatResolver.cs`、`TacticalCombatController.cs`、`EnemyAI.cs`、`src/Assets/Scripts/Map/ExplorationController.cs`、`simulations/BattleSim/BattleSim.csproj`、`Battlefield.cs`、`Combat.cs`、`BattleSimSelfTests.cs` 与 `开发管理/当前任务队列.txt`；执行前须确认这些路径不与人工改动冲突。
- 范围：按方案创建唯一纯 C# 空间查询源，Unity 经 Foundation、BattleSim 经 linked compile 共同消费；以显式数据映射接入有向边、固定点距离、范围、视线、实体障碍、等高单锚点占位和稳定拒绝原因。环境档案边规则与查询限制必须变为必填；无生产档案时只更新表头、导入器与 fixture。迁移/删除方案列出的 Unity 基础步数战斗范围分支，不重新接入 `ExplorationScene`，不创建第二服务、默认值或兼容分支。
- 禁止：不新增高度层、三维占位、格边类别、范围数值、场景、渲染器、正式入口、CTB、伤害、技能数值或生产环境档案；不从 Transform、Collider、Camera、Renderer、模型间距或欧氏距离读取规则；不保留 `HexGrid.GetTilesInRange` 或其他基础步数范围 fallback。
- 验证：先按 TDD 让共享契约失败测试证明缺失行为，再运行 `dotnet build -c Release --no-restore simulations/BattleSim`、`dotnet run --no-build -c Release --project simulations/BattleSim -- --self-test environment-rules-n-env-01`、`dotnet run --no-build -c Release --project simulations/BattleSim -- --self-test distance-model-tq055`、`dotnet build src/Assembly-CSharp.csproj`、`dotnet build src/TianZhang.EditModeTests.csproj`、`pwsh -NoProfile -ExecutionPolicy Bypass -File tools/run-unity-editmode-tests.ps1`、`pwsh -NoProfile -ExecutionPolicy Bypass -File tools/check-data-chain.ps1` 与 `git diff --check`。
- 完成条件：同一份 `SpatialRules` 源文件是 Unity/BattleSim 五类查询唯一算法来源；边、距离、范围、视线、实体障碍、占位与高度未配置均有可观察结果；所有列出的 Unity 战斗范围分支已迁移/删除且扫描无 fallback；BattleSim 和 Unity 直接测试均通过；2.5D 原型仍未启动，正式冒险入口仍唯一为 `AdventureScene`。
- 停止条件：实现需要设定未定义的高度资格/垂直触及、三维占位、格边类别或范围数值，必须保留两份算法，或必须用默认值、兼容分支或视觉状态维持旧范围；停止并请求负责人决定，只保留失败证据。

### N-GROUP-02A · 2v2 范围技能、协同走位与团队 AI 参数事实源与决策输入

- 来源：`开发管理/任务列表/数值与战斗任务.txt` N-GROUP-02，以及 `开发管理/设计-当前状态.txt` 的 G2 通过事实。
- 当前状态：待处理；依赖：G2 已通过，N-GROUP-01 已归档；本卡不等待参数决策，只生成可供负责人选择的事实输入。
- 主责：Codex / gpt-5.6-terra。
- 必读：`开发管理/开发-技术经验.txt`、`开发管理/设计-当前状态.txt`、`开发管理/任务列表/数值与战斗任务.txt`、`simulations/BattleSim/Battlefield.cs`、`Combat.cs`、`BattleSimSelfTests.cs` 与 `Program.cs`。
- 预期修改路径：新建 `开发管理/2v2范围技能协同走位与团队AI参数事实源及决策输入.txt` 与更新 `开发管理/当前任务队列.txt`；执行前须确认这些路径不与人工改动冲突。
- 范围：只对现有 BattleSim 的四单位唯一占格、距离／视线、位置控制、目标选择、环境查询与既有 2v2 fixture 进行只读定位。记录必须逐项给出实际文件、类型／成员、输入、输出及既有拒绝原因；将范围技能形状／距离、协同走位触发条件和团队 AI 优先级中已有事实、可复用但未参数化的事实、以及完全缺失而必须由负责人决定的参数严格分栏，并为每个决策项给出直接影响根。不得将推测参数、伤害数值或 AI 风格写成现行事实。
- 禁止：不修改 BattleSim、Unity、CSV、asset、场景、正式入口、伤害表、距离模型、目标选择、已有 fixture 或规则事实源；不运行全量矩阵、不以默认值、随机权重或兼容分支填补缺失参数。
- 验证：`pwsh -NoProfile -ExecutionPolicy Bypass -File tools/check-review-text.ps1 -Paths 开发管理/2v2范围技能协同走位与团队AI参数事实源及决策输入.txt,开发管理/当前任务队列.txt`，以及 `git diff --check`。
- 完成条件：决策输入可逐项回链到当前 BattleSim 文件；每个既有 2v2 语义均说明是否可直接复用；所有新增范围、协同与优先级参数要么有现行事实源，要么列为稳定决策项及影响根。完成不表示 N-GROUP-02 已实现、参数已获批准或 G2 对新规则仍然通过。
- 停止条件：定位发现多个现有所有者语义冲突、必须改变既有距离／视线／占格／目标选择语义才能记录，或任何结论需要未确认参数；停止规则实施，仅保留定位证据和待决策项。

### D-REALM-02A · 高阶内容现行引用分类与处理决策输入

- 来源：`开发管理/任务列表/数据链路任务.txt` 的 D-REALM-02、`开发管理/设计-当前状态.txt` 的玩家链与高阶内容边界，以及 `开发管理/设计-下一步建议.txt` 的数据语义入队条件。
- 当前状态：已完成；依赖：G3 已通过；无待决策。本卡只生成决策输入，不把处理选项当作已获批准的内容或数据变更。
- 主责：Codex / gpt-5.6-terra。
- 必读：`开发管理/设计-当前状态.txt`、`开发管理/任务列表/数据链路任务.txt`、`开发管理/设计-下一步建议.txt`、现行 `docs/基础设定/`、`docs/角色养成/`、`src/Assets/DataConfig/` 与 `src/Assets/Data/` 中命中的高阶内容来源；历史扫描材料只可作定位线索，不得作为现行事实。
- 预期修改路径：新建 `开发管理/高阶内容现行引用分类与决策输入.txt` 与更新 `开发管理/当前任务队列.txt`；执行前须确认这些路径不与人工改动冲突。
- 范围：只扫描现行 docs、CSV 和 asset 中与玩家链外高阶境界或高阶内容有关的有效引用，逐项记录字面量路径、稳定 ID（如有）、当前语义、现有 consumer／仅 metadata 状态与可选处理方向（保留、合并、删除或资料片隔离）。输出须把已验证的当前事实、待确认项和需负责人选择的影响严格分栏；保持当前“NPC、远期或资料片定位”口径。
- 禁止：不修改 docs、CSV、asset、导入器、运行时、数值、语言键或内容数量；不创作新高阶内容、不把历史扫描结论升级为事实、不按 `contentScope` 自动删除条目，也不替负责人选择最终处理方向。
- 验证：`rg -n --glob '*.txt' --glob '*.csv' --glob '*.asset' '(炼神|炼虚|化神|元婴|realm_lianshen|realm_lianxu)' docs src/Assets/DataConfig src/Assets/Data`，`pwsh -NoProfile -ExecutionPolicy Bypass -File tools/check-review-text.ps1 -Paths 开发管理/高阶内容现行引用分类与决策输入.txt,开发管理/当前任务队列.txt`，以及 `git diff --check`。
- 完成条件：决策输入逐项可回链到现行来源，明确区分活跃语义、仅 metadata、无 consumer 与无法判定项；每个候选项均有不扩大内容边界的处理选项和影响根，任何需要负责人选择的项都以稳定问题列出。完成不表示任何高阶内容已保留、合并、删除或隔离。
- 停止条件：某项没有现行来源、需要改写玩家境界链、需要新增内容／数值，或其处理会改变批准薄内容切片；记录缺口并请求负责人决定，不修改生产内容。
- 完成记录：已创建 `开发管理/高阶内容现行引用分类与决策输入.txt`，以 211 份 docs、4 份 CSV 与 8 份 asset 的现行扫描为基础，区分已验证事实、待确认项与 D-REALM-DEC-01～04 负责人选择；未改动生产内容，也未选择最终处理方向。

### N-SLOT-01 · 金丹槽位与丹枢加成数值方案

- 来源：`开发管理/任务列表/数值与战斗任务.txt` N-SLOT-01、2026-07-20 筑基／紫府／丹相集成规格。
- 当前状态：阻塞；依赖：N-GC-BASE-01 已完成，仍等待 N-FPD-MANSION-01 与 N-FPD-DANXIANG-01。
- 主责：Codex / ChatGPT5.5。
- 必读：`docs/superpowers/specs/2026-07-20-foundation-purple-mansion-danxiang-integration-design.md`、`docs/基础设定/角色数值设计.txt`、`docs/基础设定/境界特性.txt`、`docs/基础设定/元婴锚点与金丹位格设定.txt`、`docs/角色养成/术法槽位设计.txt` 与 `simulations/BattleSim/Program.cs`。
- 预期修改路径：`simulations/BattleSim/Program.cs`、`docs/角色养成/术法槽位设计.txt`、`docs/基础设定/角色数值设计.txt` 与本任务的验证记录；执行前须确认这些路径不与人工改动冲突。
- 范围：基于已通过的 G2 覆盖和新主／辅预算，提出并验证术法槽、普通神通槽与合法丹枢接口方案。紫府数量、府属、第四／第五府和稳定位格数量都不得自动生成普通槽位；任何槽位修正必须来自明示效果绑定。不得提前实施 N-SEAT-01 的占据结算、N-SUPPRESS-01 的冲突规则或伤害表调整。
- 验证：`dotnet build -c Release --no-restore simulations/BattleSim`、`dotnet run --no-build -c Release --project simulations/BattleSim`、`pwsh -NoProfile -ExecutionPolicy Bypass -File tools/check-review-text.ps1 -Paths 开发管理,docs,simulations`，以及 `git diff --check`。
- 完成条件：方案明确槽位与丹枢的输入、输出、适用边界、目标指标和数值阈值；覆盖 1～5 府与 1～3 稳定位格，不让第四、第五府失效或凭数量增槽，并保留可复现的 BattleSim 证据。
- 停止条件：N-FPD-MANSION-01 或 N-FPD-DANXIANG-01 尚未完成，或方案必须依赖府数自动加槽、额外位格／丹相或未确认府体数值。
