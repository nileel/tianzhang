# 当前任务队列（✅ 已审核）

> 内容冻结：G1 正式构建链、G2 BattleSim 数值可信度、G3 数据链路语义可信度全部通过且 TQ-071 完成前，禁止把新的剧情、角色、功法、术法、神通或世界设定扩写补入本队列。
> 本文件只放近期可执行、可验证、可提交的任务切片；复审和交接任务走 `开发管理/审核入口.txt`。
> 过程记录已归档至 `开发管理/任务归档/2026-07-12-管理入口瘦身前状态归档.txt`；当前状态以本文件为准。

## 队列表头

| ID | 优先级 | 主责 | 类型 | 状态 | 任务 |
|----|--------|------|------|------|------|
| TQ-057 | P0 | DeepSeek V4 Pro | G3 数据 | 待处理 | D-TRUST-02：清理现存数据矛盾 |
| TQ-059 | P0 | Codex / gpt-5.5 | G3 数据 | 阻塞（TQ-057） | D-TRUST-04：接通境界、元素和阵营限制 |
| TQ-067 | P1 | Codex / gpt-5.5 | 场景与 Unity | 待处理 | U-ARCH-03：建立项目派生 asmdef 边界 |
| N-SLOT-01 | P2 | Codex / gpt-5.5 | 数值与战斗 | 待处理 | 金丹槽位与丹枢加成数值方案 |

## 任务卡片

### TQ-057 · D-TRUST-02 清理现存数据矛盾

- 来源：可信度闸门规格 §6。
- 当前状态：待处理；依赖：TQ-056 已由 Codex 完成并验证。
- 主责：DeepSeek V4 Pro 执行；Codex / ChatGPT5.5 复审。
- 范围：对齐术法 docs/CSV/asset 数量，处理 `realm_lianshen` 缺失语言键与 `realm_lianxu` 删除后仍激活的数据，并吸收 TQ-040 的字段完整性复查；不得扩展内容或设定语义。
- 验证：`powershell -ExecutionPolicy Bypass -File tools/check-data-chain.ps1` 仅在剩余问题均有精确批准豁免或已清理时返回 0；`git diff --check`。
- 完成条件：TQ-056 所列历史数据矛盾已清理或被精确批准豁免，交接材料标明仍需 Codex / ChatGPT5.5 复审。

### TQ-059 · D-TRUST-04 接通境界、元素和阵营限制

- 来源：可信度闸门规格 §6。
- 当前状态：阻塞（TQ-057）；依赖：TQ-057。
- 主责：Codex / gpt-5.5。
- 范围：为 `realmReq`、`elementReq`、`affiliation` 分别选择并验证 runtime gate 或 metadata-only 模式，禁止字段继续伪装为有效限制。
- 验证：每个 runtime gate 具有允许与拒绝样例；运行 `powershell -ExecutionPolicy Bypass -File tools/check-data-chain.ps1` 与相关 Unity 回归。
- 完成条件：三个字段都有唯一、可观察的运行时或显式元数据口径，G3 才可进入出口检查。

### TQ-067 · U-ARCH-03 项目派生 asmdef 边界

- 来源：`开发管理/任务列表/场景与Unity任务.txt`、`UNITY_STRUCTURE.md`、可信度闸门规格 §7。
- 当前状态：待处理；依赖：TQ-065、TQ-066 均已归档。
- 主责：Codex / ChatGPT5.5。
- 必读：`UNITY_STRUCTURE.md`、`开发管理/运行时所有者记录-TQ-060.txt`、现有 `.asmdef`、相关运行时引用及 EditMode 测试。
- 范围：建立项目派生的 asmdef 边界；不得存在 sibling feature 直接依赖或实现层向上反向引用，且必须保持 Unity 序列化字段、资产 GUID、场景和预制体引用兼容。
- 验证：运行相关 .NET 编译、Unity EditMode 测试、`pwsh -NoProfile -ExecutionPolicy Bypass -File tools/check-review-text.ps1 -Paths 开发管理,src/Assets/Scripts,src/Assets/Tests` 与 `git diff --check`。
- 完成条件：项目派生 asmdef 的编译和 Unity 序列化兼容验证通过，并有可复现的边界扫描与回归证据。

### N-SLOT-01 · 金丹槽位与丹枢加成数值方案

- 来源：`开发管理/任务列表/数值与战斗任务.txt` N-SLOT-01。
- 当前状态：待处理；依赖：N-GC-BASE-01 已完成。
- 主责：Codex / ChatGPT5.5。
- 必读：`docs/基础设定/角色数值设计.txt`、`docs/基础设定/境界特性.txt`、`docs/角色养成/术法槽位设计.txt` 与 `simulations/BattleSim/Program.cs`。
- 范围：基于已通过的 G2 覆盖和当前 BattleSim 基线，提出并验证金丹槽位与丹枢加成的数值方案；不得把未验证的通用槽位数、紫府神通数量阈值或席位竞争公式写入运行时，也不得提前实施 N-SEAT-01 的占据结算、N-SUPPRESS-01 的触发概率或伤害表调整。
- 验证：`dotnet build -c Release --no-restore simulations/BattleSim`、`dotnet run --no-build -c Release --project simulations/BattleSim`、`pwsh -NoProfile -ExecutionPolicy Bypass -File tools/check-review-text.ps1 -Paths 开发管理,docs,simulations`，以及 `git diff --check`。
- 完成条件：方案明确槽位与丹枢的输入、输出、适用边界、目标指标和需要负责人选择的数值阈值；不把候选参数误报为已锁定设定，并保留可复现的 BattleSim 验证证据。
