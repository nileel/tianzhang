# 当前任务队列（✅ 已审核）

> 内容冻结：G1 正式构建链、G2 BattleSim 数值可信度、G3 数据链路语义可信度全部通过且 TQ-071 完成前，禁止把新的剧情、角色、功法、术法、神通或世界设定扩写补入本队列。
> 本文件只放近期可执行、可验证、可提交的任务切片；复审和交接任务走 `开发管理/审核入口.txt`。
> 过程记录已归档至 `开发管理/任务归档/2026-07-12-管理入口瘦身前状态归档.txt`；当前状态以本文件为准。

## 队列表头

| ID | 优先级 | 主责 | 类型 | 状态 | 任务 |
|----|--------|------|------|------|------|
| TQ-057 | P0 | Codex / gpt-5.5 | G3 数据 | 已完成（2026-07-19） | D-TRUST-02：清理现存数据矛盾 |
| TQ-059 | P0 | Codex / gpt-5.5 | G3 数据 | 阻塞（TQ-057） | D-TRUST-04：接通境界、元素和阵营限制 |
| TQ-070 | P1 | Codex / gpt-5.5 | 场景与 Unity | 待处理 | S-SAVE-01：存档版本与迁移基础 |
| N-SLOT-01 | P2 | Codex / gpt-5.5 | 数值与战斗 | 阻塞（N-FPD-DANXIANG-01、N-FPD-MANSION-01） | 金丹槽位与丹枢加成数值方案 |

## 任务卡片

### TQ-057 · D-TRUST-02 清理现存数据矛盾

- 优先级：P0；当前状态：待处理；依赖：TQ-056 已由 Codex 完成并验证。
- 主责：Codex / ChatGPT5.5。
- 范围：对齐术法 docs/CSV/asset 数量，处理 `realm_lianshen` 缺失语言键与 `realm_lianxu` 删除后仍激活的数据，并吸收 TQ-040 的字段完整性复查；不得扩展内容或设定语义。
- 验证：`pwsh -NoProfile -ExecutionPolicy Bypass -File tools/check-data-chain.ps1` 仅在剩余问题均有精确批准豁免或已清理时返回 0；`git diff --check`。
- 完成条件：TQ-056 所列历史数据矛盾已清理或被精确批准豁免，并有 Codex / ChatGPT5.5 的直接验证证据。
- 正式任务卡：`开发管理/任务列表/数据链路任务.txt` 的“TQ-057 最终批准口径”。

### TQ-059 · D-TRUST-04 接通境界、元素和阵营限制

- 来源：可信度闸门规格 §6。
- 当前状态：阻塞（TQ-057）；依赖：TQ-057。
- 主责：Codex / gpt-5.5。
- 范围：为 `realmReq`、`elementReq`、`affiliation` 分别选择并验证 runtime gate 或 metadata-only 模式，禁止字段继续伪装为有效限制。
- 验证：每个 runtime gate 具有允许与拒绝样例；运行 `pwsh -NoProfile -ExecutionPolicy Bypass -File tools/check-data-chain.ps1` 与相关 Unity 回归。
- 完成条件：三个字段都有唯一、可观察的运行时或显式元数据口径，G3 才可进入出口检查。

### TQ-070 · S-SAVE-01 存档版本与迁移基础

- 来源：`开发管理/任务列表/场景与Unity任务.txt`、可信度闸门规格 §7、TQ-068 与 TQ-069 完成归档。
- 当前状态：待处理；依赖：TQ-068、TQ-069 已归档。
- 主责：Codex / ChatGPT5.5。
- 必读：`UNITY_STRUCTURE.md`、`开发管理/开发-技术经验.txt`、`开发管理/任务归档/2026-07-13-TQ-068-最小会话状态模型归档.txt`、`开发管理/任务归档/2026-07-21-TQ-069-最小任务背包NPC状态快照归档.txt`、`src/Assets/Scripts/Game/GameSession.cs`、`src/Assets/Scripts/Game/SessionStateSnapshots.cs` 与 `src/Assets/Tests/EditMode/SessionStateSnapshotTests.cs`。
- 预期修改路径：`src/Assets/Scripts/Game/GameSession.cs`、`src/Assets/Scripts/Game/SessionStateSnapshots.cs`、`src/Assets/Tests/EditMode/SessionStateSnapshotTests.cs`、`开发管理/设计-当前状态.txt`、`开发管理/任务列表/场景与Unity任务.txt`、`开发管理/当前任务队列.txt`。
- 范围：在现有 `GameSession` 与三类 store/snapshot 边界内增加单一 schema 版本、可序列化快照、版本校验、支持旧版迁移和原子恢复／重置入口；新档、当前版往返、受支持旧档、非法档和重置必须具有确定行为。不得另建平行会话或存档服务，不扩展到存档槽位 UI、自动存档、云同步、加密或筑基／紫府持久化。
- 验证：`dotnet build src/TianZhang.Runtime.csproj --no-restore`、`dotnet build src/TianZhang.EditModeTests.csproj --no-restore`、`pwsh -NoProfile -ExecutionPolicy Bypass -File tools/run-unity-editmode-tests.ps1 -ProjectPath D:\天章游戏开发\src`、`pwsh -NoProfile -ExecutionPolicy Bypass -File tools/check-review-text.ps1 -Paths 开发管理,src/Assets/Scripts,src/Assets/Tests`，以及 `git diff --check`。
- 完成条件：新档与重置回到同一初始状态；当前版往返保留世界、场景返回上下文及任务／背包／NPC 的稳定 ID 和七个独立步骤；受支持旧档迁移结果固定；空身份、负数量、重复 ID、未知或未来版本在修改会话前失败关闭，并有可重复回归证据。
- 停止条件：现有所有者不能提供原子恢复且必须新建平行服务，或角色档案缺少稳定身份导致本切片必须扩展为完整角色序列化；此时先走负责人决策，不叠加兼容分支。

### N-SLOT-01 · 金丹槽位与丹枢加成数值方案

- 来源：`开发管理/任务列表/数值与战斗任务.txt` N-SLOT-01、2026-07-20 筑基／紫府／丹相集成规格。
- 当前状态：阻塞；依赖：N-GC-BASE-01 已完成，仍等待 N-FPD-MANSION-01 与 N-FPD-DANXIANG-01。
- 主责：Codex / ChatGPT5.5。
- 必读：`docs/superpowers/specs/2026-07-20-foundation-purple-mansion-danxiang-integration-design.md`、`docs/基础设定/角色数值设计.txt`、`docs/基础设定/境界特性.txt`、`docs/基础设定/元婴锚点与金丹位格设定.txt`、`docs/角色养成/术法槽位设计.txt` 与 `simulations/BattleSim/Program.cs`。
- 预期修改路径：`simulations/BattleSim/Program.cs`、`docs/角色养成/术法槽位设计.txt`、`docs/基础设定/角色数值设计.txt` 与本任务的验证记录；执行前须确认这些路径不与人工改动冲突。
- 范围：基于已通过的 G2 覆盖和新主／辅预算，提出并验证术法槽、普通神通槽与合法丹枢接口方案。紫府数量、府属、第四／第五府和稳定位格数量都不得自动生成普通槽位；任何槽位修正必须来自明示效果绑定。不得提前实施 N-SEAT-01 的占据结算、N-SUPPRESS-01 的冲突规则或伤害表调整。
- 验证：`dotnet build -c Release --no-restore simulations/BattleSim`、`dotnet run --no-build -c Release --project simulations/BattleSim`、`pwsh -NoProfile -ExecutionPolicy Bypass -File tools/check-review-text.ps1 -Paths 开发管理,docs,simulations`，以及 `git diff --check`。
- 完成条件：方案明确槽位与丹枢的输入、输出、适用边界、目标指标和数值阈值；覆盖 1～5 府与 1～3 稳定位格，不让第四、第五府失效或凭数量增槽，并保留可复现的 BattleSim 证据。
- 停止条件：N-FPD-MANSION-01 或 N-FPD-DANXIANG-01 尚未完成，或方案必须依赖府数自动加槽、额外位格／丹相或未确认府体数值。
