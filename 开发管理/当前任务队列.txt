# 当前任务队列（✅ 已审核）

> 内容冻结：G1 正式构建链、G2 BattleSim 数值可信度、G3 数据链路语义可信度全部通过且 TQ-071 完成前，禁止把新的剧情、角色、功法、术法、神通或世界设定扩写补入本队列。
> 本文件只放近期可执行、可验证、可提交的任务切片；复审和交接任务走 `开发管理/审核入口.txt`。
> 过程记录已归档至 `开发管理/任务归档/2026-07-12-管理入口瘦身前状态归档.txt`；当前状态以本文件为准。

## 队列表头

| ID | 优先级 | 主责 | 类型 | 状态 | 任务 |
|----|--------|------|------|------|------|
| TQ-055 | P0 | Codex / gpt-5.5 | G2 数值 | 待处理 | N-TRUST-04：G2 覆盖与极端结果重验 |
| TQ-057 | P0 | DeepSeek V4 Pro | G3 数据 | 待处理 | D-TRUST-02：清理现存数据矛盾 |
| TQ-059 | P0 | Codex / gpt-5.5 | G3 数据 | 阻塞（TQ-057） | D-TRUST-04：接通境界、元素和阵营限制 |
| TQ-064 | P1 | Codex / gpt-5.5 | 架构治理 | 待处理 | M-ARCH-01：建立 `UNITY_STRUCTURE.md` 与 runtime/content/assemblies 聚焦图 |
| TQ-068 | P1 | Codex / gpt-5.5 | 状态 | 待处理 | S-STATE-01：最小 GameSession、世界时间和起点状态模型 |

## 任务卡片

### TQ-055 · N-TRUST-04 G2 覆盖与极端结果重验

- 来源：`开发管理/任务列表/数值与战斗任务.txt` G2 任务执行边界。
- 当前状态：待处理；依赖：TQ-052、TQ-053、TQ-054、TQ-044 均已完成并验证。
- 主责：Codex / ChatGPT5.5。
- 范围：重跑修炼与 CTB 矩阵，产出可复现输出、境界样本覆盖、极端结果分类和 G2 结论；不得在诊断过程中直接调参。
- 验证：每个 Build 至少 200 个确定性修炼种子；每个目标矩阵格至少覆盖 20 对不同角色配对、总计至少 2000 场战斗，并输出胜率与 95% Wilson 置信区间；运行 `dotnet build -c Release --no-restore simulations/BattleSim`、`dotnet run --no-build -c Release --project simulations/BattleSim` 与 `git diff --check`。
- 完成条件：任何覆盖不足必须标为 `INSUFFICIENT` 并继续阻塞；0%/100% 结果只有满足覆盖门槛并报告区间后才能判为设计极端，缺陷须修复并重跑；满足全部 G2 出口条件后才可标记 G2 通过。

### TQ-057 · D-TRUST-02 清理现存数据矛盾

- 来源：可信度闸门规格 §6。
- 当前状态：待处理；依赖：TQ-056 已由 Codex 完成并验证。
- 主责：DeepSeek V4 Pro 执行；Codex / ChatGPT5.5 复审。
- 范围：对齐术法 docs/CSV/asset 数量，处理 `realm_lianshen` 缺失语言键与 `realm_lianxu` 删除后仍激活的数据，并吸收 TQ-040 的字段完整性复查；不得扩展内容或设定语义。
- 验证：`powershell -ExecutionPolicy Bypass -File tools/check-data-chain.ps1` 仅在剩余问题均有精确批准豁免或已清理时返回 0；`git diff --check`。
- 完成条件：TQ-056 所列历史数据矛盾已清理或被精确批准豁免，交接材料标明仍需 Codex / ChatGPT5.5 复审。

### TQ-059 · D-TRUST-04 接通境界、元素和阵营限制

- 来源：可信度闸门规格 §6。
- 当前状态：阻塞（TQ-057）；依赖：TQ-057。
- 主责：Codex / gpt-5.5。
- 范围：为 `realmReq`、`elementReq`、`affiliation` 分别选择并验证 runtime gate 或 metadata-only 模式，禁止字段继续伪装为有效限制。
- 验证：每个 runtime gate 具有允许与拒绝样例；运行 `powershell -ExecutionPolicy Bypass -File tools/check-data-chain.ps1` 与相关 Unity 回归。
- 完成条件：三个字段都有唯一、可观察的运行时或显式元数据口径，G3 才可进入出口检查。

### TQ-064 · M-ARCH-01 建立 Unity 结构事实图

- 来源：`开发管理/任务列表/场景与Unity任务.txt`、可信度闸门规格 §7。
- 当前状态：待处理；依赖：G1 已通过。
- 主责：Codex / ChatGPT5.5。
- 必读：`开发管理/运行时所有者记录-TQ-060.txt`、`src/Assets/`、`src/ProjectSettings/`、现有 asmdef 与验证入口的实时扫描结果。
- 范围：从当前工作树扫描生成 `UNITY_STRUCTURE.md`，覆盖文件路径、场景、运行时所有者、runtime/content/assemblies 边界、验证入口与 open gaps；不得以旧文档推断代替扫描证据。
- 验证：核对文档中的每项关键路径均能在当前工作树解析；`powershell -ExecutionPolicy Bypass -File tools/check-review-text.ps1 -Paths 开发管理`；`git diff --check`。
- 完成条件：结构图可作为后续 TQ-065～TQ-067 的事实入口，且六类必填信息均有实时扫描依据。

### TQ-068 · S-STATE-01 最小会话状态模型

- 来源：`开发管理/任务列表/场景与Unity任务.txt`、可信度闸门规格 §7。
- 当前状态：待处理；依赖：G1 已通过。
- 主责：Codex / ChatGPT5.5。
- 必读：`开发管理/运行时所有者记录-TQ-060.txt`、`src/Assets/Scripts/GameSession.cs`、新游戏、场景切换与战斗返回相关脚本及现有 EditMode 测试。
- 范围：明确并实现最小 `GameSession`、世界时间和起点状态的唯一所有者；新游戏初始化、场景切换与战斗返回后三者必须一致，不扩展任务、背包、NPC 快照或存档迁移范围。
- 验证：补齐允许/回归样例；运行相关 .NET 编译和 Unity EditMode 测试；`git diff --check`。
- 完成条件：三类状态在三条生命周期路径均有可观察的一致性回归证据，并为 TQ-069 的状态步骤模型保留明确边界。
