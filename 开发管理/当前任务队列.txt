# 当前任务队列（✅ 已审核）

> 内容冻结：G1 正式构建链、G2 BattleSim 数值可信度、G3 数据链路语义可信度全部通过且 TQ-071 完成前，禁止把新的剧情、角色、功法、术法、神通或世界设定扩写补入本队列。
> 本文件只放近期可执行、可验证、可提交的任务切片；复审和交接任务走 `开发管理/审核入口.txt`。
> 过程记录已归档至 `开发管理/任务归档/2026-07-12-管理入口瘦身前状态归档.txt`；当前状态以本文件为准。

## 队列表头

| ID | 优先级 | 主责 | 类型 | 状态 | 任务 |
|----|--------|------|------|------|------|
| TQ-057 | P0 | DeepSeek V4 Pro | G3 数据 | 待处理 | D-TRUST-02：清理现存数据矛盾 |
| TQ-059 | P0 | Codex / gpt-5.5 | G3 数据 | 阻塞（TQ-057） | D-TRUST-04：接通境界、元素和阵营限制 |
| TQ-065 | P1 | Codex / gpt-5.5 | Unity 架构 | 待处理 | U-ARCH-01：拆分 ExplorationController / SceneBuilder 真实职责边 |
| TQ-066 | P1 | Codex / gpt-5.5 | Unity 架构 | 待处理 | U-ARCH-02：拆分 BattleUIManager / Character 的 UI 与领域职责边 |
| TQ-068 | P1 | Codex / gpt-5.5 | 状态 | 待处理 | S-STATE-01：最小 GameSession、世界时间和起点状态模型 |

## 任务卡片

### TQ-057 · D-TRUST-02 清理现存数据矛盾

- 来源：可信度闸门规格 §6。
- 当前状态：待处理；依赖：TQ-056 已由 Codex 完成并验证。
- 主责：DeepSeek V4 Pro 执行；Codex / ChatGPT5.5 复审。
- 范围：对齐术法 docs/CSV/asset 数量，处理 `realm_lianshen` 缺失语言键与 `realm_lianxu` 删除后仍激活的数据，并吸收 TQ-040 的字段完整性复查；不得扩展内容或设定语义。
- 验证：`powershell -ExecutionPolicy Bypass -File tools/check-data-chain.ps1` 仅在剩余问题均有精确批准豁免或已清理时返回 0；`git diff --check`。
- 完成条件：TQ-056 所列历史数据矛盾已清理或被精确批准豁免，交接材料标明仍需 Codex / ChatGPT5.5 复审。

### TQ-059 · D-TRUST-04 接通境界、元素和阵营限制

- 来源：可信度闸门规格 §6。
- 当前状态：阻塞（TQ-057）；依赖：TQ-057。
- 主责：Codex / gpt-5.5。
- 范围：为 `realmReq`、`elementReq`、`affiliation` 分别选择并验证 runtime gate 或 metadata-only 模式，禁止字段继续伪装为有效限制。
- 验证：每个 runtime gate 具有允许与拒绝样例；运行 `powershell -ExecutionPolicy Bypass -File tools/check-data-chain.ps1` 与相关 Unity 回归。
- 完成条件：三个字段都有唯一、可观察的运行时或显式元数据口径，G3 才可进入出口检查。

### TQ-065 · U-ARCH-01 拆分 ExplorationController / SceneBuilder 真实职责边

- 来源：`开发管理/任务列表/场景与Unity任务.txt` U-ARCH-01。
- 当前状态：待处理；依赖：无已声明依赖。
- 主责：Codex / ChatGPT5.5。
- 必读：`src/Assets/Scripts/Map/ExplorationController.cs`、`src/Assets/Scripts/Editor/SceneBuilder.cs`，以及相关场景、预制体引用和 EditMode 测试。
- 范围：从 `ExplorationController` / `SceneBuilder` 拆分至少一条真实职责与依赖边；不得增加或继续膨胀 Hub 类，必须保持 Unity 序列化字段、资产 GUID、场景和预制体引用兼容。
- 验证：运行相关 .NET 编译、可用的 Unity EditMode 目标测试或 `SceneBuilder.ValidateSceneArchitectureShellsForBatchMode`、`pwsh -NoProfile -ExecutionPolicy Bypass -File tools/check-review-text.ps1 -Paths 开发管理,src/Assets/Scripts,src/Assets/Tests`，以及 `git diff --check`。
- 完成条件：至少一条真实职责与依赖边已拆分，并有保持序列化与引用兼容的可重复验证证据。

### TQ-066 · U-ARCH-02 拆分 BattleUIManager / Character 的 UI 与领域职责边

- 来源：`开发管理/任务列表/场景与Unity任务.txt` U-ARCH-02、`开发管理/场景架构Unity现状清单.txt` 的 BattleUIManager 风险记录。
- 当前状态：待处理；依赖：无已声明依赖。
- 主责：Codex / ChatGPT5.5。
- 必读：`src/Assets/Scripts/Game/BattleUIManager.cs`、涉及 UI 调用或显示状态的 `Character` 脚本、`src/Assets/Scripts/Map/ExplorationController.cs`、`src/Assets/Scripts/Editor/SceneBuilder.cs` 与相关 EditMode 测试。
- 范围：从 `BattleUIManager` / `Character` 分离至少一条真实 UI 与领域职责边；保持动作入口通过既有命令处理边界，不得让 UI 直接承担战斗领域结算，不得破坏场景、预制体、序列化字段或资产 GUID 引用。
- 验证：运行相关 .NET 编译、可用的 Unity EditMode 目标测试，`pwsh -NoProfile -ExecutionPolicy Bypass -File tools/check-review-text.ps1 -Paths 开发管理,src/Assets/Scripts,src/Assets/Tests`，以及 `git diff --check`。
- 完成条件：至少一条 UI / 领域职责与依赖边已拆分；动作入口和既有场景引用保持可重复验证，且未扩大战斗数值或规则范围。

### TQ-068 · S-STATE-01 最小会话状态模型

- 来源：`开发管理/任务列表/场景与Unity任务.txt`、可信度闸门规格 §7。
- 当前状态：待处理；依赖：G1 已通过。
- 主责：Codex / ChatGPT5.5。
- 必读：`开发管理/运行时所有者记录-TQ-060.txt`、`src/Assets/Scripts/GameSession.cs`、新游戏、场景切换与战斗返回相关脚本及现有 EditMode 测试。
- 范围：明确并实现最小 `GameSession`、世界时间和起点状态的唯一所有者；新游戏初始化、场景切换与战斗返回后三者必须一致，不扩展任务、背包、NPC 快照或存档迁移范围。
- 验证：补齐允许/回归样例；运行相关 .NET 编译和 Unity EditMode 测试；`git diff --check`。
- 完成条件：三类状态在三条生命周期路径均有可观察的一致性回归证据，并为 TQ-069 的状态步骤模型保留明确边界。
