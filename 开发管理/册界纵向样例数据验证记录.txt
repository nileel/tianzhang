# 册界纵向样例数据验证记录

状态：✅ 已验证（D-TZ-CHARTER-SAMPLE-01，2026-07-31）

## 有效样例

- `CharterRuleDefinitions.csv` 现有唯一生产行 `charter_entry_suifu_diji`；十八字段均为稳定引用，范围固定为 `CONNECTED_NODES`／`AREA`，覆盖只枚举旧水驿的已接通册界、水工与河道／湿地节点。
- `DataConfigImporter.CreateProductionCharterRuleReferenceCatalog` 只声明《开阖九章》的旧水驿至地下泵房门禁、太玄界印管理场景、流域组织授权、已登记现实供给、`conflict_charter_water_basin` 的单次跨阶资格和 `env_guanzhong_wild` 输出引用。
- 导入器已生成并 EditMode 校验 `src/Assets/Data/CharterRuleDefinitions/CharterRuleDefinition_charter_entry_suifu_diji.asset`；环境档案仍只作为事件输出引用，未接管地区长期状态。

## 拒绝断言

- 无通行许可、仅有通行但无界印管理、无合法授权：`CHARTER_UNKNOWN_AUTHORITY_REFERENCE`。
- 断开节点：`CHARTER_UNKNOWN_NODE_REFERENCE`；现实供给未登记、正提交或负提交缺失：`CHARTER_ATOMIC_COMMIT_INCOMPLETE`。
- 覆盖越出同一流域已接通集合，以及把元婴受锚标识当作静态覆盖：`CHARTER_COVERAGE_OUT_OF_BOUNDARY`。
- 未声明水府跨阶挑战档案：`CHARTER_UNKNOWN_CONFLICT_REFERENCE`。
- 以上均在 asset 写入前拒绝；元婴受锚的实际调用判定、金丹冲突裁决和原子事务仍属于后续 `U-TZ-CHARTER-MODEL-01`，本任务没有实现它们。

## 验证结果

- `dotnet build src/TianZhang.EditModeTests.csproj`：通过，0 警告、0 错误。
- `pwsh -NoProfile -ExecutionPolicy Bypass -File tools/tests/check-data-chain-tests.ps1`：通过，11 项 fixture 检查通过。
- `pwsh -NoProfile -ExecutionPolicy Bypass -File tools/check-data-chain.ps1`：通过；G3 计数为功法 29/29、术法 83/83、神通 31/31。
- `pwsh -NoProfile -ExecutionPolicy Bypass -File tools/run-unity-editmode-tests.ps1`：册界新增生产导入和拒绝断言均通过；全套 272 项中 271 通过。唯一既有失败为 `DataConfigImporterContentScopeTests.LianShenReservedAbilitiesRemainExcludedAtPlayerLoad`，它仍期待旧日志 “Excluded non-player spell”，现有未改动加载器输出 “Excluded unresolved art profile”。
- 未修改数值、倍率、概率或战斗表现，故未运行 BattleSim。

## U-TZ-CHARTER-MODEL-01 纯规则模型运行记录（2026-08-03）

> ⚠️ 已修改/未审核；修改方：DeepSeek V4 Flash；变更范围：`CharterRuleRuntime.cs`（＋.meta）、`CharterRuntimeStateData.cs`、`CharterRuleRuntimeTests.cs`（＋.meta）、本记录。

在既有水府地纪样例上新增 `TianZhang.World.CharterRuleRuntime` 纯规则入口，不持有场景、Unity 对象、单例、存档或 BattleSim 账本；输入仅为已校验定义、参考目录、动态状态、稳定 ID 和显式调用请求。按固定事务顺序校验：天章通行（遗物识别＋通行者／门禁目标）→ 界印管理／受益（界印识别＋管理者／受益者）→ 授权版本组合（`requiredAuthority` 全部版本被动态状态识别）→ 节点连通（锚点参与且涉及节点已登记连通）→ 传播范围（覆盖在流域边界内且未越出当前已接通覆盖）→ 现实供给（已登记并覆盖两个提交声明供给）→ 完整正负提交（与定义一致且均声明供给）→ 冲突扫描（只构造完整 v1 `RuleConflictInstance` 消费 shared 决定；元婴受锚只返回受锚结果）→ 原子构造新状态并只输出定义已声明的 `environmentProfile` 事件引用。

实际命令与结果：

- `pwsh -NoProfile -ExecutionPolicy Bypass -File tools/run-unity-editmode-tests.ps1`：全套 318 项中 316 通过、2 项既有无关基线失败（逐项记录见下）；册界直接用例全部通过：`CharterRuleRuntimeTests` 8/8、`CharterRuleDataTests` 26/26、`CharterConflictRulesTests` 4/5。
  - 既有失败 1：`DataConfigImporterContentScopeTests.LianShenReservedAbilitiesRemainExcludedAtPlayerLoad`（与 2026-07-31 记录一致的已知基线失败：仍期待旧日志 "Excluded non-player spell"，现有未改动加载器输出 "Excluded unresolved art profile"）。
  - 既有失败 2：`CharterConflictRulesTests.AuthorizedGrantBindsEveryConflictIdentityField`（`CharterConflictRules.cs`／`CharterConflictRulesTests.cs` 均来自 D-TZ-CHARTER-CONFLICT-02 提交 e1ffda9，本任务未改动；断言期望 `PULSE_ADVANTAGE`，shared 实现按既有优先序对等储备返回 `PULSE_NEUTRAL`）。
- `dotnet build -c Release simulations/BattleSim`（worktree 无 obj，先隐式 restore）：通过，0 警告、0 错误。
- `dotnet run --no-build -c Release --project simulations/BattleSim -- --self-test golden-core-conflict-n-jd-rule-01b`：PASS。
- `pwsh -NoProfile -ExecutionPolicy Bypass -File tools/check-review-text.ps1 -Paths 开发管理,docs,simulations,src/Assets/Scripts,src/Assets/Tests`：OK（664 个文件）。
- `pwsh -NoProfile -ExecutionPolicy Bypass -File tools/check-task-cards.ps1 -OutputJson`：status ok、readyCount 1。
- `pwsh -NoProfile -ExecutionPolicy Bypass -File tools/check-pending-whitespace.ps1`：OK（6 个文件）。
- Unity 验证完成后工作树仅含任务卡授权路径；两个新增 `.meta`（`CharterRuleRuntime.cs.meta`、`CharterRuleRuntimeTests.cs.meta`）随同一候选提交受 Git 跟踪，无验证生成的未跟踪现场。

水府地纪纵向样例的可观察边界（`CharterRuleRuntimeTests` 直接用例）：

- 通行：天章遗物识别为 `recognized` 且通行者、门禁目标明确才通过；未识别 → `charter_passage_denied`。
- 通行不等于管理：已有通行但界印未识别 → `charter_seal_management_denied`，不替换状态。
- 管理不等于改规则：仅界印管理版本被识别、缺少流域组织授权 → `charter_authorization_version_denied`。
- 节点断开：任一涉及节点 `disconnected` 或未登记 → `charter_node_disconnected`。
- 覆盖越界：覆盖越出流域边界或未接通集合 → `charter_coverage_out_of_boundary`。
- 原子拒绝：负提交与定义不一致 → `charter_atomic_commit_incomplete`；供给未覆盖两个提交或未登记 → `charter_reality_supply_unavailable`。
- 事件唯一记录：成功只输出定义已声明的 `event_suifu_water_redistribution`／`event_suifu_downstream_supply_delay` 的 `env_guanzhong_wild` 引用，逐条唯一，状态不含环境档案。
- 金丹冲突：同变量介入构造完整 v1 实例消费 shared 决定；无 archive → `JD_CHALLENGE_ARCHIVE_UNAVAILABLE`、未声明 grant → `charter_unknown_conflict_grant`、撤销 grant → `JD_CHALLENGE_REVOKED`、合法授权 → 携带 `LeftWins` 决定原子应用，无第二套 resolver。
- 元婴受锚：命中受锚只返回 `TZ_CHARTER_CONFLICT_YUANYING_ANCHORED`，不降格金丹、不开支、不提交覆盖。

## Codex 独立复审结果（2026-08-03）

结论：不通过，原“8/8 直接用例通过”不足以证明任务卡的失败关闭与一次性原子边界。

- 临时反例 `JindanInterventionWithoutCrossTierRequestCannotCommitRuleState` 证明：`crossTierChallengeRequest=null` 时仍可返回成功并构造 `NextState`，版本化挑战资格可被绕过。
- 临时反例 `AllocatedRealitySupplyCannotBeConsumedAgain` 证明：首次成功后同一供给同时保留 `registered` 与新增 `allocated`，第二次调用仍成功，现实供给、提交与占用可重复结算。
- 代码检查还确认 `RightWins`／`Neutral` 未阻止规则提交，且请求没有明确哪一候选代表本次册界调用。
- 权威 Unity 入口执行 320 项：316 通过、4 失败；新增两项失败为本次复审问题，另两项为原记录既有基线。临时反例已撤回，返工范围与后续复核要求见 `开发管理/未通过审核清单.txt`。

## U-TZ-CHARTER-MODEL-01 复审返工记录（2026-08-03）

> ⚠️ 已修改/未审核；修改方：DeepSeek V4 Flash；变更范围：`CharterRuleRuntime.cs`、`CharterRuleRuntimeTests.cs`、本记录。

按 `开发管理/未通过审核清单.txt` 的最窄返工，在同一卡只修三项缺口，不复制 resolver、不改 shared 冲突排序、不扩展存档／场景／UI、不新增账本所有者：

- 授权失败关闭：同变量金丹介入现在要求版本化跨阶请求、冲突目录声明与 archive 资格三项同时成立。`crossTierChallengeRequest` 为空时直接稳定拒绝 `charter_cross_tier_authorization_denied`，不再进入 shared 决定；目录声明失败仍为 `charter_unknown_conflict_grant`；archive 资格失败仍原样消费 shared 理由。
- shared 决定到本次调用结果的明确映射：请求新增 `charterCandidateId` 声明哪一候选代表本次册界调用（必须等于左或右候选 ID，否则请求无效）。只有 `WinnerCandidateId` 正是该候选时规则才提交状态与事件；败方或中立（无赢家）一律返回 `charter_conflict_not_won`，不替换状态、不输出事件。
- 供给唯一状态结转：成功事务按稳定 ID 用本轮 `allocated` 记录替换旧 `registered` 记录，同一现实供给、正负提交和节点占用只能结算一次，不再残留可复用的旧登记记录。

实际命令与结果：

- `pwsh -NoProfile -ExecutionPolicy Bypass -File tools/run-unity-editmode-tests.ps1`：全套 322 项中 320 通过、2 项既有无关基线失败（与首次运行记录完全一致，逐项记录见下）；册界直接用例全部通过：`CharterRuleRuntimeTests` 12/12（含 4 项新增返工用例：`JindanInterventionWithoutCrossTierRequestCannotCommitRuleState`、`CharterSideThatLosesTheConflictDoesNotCommitRuleStateOrEvents`、`NeutralConflictDecisionDoesNotCommitRuleStateOrEvents`、`AllocatedRealitySupplyCannotBeConsumedAgain`）、`CharterRuleDataTests` 26/26、`CharterConflictRulesTests` 4/5。
  - 既有失败 1：`DataConfigImporterContentScopeTests.LianShenReservedAbilitiesRemainExcludedAtPlayerLoad`（与 2026-07-31 记录一致的已知基线失败：仍期待旧日志 "Excluded non-player spell"，现有未改动加载器输出 "Excluded unresolved art profile"）。
  - 既有失败 2：`CharterConflictRulesTests.AuthorizedGrantBindsEveryConflictIdentityField`（`CharterConflictRules.cs`／`CharterConflictRulesTests.cs` 均来自 D-TZ-CHARTER-CONFLICT-02 提交 e1ffda9，本任务未改动；断言期望 `PULSE_ADVANTAGE`，shared 实现按既有优先序对等储备返回 `PULSE_NEUTRAL`）。
- `dotnet build -c Release simulations/BattleSim`（worktree 无 obj，先隐式 restore）：通过，0 警告、0 错误。
- `dotnet run --no-build -c Release --project simulations/BattleSim -- --self-test golden-core-conflict-n-jd-rule-01b`：PASS（shared 冲突语义未分叉）。
- `pwsh -NoProfile -ExecutionPolicy Bypass -File tools/check-review-text.ps1 -Paths 开发管理,docs,simulations,src/Assets/Scripts,src/Assets/Tests`：OK（665 个文件）。
- `pwsh -NoProfile -ExecutionPolicy Bypass -File tools/check-pending-whitespace.ps1`：OK（3 个文件）。
- Unity 验证完成后工作树仅含本轮修改文件，两个既有 `.meta`（`CharterRuleRuntime.cs.meta`、`CharterRuleRuntimeTests.cs.meta`）仍随同一候选提交受 Git 跟踪，无验证生成的未跟踪现场。

返工后水府地纪纵向样例的可观察边界增量：

- 缺版本化跨阶请求 → `charter_cross_tier_authorization_denied`，即使 archive 可解析也不进入 shared 决定、不替换状态。
- 请求未锁定本册界调用候选 → `charter_invocation_request_invalid`；声明败方候选 → `charter_conflict_not_won`；同优先级同脉冲中立 → `charter_conflict_not_won`，均不提交规则状态或事件。
- 首次成功把三个已登记供给唯一结转为本轮 `allocated`；从结转后状态再次调用同一供给 → `charter_reality_supply_unavailable`，不替换状态。

## Codex 返工复审结果（2026-08-03）

结论：部分通过，原三项返工主体已生效，但册界候选侧仍未被稳定 ID 唯一锁定，任务继续阻塞等待最窄返工。

- 静态核对确认：缺版本化请求、未声明 grant 与 archive 资格失败均失败关闭；供给唯一结转后不可重复消费；赢家不等于 `charterCandidateId` 或中立无赢家时不构造 `NextState`、不输出事件。
- `IsCharterCandidateDeclared` 只校验 `charterCandidateId` 等于左或右候选 ID。若左右候选使用同一 `CandidateId`，该字段无法区分册界侧；shared 决定返回任一侧获胜时，`WinnerCandidateId` 都与它相等，当前实现会成功提交规则状态和事件。
- 临时反例 `DuplicateCandidateIdsCannotDeclareWhichSideRepresentsTheCharterInvocation` 以同一 ID 构造左右候选并期望 `charter_invocation_request_invalid`；实际得到 `Succeeded=true`。权威 Unity 入口执行 323 项：320 通过、3 失败；新增失败精确命中该问题，另两项为既有无关基线。临时测试已撤回，未进入正式变更。
- 最窄返工只需让册界调用层拒绝重复候选 ID（或等价地保证 `charterCandidateId` 唯一对应一侧）并保留直接测试；shared resolver、冲突排序、供给结转、存档、场景与 UI 均不改。

## U-TZ-CHARTER-MODEL-01 重复候选 ID 唯一侧绑定返工记录（2026-08-03）

> ⚠️ 已修改/未审核；修改方：DeepSeek V4 Flash；变更范围：`CharterRuleRuntime.cs`、`CharterRuleRuntimeTests.cs`、本记录。

按 `开发管理/未通过审核清单.txt` 的最窄返工，在册界调用层拒绝左右候选重复 ID，保证 `charterCandidateId` 唯一锁定册界侧；不修改 shared resolver、冲突排序、供给结转、存档、场景或 UI：

- `IsCharterCandidateDeclared` 现在要求左右候选 `CandidateId` 互异（Ordinal）。两侧同 ID 时，`charterCandidateId` 无法区分哪一侧代表本次册界调用，shared 任一侧获胜都会返回同一 `WinnerCandidateId`；该请求直接判为 `charter_invocation_request_invalid`，不进入 shared 决定、不替换状态、不输出事件。
- 保留 Codex 返工复审的直接反例为正式用例 `DuplicateCandidateIdsCannotDeclareWhichSideRepresentsTheCharterInvocation`：左右候选同用 `jindan_left`、`charterCandidateId=jindan_left`、archive 可解析，期望稳定拒绝。

实际命令与结果：

- `pwsh -NoProfile -ExecutionPolicy Bypass -File tools/run-unity-editmode-tests.ps1`：全套 323 项中 321 通过、2 项既有无关基线失败（逐项记录见下）；册界直接用例全部通过：`CharterRuleRuntimeTests` 13/13（含新增 `DuplicateCandidateIdsCannotDeclareWhichSideRepresentsTheCharterInvocation`）、`CharterRuleDataTests` 26/26、`CharterConflictRulesTests` 4/5。
  - 既有失败 1：`DataConfigImporterContentScopeTests.LianShenReservedAbilitiesRemainExcludedAtPlayerLoad`（与 2026-07-31 记录一致的已知基线失败：仍期待旧日志 "Excluded non-player spell"，现有未改动加载器输出 "Excluded unresolved art profile"）。
  - 既有失败 2：`CharterConflictRulesTests.AuthorizedGrantBindsEveryConflictIdentityField`（`CharterConflictRules.cs`／`CharterConflictRulesTests.cs` 均来自 D-TZ-CHARTER-CONFLICT-02 提交 e1ffda9，本任务未改动；断言期望 `PULSE_ADVANTAGE`，shared 实现按既有优先序对等储备返回 `PULSE_NEUTRAL`）。
- `dotnet build -c Release simulations/BattleSim`（worktree 无 obj，先隐式 restore）：通过，0 警告、0 错误。
- `dotnet run --no-build -c Release --project simulations/BattleSim -- --self-test golden-core-conflict-n-jd-rule-01b`：PASS（shared 冲突语义未分叉）。
- `pwsh -NoProfile -ExecutionPolicy Bypass -File tools/check-review-text.ps1 -Paths 开发管理,docs,simulations,src/Assets/Scripts,src/Assets/Tests`：OK（665 个文件）。
- `pwsh -NoProfile -ExecutionPolicy Bypass -File tools/check-pending-whitespace.ps1`：OK（8 个文件）。
- Unity 验证完成后工作树仅含本轮修改文件，两个既有 `.meta`（`CharterRuleRuntime.cs.meta`、`CharterRuleRuntimeTests.cs.meta`）仍受 Git 跟踪，无验证生成的未跟踪现场。

重复候选 ID 返工后水府地纪纵向样例的可观察边界增量：

- 左右候选使用同一 `CandidateId`（`charterCandidateId` 等于该 ID、archive 可解析）→ `charter_invocation_request_invalid`，不进入 shared 决定、不提交规则状态或事件；正常互异候选的合法授权路径不受影响（13/13 直接用例通过）。

## Codex 独立复审通过（2026-08-03）

- 审核正式业务提交 `389a08a7c2a5123cf712a33537e480a4b7cf3897` 相对 `3b39b12` 的限定 diff，并回查动态状态、共享冲突契约、有效样例与任务卡。重复候选 ID 在调用层稳定拒绝；没有复制 resolver、grant 或账本所有者。
- `pwsh -NoProfile -ExecutionPolicy Bypass -File tools/run-unity-editmode-tests.ps1`：323 项中 321 通过，`CharterRuleRuntimeTests` 13/13 全通过；仅保留已登记的两项无关基线失败（`PULSE_NEUTRAL` 对旧 `PULSE_ADVANTAGE` 期望、LianShen 旧日志断言）。
- 为满足 `--no-restore` 验证前提，在全新 worktree 运行 `dotnet restore simulations/BattleSim` 后，`dotnet build -c Release --no-restore simulations/BattleSim` 为 0 警告、0 错误；`dotnet run --no-build -c Release --project simulations/BattleSim -- --self-test golden-core-conflict-n-jd-rule-01b` 为 PASS。复审结论：通过。
