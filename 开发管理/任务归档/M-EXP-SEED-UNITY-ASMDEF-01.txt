---TASK-META---
{
  "schemaVersion": 1,
  "id": "M-EXP-SEED-UNITY-ASMDEF-01",
  "title": "晋级 Unity asmdef 投影排查经验",
  "priority": "P1",
  "route": "codex_execute",
  "owner": "codex",
  "domain": "unity",
  "stage": "implementation",
  "dispatchState": "completed",
  "blockedBy": [],
  "stateReason": "已独立核验并晋级 EXP-UNITY-002 为 active 的 must_read 经验；任务完成归档。",
  "expectedPaths": [
    "开发管理/经验库/风险索引.json",
    "开发管理/经验库/经验卡/EXP-UNITY-002.txt",
    "开发管理/任务列表/管理与自动化任务.txt",
    "开发管理/当前任务队列.txt",
    "开发管理/任务卡/M-EXP-SEED-UNITY-ASMDEF-01.txt",
    "开发管理/任务归档/M-EXP-SEED-UNITY-ASMDEF-01.txt",
    "开发管理/任务卡/M-EXP-PREFLIGHT-TRIAL-01.txt"
  ],
  "sourceBacklog": "开发管理/任务列表/管理与自动化任务.txt"
}
---TASK-BODY---
# M-EXP-SEED-UNITY-ASMDEF-01 · 晋级 Unity asmdef 投影排查经验

## 来源与当前边界

- 单一结果：核验并晋级 `EXP-UNITY-002`，把命名程序集下的依赖排查定位到 asmdef／asmref 与 Unity 生成 csproj，而不是维护旧 Assembly-CSharp 或手改生成文件。
- 直接事实源是 `开发管理/开发-技术经验.txt#asmdef 分层后的 csproj 排查`、当前程序集定义和 `tools/check-unity-assembly-boundaries.ps1`。
- 本卡不修改程序集或检查器。

## 必查范围

- `src/Assets/Scripts/**.asmdef` 与两个现行 `.asmref` 的直接依赖；只读取与检查器要求的目标集合。
- `tools/check-unity-assembly-boundaries.ps1` 的 required target、引用方向和拒绝条件。
- `src/TianZhang.EditModeTests.csproj` 的生成投影用途、clean linked worktree 可缺失边界及 `run-unity-editmode-tests.ps1` 权威入口。

## 实施范围

- 建立 `EXP-UNITY-002.txt` 和唯一索引项；触发限定 asmdef／asmref、namespace missing、ProjectReference 或明确程序集边界语义。
- 标明未来程序集所有者或生成投影入口变化后的失效条件。
- 完成后只移除试运行卡中的本前置。

## 禁止项

- 不修改 `.csproj`、asmdef、asmref、源码或边界检查器，不把普通 C# 编译错误泛化命中。
- 不提前升级门禁；门禁独立卡为 `M-EXP-GATE-UNITY-ASMDEF-01`。

## 验证

- `pwsh -NoProfile -ExecutionPolicy Bypass -File tools/check-unity-assembly-boundaries.ps1`。
- `pwsh -NoProfile -ExecutionPolicy Bypass -File tools/run-unity-editmode-tests.ps1`。
- 若当前 worktree 已有 `src/TianZhang.EditModeTests.csproj`，执行 `dotnet build src/TianZhang.EditModeTests.csproj`；缺失时记录生成投影缺失并以 Unity EditMode 结果作为权威验证，不复制或手改投影。
- 匹配器 fixture 覆盖 asmdef+精确诊断命中和普通 C# 文件零命中。
- `pwsh -NoProfile -ExecutionPolicy Bypass -File tools/check-task-cards.ps1`。

## 完成条件

- 当前程序集所有者、失败方式和失效条件可复核；`EXP-UNITY-002` active 且试运行卡只移除本 ID。

## 停止条件

- 权威 Unity EditMode 编译或测试失败、当前程序集所有者与经验描述冲突、Assembly-CSharp 恢复为活动所有者、或没有稳定排除例；保持 blocked，不写 active。
