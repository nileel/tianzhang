# TQ-050 · 权威 Unity EditMode 测试入口归档

- 执行：2026-07-11 TZG Hourly Controller / `gpt-5.6-terra`。
- 任务：Q-UNITY-02，建立 `tools/run-unity-editmode-tests.ps1`。

## 交付

- 新增统一入口：默认启动 Unity 6.0.3.18f1 的 EditMode 测试，等待实际测试进程结束后解析 XML；不以 Unity 进程退出码替代测试结果判断。
- 结果 XML 与日志默认写到系统临时目录，避免 Unity 退出时清理项目 `Temp/` 下的结果文件。
- 新增 `tools/test-run-unity-editmode-tests.ps1`，覆盖通过、失败、缺失、不可解析和零测试 XML 五种结果。

## 验证

- RED：入口初始骨架将所有结果返回 1，成功 XML 用例按预期失败。
- GREEN：`powershell -ExecutionPolicy Bypass -File tools/test-run-unity-editmode-tests.ps1` 退出 0；四个负例均返回非 0。
- 真实入口：`powershell -ExecutionPolicy Bypass -File tools/run-unity-editmode-tests.ps1` 生成完整 `test-run` XML，`total=54, passed=54, failed=0, result=Passed`。
- `git diff --check`：通过。

## 已知后续工作

真实 EditMode 测试会改动场景与 Build Settings；这些生成改动已人工恢复，且未纳入本任务提交。TQ-051 专门处理成功与异常路径的隔离和自动恢复。
