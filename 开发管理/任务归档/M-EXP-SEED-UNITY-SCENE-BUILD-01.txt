---TASK-META---
{
  "schemaVersion": 1,
  "id": "M-EXP-SEED-UNITY-SCENE-BUILD-01",
  "title": "晋级 Unity 场景程序化生成经验",
  "priority": "P1",
  "route": "codex_execute",
  "owner": "codex",
  "domain": "unity",
  "stage": "implementation",
  "dispatchState": "completed",
  "blockedBy": [],
  "stateReason": "已独立核验并晋级 EXP-UNITY-001 为 active 的 must_read 经验；任务完成归档。",
  "expectedPaths": [
    "开发管理/经验库/风险索引.json",
    "开发管理/经验库/经验卡/EXP-UNITY-001.txt",
    "开发管理/任务列表/管理与自动化任务.txt",
    "开发管理/当前任务队列.txt",
    "开发管理/任务卡/M-EXP-SEED-UNITY-SCENE-BUILD-01.txt",
    "开发管理/任务归档/M-EXP-SEED-UNITY-SCENE-BUILD-01.txt",
    "开发管理/任务卡/M-EXP-PREFLIGHT-TRIAL-01.txt"
  ],
  "sourceBacklog": "开发管理/任务列表/管理与自动化任务.txt"
}
---TASK-BODY---
# M-EXP-SEED-UNITY-SCENE-BUILD-01 · 晋级 Unity 场景程序化生成经验

## 来源与当前边界

- 单一结果：核验并晋级 `EXP-UNITY-001`，要求正式 `.unity` 结构通过既有 Editor builder 生成，避免手写 YAML。
- 直接事实源是 `开发管理/开发-技术经验.txt#Unity 开发经验`、现行场景 builder/support 和 EditMode 场景测试。
- 本卡不修改场景、builder 或测试。

## 必查范围

- `src/Assets/Scripts/Editor/SceneBuildSupport.cs` 的 NewScene／SaveScene 完整逻辑单元。
- `AdventureSceneBuilder.cs`、`StartMenuSceneBuilder.cs`、`SettlementSceneBuilder.cs`、`WorldSceneBuilder.cs` 的菜单入口和保存调用。
- `src/Assets/Tests/EditMode/SceneArchitectureEditorTests.cs` 与相关 builder 测试。

## 实施范围

- 建立 `EXP-UNITY-001.txt` 和唯一索引项；路径命中 `.unity` 时还必须由标题／必查／实施范围出现 builder、EditorSceneManager 或明确场景重建语义。
- 排除只读场景检查、截图和不写 `.unity` 的运行时任务；记录 builder 所有者改变后的失效条件。
- 完成后只移除试运行卡中的本前置。

## 禁止项

- 不修改或重建任何场景，不把所有 Unity 任务都命中，不新增门禁或 SceneBuilder。

## 验证

- `pwsh -NoProfile -ExecutionPolicy Bypass -File tools/run-unity-editmode-tests.ps1`。
- 匹配器 fixture 覆盖 `.unity`+builder 命中、仅截图／只读场景零命中和 Windows 路径归一化。
- `pwsh -NoProfile -ExecutionPolicy Bypass -File tools/check-task-cards.ps1`。

## 完成条件

- 当前所有者、失败路线、适用／排除／失效条件均有直接证据；`EXP-UNITY-001` active 且试运行卡只移除本 ID。

## 停止条件

- 正式场景已不由 Editor builder 管理、测试不能证明保存链，或触发必须仅靠 `.unity` 路径泛化；保持 blocked，不写 active。
