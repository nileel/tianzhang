---TASK-META---
{
  "schemaVersion": 1,
  "id": "M-EXP-SEED-BS-BUILD-RUN-01",
  "title": "晋级 BattleSim 先构建后运行经验",
  "priority": "P1",
  "route": "codex_execute",
  "owner": "codex",
  "domain": "battlesim",
  "stage": "implementation",
  "dispatchState": "completed",
  "blockedBy": [],
  "stateReason": "已独立核验并晋级 EXP-BS-001 为 active 的 must_read 经验；任务完成归档。",
  "expectedPaths": [
    "开发管理/经验库/风险索引.json",
    "开发管理/经验库/经验卡/EXP-BS-001.txt",
    "开发管理/任务列表/管理与自动化任务.txt",
    "开发管理/当前任务队列.txt",
    "开发管理/任务卡/M-EXP-SEED-BS-BUILD-RUN-01.txt",
    "开发管理/任务归档/M-EXP-SEED-BS-BUILD-RUN-01.txt",
    "开发管理/任务卡/M-EXP-PREFLIGHT-TRIAL-01.txt"
  ],
  "sourceBacklog": "开发管理/任务列表/管理与自动化任务.txt"
}
---TASK-BODY---
# M-EXP-SEED-BS-BUILD-RUN-01 · 晋级 BattleSim 先构建后运行经验

## 来源与当前边界

- 单一结果：核验并晋级 `EXP-BS-001`，约束修改模拟器后先 Release build、再 `--no-build` 运行当前实际结果。
- 直接事实源是 `AGENTS.md#数值与 BattleSim`、`开发管理/开发-技术经验.txt#模拟器` 和当前 `simulations/BattleSim` 入口。
- 本卡不修改模拟器、不下数值结论、不新增门禁脚本。

## 必查范围

- 上述两个规则章节的完整命令与“输入未变化不重复运行”边界。
- `simulations/BattleSim/BattleSim.csproj`、`Program.cs::Main` 和 `BattleSimSelfTests.cs` 的现行入口关系。

## 实施范围

- 冻结适用路径为 BattleSim 源码／项目入口和明确的数值验证任务，排除只读既有结果且输入未变化的任务。
- 建立 `EXP-BS-001.txt` 与唯一索引项；初始 `level=must_read`，只引用已有固定检查入口。
- 完成后只移除试运行卡中的本前置。

## 禁止项

- 不把历史数值输出写成经验事实，不修改命令顺序，不引入重试或 Debug 兼容入口。
- 不让仅编辑无关文档的 `battlesim` 任务仅凭 domain 自动命中。

## 验证

- `dotnet build -c Release --no-restore "D:\天章游戏开发\simulations\BattleSim"`。
- `dotnet run --no-build -c Release --project "D:\天章游戏开发\simulations\BattleSim"`。
- `pwsh -NoProfile -ExecutionPolicy Bypass -File tools/test-get-experience-risk-preflight.ps1`，并验证源码路径命中／无关文档零命中。
- `pwsh -NoProfile -ExecutionPolicy Bypass -File tools/check-task-cards.ps1`。

## 完成条件

- 当前 build→run 入口可重复通过，适用、排除与失效条件明确，`## 开工前` 在字符边界内。
- `EXP-BS-001` active 且试运行卡只移除本 ID。

## 停止条件

- 当前项目入口、命令或事实源已改变，实际 build／run 失败，或候选必须保存历史数值才成立；本卡保持 blocked，不写 active。
