# N-DIST-02 · 1v1 位置控制归档

- 完成日期：2026-07-21
- 主责：Codex / gpt-5.6-sol
- 业务提交：本归档所在提交

## 完成结果

- `HexBattlefield` 新增六向强制位移接口；位移按指定方向逐边结算，遇移动阻挡或占用落点立即停止，不绕路。
- 《破阵冲锋》的事实源击退距离以独立位置控制配置登记为 1 格；未新增无事实源的击退距离、概率或持续参数。
- 1v1 行动选位统一通过 `Combat.ResolveActionPosition`；定身把本次可用移动归零，但保留原地合法攻击资格，贴身且低于最小射程时不会后退或绕过射程拒绝。
- 既有伤害表、2v2、伤害计算和防御结算段未修改；有向边、加权边长、地形环境、高低差、掩体与 Unity 战斗语义不在本任务扩展。

## 验证证据

- Release build 通过：0 警告、0 错误。
- `position-control-n-dist-02` 通过，覆盖击退距离变化、阻挡停止、定身不移动、定身下原地合法攻击与最小射程拒绝。
- `battlefield-foundation-n-dist-01` 与 `distance-model-tq055` 通过。
- 默认 BattleSim 与 `--g2-audit --cycles 400` 均返回 0。
- 审核文本、暂存前空白、暂存后差异和 workspace guard 结果以本任务提交门禁为准。

## 后续入口

- `N-GROUP-01` 的 N-DIST-02 前置已满足，可复用同一距离、视线和位置控制语义推进 2v2 占格基座。
- 后续若需有向边、可变空间边长或非法边模型，应由 N-ENV-01 的事实源与接口统一扩展，不在位置控制路径另建第二套距离服务。
