# 已完成任务验证记录归档

本目录保存已经完成并具有对应任务归档的验证记录，仅用于历史追溯，不作为当前任务、当前风险或系统行为的默认事实源。

- 当前任务与状态分别读取 `开发管理/任务卡/`、`开发管理/当前任务队列.txt`、`开发管理/设计-当前状态.txt` 和各分线 backlog。
- Unity 当前行为读取 `src/`；角色数值结论以 BattleSim 实际运行结果为准。
- 仍被活跃任务、当前状态或 backlog 直接引用的验证材料保留在 `开发管理/` 根目录，待对应工作闭环后再归档。
- 2026-08-09 首次整理：只迁移无活跃引用、且能回链到已完成任务归档的验证记录；所有仓库内字面量引用同步更新到本目录。
