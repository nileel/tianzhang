# N-DIST-01 · 1v1 六角战场基座归档

- 完成日期：2026-07-21
- 主责：Codex / gpt-5.6-terra
- 业务提交：`954f5c8fc92dda8373660671f71190bed094bdb6`

## 完成结果

- BattleSim 的 1v1 标量距离状态已替换为轴向六角格位。
- 已接入显式移动成本、移动阻挡和视线阻挡接口；既有伤害表及 `Combat` 的伤害计算／防御结算段未修改。
- 有向边、可变空间边长、高低差、掩体、位置控制和 2v2 占格继续留给后续任务，不在本任务内扩展。

## 验证证据

- `battlefield-foundation-n-dist-01` 与既有 `distance-model-tq055` self-test 通过。
- Release build 为 0 警告、0 错误；默认 BattleSim 完整运行返回 0。
- 审核文本、暂存前空白、暂存后 `git diff --cached --check` 与 workspace guard 均通过。

## 后续入口

- `N-DIST-02` 与 `N-ENV-01` 的唯一前置 `N-DIST-01` 已满足，可按统一排序在后续队列维护中提升。
- 后续任务必须复用本任务的距离与视线语义；没有事实源的参数仍须先请求负责人决定。
