# R-20260704-03 / TQ-031 第一章主线剧情骨架复审归档

复审时间：2026-07-05
复审方：Codex / gpt-5.5
执行方：DeepSeek V4 Pro
复审对象：`6f63f6b`，新增 `docs/剧情/主线/第一章-龙脊余震.txt`。

## 结论

通过。

第一章骨架满足 TQ-031 完成条件：
- 覆盖太一道庭、混元山、太虚观、玄荒殿、散修五类入口，超过至少三类入口要求。
- 使用五个任务节拍：初涉修真、龙脊余震、节点争夺、信息碎片、章末抉择。
- 玩家范围限定在凡人/练气至筑基初期，不推进到金丹以上。
- 司马承景和苻渊不直接出场；谢观微、姚观寂、拓跋烈、卫长庚等重要 NPC 只在限定入口与限定场景中出现。
- 信息分层符合 `背景与重要NPC设计规范.txt`：谢观微、吕星枢、钟离幽等只提供局部信息或半截判断，不构成全知 NPC。

保留风险：文档中出现 `丹枢`、`位格`、`席位`等术语作为世界背景线索。当前未把这些机制解锁给玩家，后续支线拆分时仍需保持“背景信息可知、金丹机制不解锁”的边界。

## 验证

- `powershell -ExecutionPolicy Bypass -File tools/check-review-text.ps1 -Paths docs/剧情,开发管理`：通过。
- `git show --check 6f63f6b`：通过。
- `rg -n "正籍|辅籍|敕籍|寄丹|妖血敕|炼虚" docs/剧情/主线 docs/剧情/重要NPC docs/剧情/世界背景故事.txt docs/剧情/背景与重要NPC设计规范.txt`：主线文件仅在禁止条款中出现旧口径；NPC 正文旧口径残留属于 TQ-030 已知未通过项。

未运行 BattleSim / Unity 编译：本项为纯剧情设计文档，不改数值、CSV、asset 或代码。

## 后续

TQ-031 已闭环。TQ-032 普通 NPC 模板与候选清单可读取本骨架中的新增 NPC 类型和五入口结构。
