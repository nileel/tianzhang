---TASK-META---
{
  "schemaVersion": 1,
  "id": "M-EXP-SEED-MGMT-WORKTREE-01",
  "title": "晋级共享 worktree 与集成锁经验",
  "priority": "P1",
  "route": "codex_execute",
  "owner": "codex",
  "domain": "management",
  "stage": "implementation",
  "dispatchState": "ready",
  "blockedBy": [],
  "stateReason": "基础匹配器已完成 Codex 独立复审；M-EXP-PREFLIGHT-01A 前置已解除，按既定顺序入队，仍须独立核验 EXP-MGMT-001。",
  "expectedPaths": [
    "开发管理/经验库/风险索引.json",
    "开发管理/经验库/经验卡/EXP-MGMT-001.txt",
    "开发管理/任务列表/管理与自动化任务.txt",
    "开发管理/当前任务队列.txt",
    "开发管理/任务卡/M-EXP-SEED-MGMT-WORKTREE-01.txt",
    "开发管理/任务归档/M-EXP-SEED-MGMT-WORKTREE-01.txt",
    "开发管理/任务卡/M-EXP-PREFLIGHT-TRIAL-01.txt"
  ],
  "sourceBacklog": "开发管理/任务列表/管理与自动化任务.txt"
}
---TASK-BODY---
# M-EXP-SEED-MGMT-WORKTREE-01 · 晋级共享 worktree 与集成锁经验

## 来源与当前边界

- 单一结果：核验并晋级 `EXP-MGMT-001`，记录在共享主工作区有用户改动或并发责任链时必须隔离写入、实时复核冲突并使用正式集成锁。
- 直接事实源是 `AGENTS.md#修改与验证`、`开发管理/AI协作规则.txt#快捷工作流`、自动工作流规则和正式集成脚本／测试。
- 管理语义误报代价高，首期固定 `triggerMode=explicit_only`。

## 必查范围

- 上述三个规则章节的手动／自动 worktree、schema 5 Show、路径冲突、同 taskId 和 fast-forward 条件。
- `tools/invoke-project-integration.ps1`、`hourly-integration-lock.ps1` 的路径和锁合同。
- `tools/test-hourly-integration-lock.ps1` 与自动化工作区隔离测试的直接正反例。

## 实施范围

- 建立 `EXP-MGMT-001.txt` 和唯一索引项；只有任务卡显式引用才命中，不通过 domain、worktree 字样或路径自动推断。
- 明确独立只读对话、不存在任何写入的排除条件，以及集成架构变化后的失效条件。
- 完成后只移除试运行卡中的本前置。

## 禁止项

- 不修改规则、runtime、锁、集成脚本或工作树，不让索引裁决任务主责或授权。
- 不把本经验变成第二套集成流程，不自动创建 worktree 或执行 Git。

## 验证

- `pwsh -NoProfile -ExecutionPolicy Bypass -File tools/test-hourly-integration-lock.ps1`。
- 匹配器 fixture 证明未显式引用时零命中、显式引用时命中且不执行任何 Git／锁操作。
- `pwsh -NoProfile -ExecutionPolicy Bypass -File tools/check-task-cards.ps1`。

## 完成条件

- 当前规则与脚本能证明根因、适用／排除／失效边界；`EXP-MGMT-001` active 且试运行卡只移除本 ID。

## 停止条件

- 规则与脚本所有者冲突、经验需要自动改派或新 runtime 状态、或 explicit_only 仍不能防误用；保持 blocked，不写 active。
