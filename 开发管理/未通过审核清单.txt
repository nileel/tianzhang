# 未通过审核清单（✅ 已审核）

> 2026-06-28 Codex / gpt-5.5 批量复审：TQ-007 目标提交 3383f11 通过；TQ-010 目标提交 e59bff8 通过但当前工作树有新 asset 回归；TQ-014-DS-01/03/Input/b549495 通过；TQ-014-DS-04/05、TQ-015B-2/3、DS-NEW-01/02 与当前工作树仍不通过。详见 `开发管理/AI合作归档/2026-06-28-AI合作批量复审归档.txt`。
> 2026-06-28 Codex / gpt-5.5 复审 HANDOFF-20260627-05：TQ-014-DS-05 不通过；新增控制器未挂入 World/Settlement 场景，目标范围 `git diff --check` 仍失败。
> 2026-06-19 Codex / gpt-5.5 复审通过：TQ-003 commit 767f93d 已通过严格覆盖、meta 配对、关键字段一致性、旧路径扫描、Unity C# build 与 `git diff --check`；当前未通过/待复审项清零。
> 2026-06-27 Codex / gpt-5.5 复审 b674f79：TQ-006 通过并从清单移除；TQ-007/TQ-010/TQ-015B-2/3 仍不通过，失败原因更新为本轮证据。
> 2026-06-25 Codex / gpt-5.5 批量复审：TQ-006、TQ-007、TQ-010、TQ-015B-2/3 未通过；TQ-013D 与金丹位格三字命名替换通过并已归档。
> 2026-06-19 Codex / gpt-5.5 复审补修历史：复审 TQ-003 commit 43d0a92 时发现新增 asset 缺 57 个 `.meta`、`spell_tx_anshi` asset 字段与 CSV 不一致、`SceneBuilder` 仍引用旧 asset 路径；后续已在 767f93d 复审通过。
> 2026-06-19 Codex / gpt-5.5 复审通过：TQ-012 规范/模板补修项已通过复审并从清单移除；当时清单仅保留 TQ-003 Unity asset 覆盖缺口，现已清零。
> 2026-06-19 Codex / gpt-5.5 复审修正：TQ-003 commit f7f6944 的 CSV/Language 返工已机械核对；本轮修正 `gongfa_taiyiyugedian` 资质星级 2→3 后，当时数据链路剩余问题收窄为 Unity asset 覆盖缺口。
> 2026-06-19 Codex / gpt-5.5 复审：TQ-003 commit 567c3f2 部分通过。旧 #3 Language 重复 key、旧 #4 管理状态校准、旧 #5 未审核状态保留已复核通过；数据链路剩余问题合并为当前 #1。
> 2026-06-17 Codex / ChatGPT5.5 复审通过：从 `开发管理/审核规则.txt` 拆出为动态清单，内容沿用原未通过项；清单内条目仍需逐项修复。依据：40d5887、26e8210。

## 用途

记录 2026-06-16 对既有未审核内容复核后的未通过项，方便后续逐项修复。处理并审核通过后，应按 `开发管理/审核规则.txt` 的“审核记录归档”规则从本清单移除。

## 当前未通过/待复审项

### REVIEW-20260628-01 · TQ-014-DS-04/05 场景脚手架与 World/Settlement 数据壳

- 结论：部分通过，当前仍不通过。
- 目标提交：fe610ed、959fbda、fb7f7ed、0b4ab27、334e88e、3cb5a7d、9a12f5e；历史修补 dfa69a1 / 82ab968 已被后续提交覆盖。
- 已通过部分：`SceneBuilder.BuildEmptyScene` 已支持 `sceneControllerType` 并在 World/Settlement/Adventure 菜单入口挂载对应 Controller；`GameSession` 与 `SceneFlowManager` 已保存 `CurrentSettlementId` / `CurrentAdventureId`；新增 `.meta` 均为多行 Unity YAML 且 GUID 为 32 位 hex；`Assembly-CSharp.csproj` 当前包含新增脚本；`dotnet build src/Assembly-CSharp.csproj` 通过。
- 未通过问题：
  1. HEAD 中 `WorldScene.unity` / `SettlementScene.unity` / `AdventureScene.unity` 仍未提交 Controller 引用；当前工作树只有 World/Settlement 场景含 Controller，Adventure 场景仍无 `AdventureSceneController`。
  2. 当前工作树存在未提交 Unity/asset 脏改，全量 `git diff --check` 失败；其中 3 个 Spell asset 被改回空 `element`，已另列当前工作树阻塞。
  3. Unity Editor 端到端生成、加载、返回尚未验证；当前 `.unity` 变更不能直接视为已通过。
- 返工建议：用 Unity Editor 重新执行 SceneBuilder 菜单，提交 World/Settlement/Adventure 场景资产中的 Controller 挂载；清理或回滚当前工作树里无关 asset 数值回归；重新运行 `dotnet build src/Assembly-CSharp.csproj`、场景/asset 专项检查与可接受范围的 diff 检查。

### REVIEW-20260628-02 · 当前工作树 Unity/asset 脏改

- 结论：不通过。
- 范围：当前未提交 diff，不归咎于 TQ-010 目标提交 e59bff8。
- 已通过部分：`e59bff8` 目标提交自身已通过，109 个 Spell/Skill asset 在该提交树中均恰好 1 个非空 `element`。
- 未通过问题：
  1. 当前工作树的 `Spell_spell_jinguangpoyue.asset`、`Spell_spell_liuhuolingfu.asset`、`Spell_spell_tx_anshi.asset` 各只有 1 个但为空的 `element` 字段。
  2. `Spell_spell_liuhuolingfu.asset` 与 `Spell_spell_tx_anshi.asset` 当前 diff 同时改动了 `type`、`range`、`mpCost`、`cooldownTicks`、`damageMultiplier` 等数值字段，超出 TQ-010 去重范围。
  3. 当前工作树全量 `git diff --check` 失败，失败来自未提交 Unity/asset YAML 空值行与上述 asset 回归。
- 返工建议：回滚或按 CSV 重新导入 3 个 Spell asset，确保 109 个 Spell/Skill asset 每个恰好 1 个非空 `element`；再决定是否保留并提交 Unity 场景生成 diff。

### REVIEW-20260628-03 · TQ-015B-2/3 NPC 与批量生产模板迁移

- 结论：部分通过，当前仍不通过。
- 目标提交：95055c1；错字修复提交：a3be216。
- 已通过部分：`a3be216` 已清理点名的“建/道”机械错字；旧 `丹籍`、`一正二辅`、`正籍/辅籍/敕籍/寄丹` 在当前目标范围内仅保留为历史说明或模板禁用项。
- 未通过问题：慕容朔、拓跋烈、钟离幽、祝融烈仍把人物丹枢/派系概念写入席位括号名，未引用 `docs/基础设定/元婴锚点与金丹位格矩阵.txt` 的正式三字名：`天狼曜`、`苍狼真形`、`地脉阴河`、`焚天火种` 均不在 51 个正式席位名中。
- 返工建议：把“元婴分支 + 源/化/界席位”改为矩阵正式名；把人物特色放入“丹枢方向/战斗表达”，不得放进席位括号名；返工后用脚本比对 NPC 中 `源位（...）/化位（...）/界位（...）` 是否均属于矩阵 51 名。

### REVIEW-20260628-04 · DS-NEW-01/02 炼虚与 realm_lianshen 报告

- 结论：不通过。
- 目标提交：c7396fe、fb2ae37。
- 未通过问题：
  1. `开发管理/realm_lianshen专项检查.txt` 含 28 个孤立 CR 控制字符，导致 `realm_lianshen` / `realm_lianxu` / `rg` 显示污染。
  2. 两份报告统计与当前实际 CSV 不一致：`GongFa.csv` 实际为 6 条 `realm_lianxu` + 4 条 `realm_lianshen`，不是报告中的 5+4 或 9 总数。
  3. `realm_lianshen专项检查.txt` 验证记录仍写 `check-review-text.ps1: 待运行`，与交接自述和本轮检查不一致。
- 返工建议：先修控制字符与统计口径，再由 Codex 判断 F1（`realm_lianshen` 是否独立境界 / 资料片 key / 合并到 `realm_lianxu`）。

## 后续复核建议

- 优先清理当前工作树 asset 回归与场景资产提交边界；否则 TQ-010 目标提交虽通过，当前数据闸门仍不干净。
- TQ-014-DS 下一轮只处理场景资产生成/提交、Adventure Controller 挂载与 Editor 端验证，不扩大到 CTB/伤害/冷却/槽位语义。
- TQ-015B-2/3 返工只改 NPC 席位正式名与丹枢表达，不扩大到 TQ-015C 数值方案。
- DS-NEW-01/02 修正前，不以其 F 类统计作为架构决策依据。
