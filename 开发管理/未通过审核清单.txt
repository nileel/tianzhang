# 未通过审核清单（✅ 已审核）

## 用途

记录当前仍未通过、已返工待复核或必须由 `2` 流程处理的审核项。历史审核过程已归档至 `开发管理/任务归档/2026-07-12-管理入口瘦身前状态归档.txt`；当前状态以本文件为准。

## 当前未通过/待复核项

- **C-GZ-ENEMY-01（复审仍不通过）** | 目标提交：`3e7801ce6dcf2f48bdf5b055afd190ec68962704`（前次退回提交：`a3ff18281f5411a7cb6d0102c60c7f38f372fcc3`）
  - 问题编号：`REV-C-GZ-ENEMY-01-01`
  - 事实依据：本次已删除权重和黑曜石甲兽内容，但档案的“战斗识别特征”仍列出格挡 15%／25%、暴击 5%／10% 等具体数值，并作出 CTB 先手断言，仍违反任务卡的数值禁止项及本问题的定向返工要求。档案还把 `EnemyData.dropEntries[]` 的 `dropChancePercent` 结算、正式敌人缺失时不使用 fallback 写成当前运行时行为；框架仅定义后续 EnemyData 目标，任务卡明确这些字段尚未进入完整运行时结构。两项掉落的“来源语义”又新增岩质甲壳、土属性灵力、炼器／阵法基底和补充灵力等材质、用途与经济断言，既无当前 CSV／Language 直接事实支持，也越过本卡边界。
  - 建议修复范围：仅重写 `docs/怪物/关陇玄域/石甲兽.txt`，保留稳定 ID、类型、练气期低阶、关中野外单敌用途、`ai_melee` 最小行为、战斗模板引用、无装备能力和当前 CSV 掉落候选。删除所有具体数值、CTB 或现行掉落运行时断言；掉落只说明候选 ID 与概率／数量交由 `N-GZ-REWARD-01`，不描述材质、用途、补灵、炼器、阵法或经济效果。不得修改 CSV、Language、asset、场景、AI 或战斗代码；完成后以同一卡重新标记未审核并提交复审。

- **C-GZ-ITEM-01（不通过）** | 目标提交：`22b7967d733d2769d6998e9f09e9754ef70a5add`
  - 问题编号：`REV-C-GZ-ITEM-01-01`
  - 事实依据：`docs/怪物/关陇玄域/石甲兽.txt` 所写晶化背甲、生态与掉落来源仍属于已不通过的 `C-GZ-ENEMY-01`，不能充当本卡正式事实；本卡两份档案却将其写为既定来源。`劣质灵石.txt` 又称其为日常灵气补给品、对练气修士可提供微弱灵气，越过任务卡禁止的修炼／使用效果和经济用途；`石甲碎片.txt` 声称风隼 `enemy_fengsun` 也引用 `item_shijia_piece`，但 `Enemies.csv` 该行实际为 `item_fengsun_feather` 与 `item_lingshi_low`。
  - 建议修复范围：仅重写两份物品档案。保留与 `Language.csv`、`Enemies.csv` 一致的稳定 ID、显示名、基础资源／妖兽材料类别、可堆叠身份及未来掉落／悬赏库存用途；来源仅表述为当前 CSV 的候选引用，不采用未通过怪物档案的生态、材质或掉落断言；删除经济、修炼、灵气补给和使用效果表述，删除错误的风隼引用与“唯一掉落来源”断言；不修改 CSV、Language、asset、运行时代码或任何参数。

## 复审路由

复审时先读 `开发管理/审核入口.txt`，再读 `开发管理/AI合作沟通.txt` 当前条目与 `开发管理/任务列表/审核与交接任务.txt`。
