# 未通过审核清单（⚠️ 待复核）

## 用途

记录当前仍未通过、已返工待复核或必须由 `2` 流程处理的审核项。历史审核过程已归档至 `开发管理/任务归档/2026-07-12-管理入口瘦身前状态归档.txt`；当前状态以本文件为准。

## 当前未通过/待复核项

### REV-C-GZ-BOUNTY-01-01 · 一次性石甲兽悬赏内容档案

- 任务／提交：`C-GZ-BOUNTY-01`；`530a3504fcc37e09d5a8ccc1514b299573504cf3`。
- 结论：部分通过；已将同一卡设为 `blocked` 并移出当前队列。
- 证据：目标档案把 `BountyRuntime`、`BountyStateStore`、`SettlementFeatureDispatcher`、schema 3 与 `GameSession` 原子领奖写成现有行为；实时源码没有上述悬赏所有者，`GameSessionSnapshot.CurrentSchemaVersion` 仍为 2。
- 定向返工：仅修订 `docs/剧情/任务/关中/quest_guanzhong_bounty_shijiahou.txt`，把所有未接入的悬赏、背包与存档行为明确为后续契约或正式框架引用；保留已核对通过的 ID、入口、目标、失败关闭、一次性规则、奖励留白和 D 级演出。
- 复核门槛：保留「⚠️ 已修改/未审核」，通过剧情／管理文本检查和 `git diff --check` 后转 `codex_review`；不需要 BattleSim、数据链检查或 Unity 编译，除非返工越出文档范围。

## 复审路由

复审时先读 `开发管理/审核入口.txt`，再读 `开发管理/AI合作沟通.txt` 当前条目与 `开发管理/任务列表/审核与交接任务.txt`。
