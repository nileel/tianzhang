# 未通过审核清单（✅ 已审核）

## 用途

记录当前仍未通过、已返工待复核或必须由 `2` 流程处理的审核项。历史审核过程已归档至 `开发管理/任务归档/2026-07-12-管理入口瘦身前状态归档.txt`；当前状态以本文件为准。

## 当前未通过/待复核项

- **C-GZ-ENEMY-01（复审不通过）** | 目标提交：`3d984a0114c680669e2de5ac0cd4b9b37c22d7e2`（前次问题：`REV-C-GZ-ENEMY-01-01`）
  - 问题编号：`REV-C-GZ-ENEMY-01-02`
  - 事实依据：档案仍称“战斗胜利后生成结构化结算，掉落条目由既有 `Enemies.csv` 定义”。实际 `DataConfigImporter.ImportEnemies` 未消费 `type`、`aiType`、`realm`、`dropTable` 或 `description`，`TacticalCombatController.CreateDropItems` 仍按 `realmMultiplier` 返回硬编码文本，故该句仍把后续 `EnemyData` 目标写成当前运行时行为，违反任务卡边界。档案的“战斗识别特征”还保留“行动条推进速度低”、低暴击、无闪避、无魂盾等 CT 或范围外属性断言；任务卡仅授权厚甲、迟缓、近战和格挡倾向。
  - 建议修复范围：仅重写 `docs/怪物/关陇玄域/石甲兽.txt`。保留稳定 ID、类型、练气期低阶、关中野外单敌用途、`ai_melee` 最小行为、战斗模板引用、无装备能力和 CSV 掉落候选 ID；删除当前结构化掉落／CSV 消费断言及范围外 CT、暴击、闪避、魂盾断言。掉落仅说明候选 ID，概率与数量由 `N-GZ-REWARD-01` 锁定；不得修改 CSV、Language、asset、场景、AI 或战斗代码。

- **C-GZ-ITEM-01（复审仍不通过）** | 目标提交：`74977b6438d7bb8c734922ddcf777b9ffe5c1e0f`（前次退回提交：`22b7967d733d2769d6998e9f09e9754ef70a5add`）
  - 问题编号：`REV-C-GZ-ITEM-01-01`
  - 事实依据：本次已删除错误风隼引用、唯一掉落来源、显式经济／修炼效果和石甲兽的晶化／矿化断言，但返工未完成。`劣质灵石.txt` 仍把自然灵气渗透、低阶妖兽活动区及浅层灵矿边缘写成来源描述；`石甲碎片.txt` 仍将石甲兽甲壳受击碎裂、体表及外观写成来源／材质事实。上述内容既不能由 `Language.csv` 与 `Enemies.csv` 的名称、ID 和候选引用证明，也不得消费仍未通过的 `C-GZ-ENEMY-01` 档案。`石甲碎片.txt` 又把物品定义为可提交或计数的悬赏击杀证据，越过本卡仅允许掉落或悬赏奖励库存单位的边界。
  - 建议修复范围：仅重写两份物品档案的描述、来源和允许用途。保留与 `Language.csv`、`Enemies.csv` 一致的稳定 ID、显示名、基础资源／妖兽材料类别、可堆叠身份及未来掉落／悬赏奖励库存用途；描述仅服务物品身份，不补写自然生成、矿物、怪物生理、掉落过程、生态或材质事实；来源只表述当前 CSV 的候选引用；删除“悬赏击杀证据”用途。不得修改 CSV、Language、asset、运行时代码或任何参数。

## 复审路由

复审时先读 `开发管理/审核入口.txt`，再读 `开发管理/AI合作沟通.txt` 当前条目与 `开发管理/任务列表/审核与交接任务.txt`。
