# 未通过审核清单（✅ 已审核）

> 2026-06-19 Codex / gpt-5.5 复审：TQ-003 commit 567c3f2 部分通过。旧 #3 Language 重复 key、旧 #4 管理状态校准、旧 #5 未审核状态保留已复核通过；数据链路剩余问题合并为当前 #1。
> 2026-06-17 Codex / ChatGPT5.5 复审通过：从 `开发管理/审核规则.txt` 拆出为动态清单，内容沿用原未通过项；清单内条目仍需逐项修复。依据：40d5887、26e8210。

## 用途

记录 2026-06-16 对既有未审核内容复核后的未通过项，方便后续逐项修复。处理并审核通过后，应按 `开发管理/审核规则.txt` 的“审核记录归档”规则从本清单移除。

## 当前未通过项

1. 数据链路仍未完全闭合（TQ-003 567c3f2 复审部分通过）：
   - `GongFa.csv` 仍为 24 条，docs 功法为 29 篇；太一道庭 5 篇（九江水府箓/三洞法籍录/太一玉格典/万法都箓经/正一盟威箓）仍未入 CSV。
   - 4 条功法 `affiliation` 仍与 docs 归属不符：`gongfa_nanhuachanyandian`、`gongfa_jiansubaopujing` 应为古修，`gongfa_kongwuborejing` 应为太虚观，`gongfa_taiyixuanyijian` 应为兰台书院。
   - `Language.csv` 当前缺少 `faction_guxiu` 与 `faction_lantaishuyuan`；若直接修正 affiliation，Unity 导入器会把缺失 faction key 原样写入 asset。
   - `powershell -ExecutionPolicy Bypass -File tools/check-data-chain.ps1 -FailOnMissingAssets` 仍失败：GongFa 缺 11、Spells 缺 28、Skills 缺 13；Unity `.asset` 有效覆盖尚未收口。
   - A9/A10/A6 相关条目需继续保留「⚠️ 已修改/未审核」，直到上述 CSV/Language/asset 缺口完成并复审。
2. `docs/角色养成/术法设计规范.txt` 未清洁：
   - 第 26 行仍有 `` `n `` 转义残留。
   - 第 28 行仍有 `本命升格\ —\` 反斜杠残留。

## 后续复核建议

1. 修复后重新运行 CSV 行数、docs 文件数、Language 重复 key、CSV→asset 覆盖率检查。
2. 若涉及 Unity asset 生成，应通过 Unity `天章/导入全部配置` 或等价导入流程验证。
