# 未通过审核清单（✅ 已审核）

> 2026-07-05 Codex / gpt-5.5：批量复审当前待审核队列；TQ-027、TQ-031 通过并归档；R-20260630-02 / f6546d8 不通过，已新增待返工项。
> 2026-07-05 Codex / gpt-5.5：复审 TQ-030 / cbefff8 部分通过，清单事实漏判 1 项，待返工后再复审。
> 2026-07-04 Codex / gpt-5.5：当前清单为空；瘦身前全文已归档至 `开发管理/任务归档/2026-07-04-未通过审核清单瘦身前归档.txt`。

## 用途

记录当前仍未通过、已返工待复核或必须由 `2` 流程处理的审核项。历史审核过程不写在本文件正文；查旧记录时读 `开发管理/任务归档/` 与 `开发管理/AI合作归档/`。

## 当前未通过/待复审项

### R-20260630-02 · E1-E4 高阶/炼虚旧口径标注修复（不通过，待返工）

- 复审对象：DeepSeek V4 Pro 提交 `f6546d8`，修改 `docs/基础设定/战斗系统.txt`、`docs/基础设定/角色数值设计.txt`、`docs/基础设定/灵根设定.txt`、`开发管理/炼虚与高阶内容影响面清单.txt`。
- 复审结论：不通过。E2/E3 的旧口径标注意图基本成立，但提交存在阻塞性文本错误与超范围格式改动，不能直接标为已审核。
- 未通过原因：
  1. `docs/基础设定/灵根设定.txt` 第105行将"炼虚"写成了字面量 `` `u{70BC}虚``，正文会直接显示坏字符串。
  2. `docs/基础设定/战斗系统.txt` 在目标提交中出现 281 行新增、380 行删除的大幅重排，超出交接自述的"领域神通行加旧口径/资料片保留标注"范围；该类格式归一化需要单独任务或回退为最小标注 diff。
- 返工范围：修正 `灵根设定.txt` 的坏转义；将 `战斗系统.txt` 收束为最小旧口径标注变更，或单独提交并说明整文件格式重排的必要性；同步校准 `开发管理/炼虚与高阶内容影响面清单.txt` 中 E2/E4 状态。
- 返工后验证：`rg -n "u\\{70BC\\}|修n魂|领域神通|旧口径/资料片|炼虚" docs/基础设定/战斗系统.txt docs/基础设定/角色数值设计.txt docs/基础设定/灵根设定.txt 开发管理/炼虚与高阶内容影响面清单.txt`；`powershell -ExecutionPolicy Bypass -File tools/check-review-text.ps1 -Paths 开发管理,docs/基础设定`；`git diff --check`。

### R-20260704-02 / TQ-030 · 重要 NPC 叙事字段与旧口径污染清单（部分通过，待返工）

- 复审对象：DeepSeek V4 Pro 提交 `cbefff8`，新增 `开发管理/NPC叙事字段检查清单.txt`。
- 复审结论：部分通过。清单覆盖范围与核心阻塞项判断基本成立：`docs/剧情/重要NPC/慕容朔.txt` 第121/124/127行仍使用"妖血敕封席位"，与 `docs/剧情/背景与重要NPC设计规范.txt` 第27行"不得称妖血敕籍，应称妖血契、血脉注入或同类关系"冲突。
- 未通过原因：`开发管理/NPC叙事字段检查清单.txt` 对 `docs/剧情/重要NPC/卫长庚.txt` 的关系数量判断不准确。该 NPC header 第6行只列出【吕星枢】【苻渊】两条强关系，正文第195行起的【姚观寂】只在"关系脉络"中出现；清单却写为"关系限制 ✅ 3条"并未把卫长庚纳入 `NPC-CHECK-003` 强关系不足列表。
- 返工范围：修正 `开发管理/NPC叙事字段检查清单.txt` 中卫长庚逐文件结论、总体统计和 `NPC-CHECK-003` 汇总；保留或继续标明 `NPC-CHECK-001` 慕容朔旧口径为阻塞项，不直接改 NPC 正文。
- 返工后验证：`rg -n "正籍|辅籍|敕籍|寄丹|妖血敕|炼虚" docs/剧情`；`powershell -ExecutionPolicy Bypass -File tools/check-review-text.ps1 -Paths docs/剧情,开发管理`；`git diff --check`。

## 后续复核建议

- 复审 DeepSeek 交接或外部提交时，先读 `开发管理/审核入口.txt`，再读 `开发管理/AI合作沟通.txt` 当前条目与 `开发管理/任务列表/审核与交接任务.txt`。
- 若重生成 StartMenu/World/Settlement/Adventure，复跑 `TianZhang.Editor.SceneBuilder.ValidateSceneArchitectureShellsForBatchMode` 与场景 GUID 专项检查；不得扩大到 CTB、伤害、冷却、槽位语义。
- F1 口径仍以 2026-06-28 Codex / gpt-5.5 结论为准：`realm_lianshen` 不作为当前独立境界 key，`realm_lianxu` / `realm_lianshen` 均按旧高阶/NPC/资料片保留内容处理。
