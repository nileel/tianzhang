# Codex 浏览器桥 DNS 诊断与治理记录

## 2026-08-16 · 首次诊断

### 现象与影响

- Tripo 网页本体约 2.17 秒完成加载，但浏览器桥 DOM／evaluate 读取总耗时约 21.4～22.4 秒。
- 每次读取均出现两个对 `ab.chatgpt.com` 的 Statsig POST 失败，每个请求在 10000 ms 超时，和固定约 20 秒额外等待吻合。
- 本次排查没有上传 Tripo 输入、没有提交生成，也没有消耗 credits。

### 根因证据

- Windows 普通系统解析把 `ab.chatgpt.com` 指向 `199.16.156.40`。
- 传统 DNS 查询链路返回 `69.63.184.30`、`128.242.240.20` 等不一致地址；Google 与 Cloudflare DoH 对照均返回 `104.18.32.47`、`172.64.155.209`。
- 将 HTTPS 请求临时指定到 DoH 返回端点时，连接约 0.61～0.71 秒完成；普通路径约 2.80 秒，浏览器桥仍叠加两个 10 秒遥测超时。
- 由此确认当前直接根因为普通 DNS 路径污染／拦截，浏览器桥 Node 侧遥测没有沿用可用的安全解析路径；Tripo 页面本身和游戏资源占用不是当前主因。若游戏加速器改变 DNS、TUN 或路由，它仍可能是污染路径的触发条件，需在长期任务中用开／关对照确认。

### 短期绕过与回退

- 计划临时把 `ab.chatgpt.com` 映射到本次 DoH 返回的两个端点，并以明确注释标记；该方法只用于恢复当前网页操作，不作为长期方案。
- hosts 原文件 SHA-256：`DF21AFD6FADFA147E2E7C3A0AFFCBB1D01B8F1F146503A82FC8E379CCCEA968A`。
- 原文件备份：`C:\Users\WINDOWS\AppData\Local\Temp\codex-hosts-backup-20260816-01`，备份 SHA-256 与原文件一致。
- 当前进程没有管理员权限；提升后的自动补丁入口不可执行，因此截至本记录写入时 hosts 保持未修改。短期绕过需负责人在管理员编辑器中粘贴标记行，Codex 随后负责解析和浏览器桥复测。
- 长期路径验证通过后，必须删除整段临时标记并再次复测；若短期绕过异常，使用上述备份恢复原文件。

### 长期治理入口

- P0 任务：`A-CODEX-BROWSER-BRIDGE-DNS-01`。
- 目标：建立对浏览器桥 Node 进程生效、可回退且不依赖固定 CDN IP 的安全解析路径，增加只读诊断脚本，并清理临时 hosts 绕过。
