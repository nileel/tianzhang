# 金丹位格批量生产模板

✅ 已审核（2026-08-05 用户确认；Codex 同步具体位置字段与单文档生产路由）

> 本文约束后续角色、丹枢、功法、术法、神通、洞府与事件生产。结构与生命周期以 `元婴锚点与金丹位格设定.txt` 为准；道路与效果以 `元婴锚点与金丹位格矩阵.txt` 为准；装配、兼容与冲突以 `金丹基础效果装配与冲突规则.txt` 为准。具体位置本身必须使用 `金丹位格/金丹位格设计规范.txt` 单独生产，并登记到 `金丹位格/金丹位格索引.txt`。

## 一、通用硬约束

### 1.1 四类显示字段与稳定 ID

每份涉及金丹位格的内容必须分别填写：

| 字段 | 唯一职责 | 合法来源 |
|---|---|---|
| 金丹名称 | 表示所属世界规则 | `road_id` 对应的 `jindanNameKey` |
| 具体位置名称 | 表示世界中被证取和争夺的真实席位 | `positionId` 对应的 `seatNameKey` |
| 位格类型名称 | 表示源位、化位或界位 | 位格类型对应的 `positionNameKey` |
| 效果名称 | 表示当前装配的基础效果 | `effect_id` 对应的 `effectNameKey` |

1. 四类显示字段不得互相吸收，不保存强制拼接标题；具体位置名称不得跟随装配变化，位格类型名称不得使用效果名，效果名称不得加入道路名或源／化／界后缀。
2. 五十一个正式效果名称由 `DS-JINDAN-NAMES-20260718-01` 起草，须经 Codex 审查和用户确认。完成前只引用矩阵的 `effect_id` 与功能性临时代号，批量生产不得自行发明正式名称。
3. 每份内容必须回指稳定 `positionId`、`road_id`、`positionType`、`effect_id` 和矩阵节号，不能从显示文本反推规则对象。

### 1.2 结构硬约束

1. 现行只有十七道路和源位、化位、界位三种真实位格类型；未经用户批准不得创建第十八道路或第四位格类型。
2. 每条道路必须且只能存在一个源位，可以存在多个理念不同的化位和界位；每个具体位置同一时间只能有一名稳定占据者。
3. 金丹初期、中期、圆满共享 `JINDAN` 规则位阶，依次具有一、二、三个真实位格；中期必须取得另一位格类型，圆满取得剩余类型，最终固定为一个源位、一个化位和一个界位。阶段不提供自动压制。
4. 每个真实位格同一时间只装配具体位置白名单中的一项效果。取得位格不表示候选效果同时生效；新增位置不自动新增效果。
5. 同一 `effect_id` 可跨位格纵向嵌套；不同 `effect_id` 默认独立，只有命中已登记 `comboProfileId` 才能组合。组合产物不是新位置；不得按名称、母属性或自由标签临场合成。
6. 每个 `STABLE` 位格必须由一个互不重复的 `primaryCarrierAbilityInstanceId` 主承载；同一紫府神通可辅助其他位格，但效果、支点、资源、充能和冷却只按唯一实例账本计算一次。
7. 成丹时必须建立唯一、不可变的 `JindanCoreBinding`。丹枢核心不可更换、移植、顶替、复制或通过洞府重铸重选；安全洞府只可原子重铸主承载或府位绑定。
8. 稳定金丹任一真实位格丧失或丹枢核心永久毁坏，必须原子触发丹毁死亡；不存在回退阶段、缺位存活或事后补绑。
9. 宗门只能掌握传承、资源、解释、占据方式或局部地域，不能垄断整条道路。夺取、继承、敕封、暂寄和独立应位是证位起点、权利来源或过渡状态，不是新位格。

### 1.3 禁止事项

- 不得改写道路变量、效果权限、源／化／界版本、不可越界项或现实支点。
- 不得让丹枢、组合、辅助神通或表现档案创造额外效果槽、额外位格或新道路权限。
- 不得把兼容编译通过、异效组合成功或普通战斗胜利直接写成自辟元婴成立。
- 不得在未运行 BattleSim 时自定倍率、阈值、目标数、概率、CT、资源费或本命神通数量。
- 不得把元婴写成常规模板、低配版本或可批量复制的位格。
- 不得使用炼虚作为现行可用境界。
- 不得把太一道庭写成全部敕封或位格的垄断者，不得把司马承景写成常规可击杀单位。

### 1.4 每份产出必填字段

```text
road_id：
金丹名称字段：
positionId：
具体位置名称字段：
positionType：SOURCE / TRANSFORM / DOMAIN
位格类型名称字段：源位 / 化位 / 界位
effect_id：
效果名称字段：功能性临时代号；正式名待 DS-JINDAN-NAMES-20260718-01
矩阵回指：
对象与操作边界：
不可越界项：
现实媒介／支点：
代价与容量：具体数值待 BattleSim；仅引用合法配置档案
primaryCarrierAbilityInstanceId：角色实例层填写；设定草案填写主承载要求
辅助接口：只写显式开放贡献，不复制账本
JindanCoreBinding：只确认不可换绑，不临时改名或换核
组合关系：独立 / 同效嵌套 / 已登记 comboProfileId
丹毁边界：真实位格或核心丧失即死亡
```

### 1.5 提交前自检

- [ ] `positionId` 是否登记在索引，`road_id` 是否属于十七道路，`effect_id` 是否属于该位置白名单？
- [ ] 是否将金丹、具体位置、位格类型、效果四类字段分开？
- [ ] 每个真实位格是否只装配一项效果？
- [ ] 不同效果组合是否引用明确 `comboProfileId`，而非自由标签？
- [ ] 每个稳定位格是否使用互不重复的主承载实例？
- [ ] 辅助神通是否只贡献一次并共享唯一账本？
- [ ] 是否保持原 `JindanCoreBinding` 不可换绑？
- [ ] 是否明确真实位格／核心丧失触发丹毁死亡？
- [ ] 是否突破对象、操作、媒介、结果或不可越界项？
- [ ] 是否误将兼容、组合或普通胜利写成自辟元婴？
- [ ] 数值是否标为待 BattleSim 验证并进入配置？
- [ ] 正式效果名称是否仍留给既定命名任务？

### 1.6 具体位置内容路由

1. 每个具体位置必须单独一份文档，路径、必填章节与审核清单以 `docs/基础设定/金丹位格/金丹位格设计规范.txt` 为准。
2. 本文件只约束角色、丹枢、功法、术法、神通、洞府和事件如何消费具体位置，不替代具体位置文档。
3. “山河落点”只作为用途索引；具体位置文档必须另写玩家可实际操作的主场景入口、成本、维护、产出、对抗、失败和战棋反馈。
4. 新增位置必须先完成独立文档并登记索引，再供其他内容引用；不得在角色或事件文档中临时发明未登记位置。

## 二、角色模板

每名角色独立文件，并继续遵守剧情／NPC 生产规范。

```text
【角色名／道号】

历史锚点：借用结构与不照抄声明
叙事层级：本章功能／当前目标／失败代价／行动时限
知识边界：已知／误判／不可知

金丹名称字段：
road_id：
真实位格：列出当前一至三位的 positionId 与 positionType
位格取得顺序：
位格占据方式与状态：

每个位格：
- positionId：
- 具体位置名称字段：
- seatInstanceId：
- positionType：
- effect_id：
- primaryCarrierAbilityInstanceId：
- supportCarrierAbilityInstanceIds：
- boundMansionSlotId：
- bindingState：STABLE / UNSTABLE / TEMPORARY
- compatibilityProfileId：

JindanCoreBinding：boundDanshuCoreId 与不可换绑声明
丹枢接口：媒介／偏向对象／代价分配／丹域景象／战斗表现／宗门叙事
组合关系：独立／同效嵌套／comboProfileId
关系限制：至多三条强关系，其余为阵营或弱联系
战斗表达：战法标签／战略威胁／克制关系，不写未经验证数值
丹毁边界：任一稳定真实位格或核心丧失即死亡
```

## 三、丹枢模板

丹枢是不可变核心绑定下的个人表达接口。它只能在道路与效果已经提供权限时调整媒介、偏向对象、代价分配、丹域景象、战斗表现与宗门叙事，不能修改道路变量、对象、操作、结果或不可越界项。

```text
丹枢名称：临时代号
金丹名称字段：
road_id：
boundDanshuCoreId：
JindanCoreBinding：不可换绑声明
媒介：
偏向对象：
代价分配：
丹域景象：
战斗表现：
宗门叙事：
不可越界：
提供的 fireSeedProfileId 或其他接口：没有合法来源则填无
```

丹枢不得直接充当紫府神通主承载，不得凭空提供特殊火种，也不得为位格增加装配槽。

## 四、功法／术法／神通模板

新增或修改内容必须先读取对应设计规范，并在原模板外补充：

| 字段 | 说明 |
|---|---|
| `road_id` | 适配道路 |
| `positionId` | 适配的具体位置 |
| `positionType` | 源位、化位或界位 |
| `effect_id` | 当前适配效果 |
| 矩阵回指 | 具体章节和效果行 |
| 主承载资格 | 是否可作为某位格独立主承载及兼容理由 |
| 辅助接口 | 显式开放的媒介、触发、代价改道或组合接口 |
| 丹枢接口 | 六种允许个性化维度 |
| 不可越界 | 对象、操作、媒介、结果和道路边界 |

功法必读 `docs/角色养成/功法设计规范.txt` 与 `docs/角色养成/功法/功法设计.txt`；术法必读 `docs/角色养成/术法设计规范.txt` 与 `docs/角色养成/术法/术法设计.txt`；神通必读 `docs/角色养成/神通设计规范.txt` 与 `docs/角色养成/神通/神通设计.txt`。

## 五、洞府与山河模板

```text
洞府／项目名称：临时代号
所在地域：
positionId／road_id／effect_id／positionType：
MountainRiverNode：
OrganizationAuthorityGrant：
维护账户：
mountainRiverUpkeepProfileId：只以灵石作为日常支付
设施、灵脉、阵眼、岗位：分别写为条件或容量
失败档案：降级／暂停／关闭
RegionEventSignal：
不可越界：不得垄断道路、免费生成资源或绕过 WorldStateCommit
```

## 六、事件模板

```text
事件名称：临时代号
触发条件：
涉及 positionId、道路、位格类型与 effect_id：
涉及人物：引用 NPC 档案路径
占据方式或状态变化：
JindanCoreBinding 影响：通常不得改变；若涉及毁核须明确丹毁死亡
主承载／辅助接口影响：
不可越界：不得创建第四位格或改矩阵权限
结果分支：至少两条；位格归属变化必须原子处理死亡与新占据
```

## 七、自辟元婴模板边界

自辟方向必须引用 `自辟元婴方向设定.txt`，并额外证明新高位变量不被十七道路完整解释、能长期存在且可承接。普通 `compatibilityProfileId`、`comboProfileId`、金丹争位或同阶 QTE 都不能替代该证明。成功晋升只在新 `YuanyingRoadAnchor` 原子建立后退出输入三位；新元婴不得处分旧道路位格。

## 八、DeepSeek 交接要求

1. DeepSeek 只在用户或任务明确授权范围内生产，修改后标记「⚠️ 已修改/未审核」并写明实际修改方。
2. 五十一个正式效果名称只由 `DS-JINDAN-NAMES-20260718-01` 处理；该任务不得修改效果功能、位格版本、装配、兼容、冲突或矩阵结构。
3. 每批产出完成后勾选自检、列出边界疑点、更新 `开发管理/AI合作沟通.txt`，交由 Codex／ChatGPT5.5 复审。
4. 未经数值任务和 BattleSim 验证，不得把任何配置默认值称为平衡结论。
