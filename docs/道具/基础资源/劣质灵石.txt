# 劣质灵石 · 正式物品档案

> 稳定 ID：`item_lingshi_low`
> 状态：✅ 已审核（2026-07-29 Codex 直接修正 `REV-C-GZ-ITEM-01-02`）

## 身份

- 正式名称：劣质灵石
- 稳定 ID：`item_lingshi_low`
- 物品类别：基础资源
- 可堆叠：是

## 描述

劣质灵石是稳定 ID 为 `item_lingshi_low` 的基础资源。

## 当前证据

- `Enemies.csv` 中 `enemy_shijiahou` 行将 `item_lingshi_low` 列为掉落候选 ID，具体概率和数量由 `N-GZ-REWARD-01` 锁定

## 允许用途

- 未来可作为掉落或悬赏奖励的库存候选

## 未开放用途

- 不定义价格、汇率、修炼收益、装备效果、炼器配方、使用效果或堆叠上限
- 具体掉落概率、授予数量和 `maxStack` 由 `N-GZ-REWARD-01` 锁定

## 排除声明

- 不修改 `Language.csv`、`Enemies.csv` 或任何 CSV / asset / 运行时代码
- 不建立灵石兑换、货币中间层或全局经济基线
