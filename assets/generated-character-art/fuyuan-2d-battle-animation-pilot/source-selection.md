# 苻渊 2D 战斗动画 pilot · 方法选择

日期：2026-08-25
任务：`A-CHAR-2D-BATTLE-ANIM-PILOT-01`

本表只记录这次隔离苻渊样例的实际试片，不把任何候选的结果外推为正式角色批次成本。身份参考仅为既有 `fuyuan-2d-tactical-sprite/fuyuan_tactical_six_direction.png`；它没有被裁切、扩帧、镜像或作为输出素材使用。

| 方法 | 调研支持／本轮缺口 | 实际工具与授权 | 攻击／受击／死亡六向试片 | 实际分钟／费用／credits／次数 | 返工与结论 | 状态 |
|------|-------------------|----------------|---------------------------|--------------------------------|------------|------|
| 逐帧 | 允许作为候选；本机没有可写的 Aseprite 或 Krita 母源工具。 | 未调用；没有外部许可或 credits 消耗。 | 未尝试，不是质量失败。 | 0 分钟／0 元／0 credits／0 次。 | 缺少可编辑母源工具，不能把它猜成零成本路线。 | 未尝试 |
| 剪纸／骨骼 | 允许作为候选；本机没有 Spine GUI／可编辑 cutout 输入。 | 未调用；没有外部许可或 credits 消耗。 | 未尝试，不是质量失败。 | 0 分钟／0 元／0 credits／0 次。 | 未获得可用于本卡的骨骼制作工具和输入。 | 未尝试 |
| 3D 预渲染 | 允许作为候选；先按项目 Blender 通道只读核验。 | `blender_lab.get_blendfile_summary_datablocks` 于 2026-08-25 返回 localhost:9876 未连接；按项目路由未切换第二条 Blender 通道。 | 试片未开始，不是质量失败。 | 0 分钟／0 元／0 credits／0 次。 | 会话未连接，未创建或伪造 `.blend` 母源。 | 未选定 |
| 有限帧／棋子化 | 符合固定六向、raster atlas 和有限帧合同；不把它称为正式角色批次方案。 | 内置 ImageGen、`remove_chroma_key.py` 和本地 `System.Drawing` 栅格导出；无外部 API、许可费用或 credits。 | 攻击、受击、死亡各完成六方向三帧接触表。攻击第 1 次只生成五行，按失败记录；第 2 次和受击、死亡均通过身份、主色、六向和动作语义的源图人工检查。 | 实际活跃窗口 18 分钟／0 元／0 credits／7 次生成（含 1 次失败）。 | 失败首稿没有第六方向；没有补画、镜像或裁切修复，改用完整六行重生成。攻击为蓄势—命中—回收，受击为预冲击—冲击—恢复，死亡为开始—下沉—终态。 | 选定 |

## 选定母源与边界

- 唯一母源：`source/fuyuan_battle_animation.spine`，SHA-256 `973057B58EE5F4994F0E06F4047D2644658ADE3C41F87A527258CD2FE7A0D037`。它是可编辑的 Spine JSON 状态／事件／atlas 栅格时间线；不被运行时当作骨骼、Animator 或 SkinnedMeshRenderer。
- 该选择的实际输出是六份三帧、六方向 RGBA raster atlas。四角透明、统一 `768×768` cell 与 `(0.5,0.010416667)`（左下原点 8 px）脚底锚点由 `manifest.json` 冻结；任何 Unity 私有缩放、平移或镜像均不在本卡范围。
- 宽袖和双手在源图检查中保持连通可读；本次不含武器、地块、阴影、常驻特效、投射物、法阵、文字或 UI。固定 `VisualBaselineBoard` 的真实遮挡和多单位矩阵仍只由下游 `U-CHAR-2D-BATTLE-ANIM-INTEGRATION-01` 在导入隔离 probe 后验证；本卡未复制资产到 `src/`。
